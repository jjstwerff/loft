reorder user documentation into a more logical flow

update remaining documentation of state.rs with the moved name
validate `Store` allocator | O(B/8) linear scan; no free list    and update this in DEVELOPERS documentation
validate Scope analysis | Borrowed-ref pre-init is incomplete     and update this in DEVELOPERS documentation
Type cycles detection
Hash table | 87.5% load threshold
clear out caveats that are actually fixed

make a plan to allow for a debug option to unwind the current stack and return the called functions with their parameters
make a plan to potentially extend the stack when needed with a debug option that limits that

***** 1.0 *****


Plan an efficient loft script with awk/grep/sed/cat/find type functionality but minimal features for the use during claude development steps, it should reduce the amount of different developing interruptions. Add its own unit testing.

investigate the versus rust conclusions and update the document with it

evaluate ways to get rid of the scratch buffer for strings for efficiency

---- Code generation:
- continue with the rust code generation by evaluating one tests at the time inside /generated/tests/
- per test that needed changes in the code generation evaluate the code quality according to /doc/claude/CODE.md

Evaluate the ways untrusted loft code could compromise a system.

Evaluate how loft could strill trigger a runtime exception

Evaluate how the project can be made easier to develop against

evaluate a debug mode where the stack is predefined with a specific complex pattern where all functions and variables take more space than normal to investigate if they are writing to the correct spots, leaving a scope will restore the specific pattern, writing new variables expect that specific pattern or fail.

Keep the "Format" class usable in user code. Even think about "File".
- Part of library name spacing?


Development:
- create a branch per planning step
- do all the development to fix the issue on a single commit, unit tests and changes combined
  . validate this new code against the CODE.md standards, but do not solve number of lines or code complexity issues due to their disruptive nature of the other code
- when the tests eventually all succeed rebase the current commit
  . first commit the tests with failing tests on ignore
  . the commit the actual changes to the code, split it in multiple commits when multiple parts of the project are involved
  . then undo the ignores on the tests in a commit
  . now commit the structure changes due to size or complexity of the code
  . now commit the documentation changes
- validate this branch via the CI infrastructure

