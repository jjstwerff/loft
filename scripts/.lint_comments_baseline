src/api_diff.rs	* a **removed** member, or one whose type changed, breaks every existing use;
src/api_diff.rs	A break: the named public symbols were removed or changed.
src/api_diff.rs	Diff `old` against `new`. Additions never break; a removed or changed existing public
src/arena.rs	! A keyed collection used to claim one store record per entry. A record carries an
src/arena.rs	! index (@PLN135 arc H).
src/bin/loft-lsp.rs	T3 — Warnings for buffer tags the scanner flagged as broken (a `@P`/`@PLAN`
src/bin/loft-lsp.rs	this the editor shows a message that (since the prose trim) deliberately no longer
src/bin/loft-lsp.rs	uri) or the legacy `rootPath`. Used to locate `<root>/index/`.
src/c_call.rs	! @PLN128 arc C — that ceiling is now the CONTRACT's, not this caller's. It was
src/c_call.rs	! @PLN24 arc B — the interpreter's caller for a `#c` binding.
src/c_call.rs	! The `--native` backend (arc C) hands the declared signature to rustc, which
src/c_call.rs	@I74 — CDylib extension loader: the `#c` direct C-ABI caller (@PLN24 arc B).
src/c_call.rs	@PLN128 arc D — the count goes only where the C signature has
src/c_call.rs	@PLN128 arc E — a float return goes down its own rung, because the value
src/c_call.rs	@PLN24 arc D — open what the program declared, BEFORE any symbol is
src/c_call.rs	@PLN24 arc G — [`resolve`], for a `--native` binary.
src/c_call.rs	@PLN24 arc G — an OPTIONAL library is deliberately not opened here. It is
src/c_call.rs	@PLN24 arc G — is this C library usable RIGHT NOW?
src/c_call.rs	@PLN24 arc G — the construct `--native` emits for an optional library's
src/c_call.rs	A lazily resolved pointer must keep the one thing arc C bought: the
src/c_call.rs	The same ladder, transmuted to return a `double` (@PLN128 arc E).
src/c_call.rs	declared library still happens below when that does not answer — the arc B rule
src/c_call.rs	destination record rather than the value stack (@PLN24 arc D).
src/c_call.rs	duckdb, which is the cost arc G exists to avoid.
src/c_call.rs	is unchanged, a symbol still means one thing — but it is no longer the first
src/c_call.rs	is written once per rung instead of twice. It used to be spelled twice
src/c_call.rs	rather than what only this one can do (@PLN128 arc C).
src/c_call.rs	signature, `atoi("-1")` is -1 — the cell arc C used, now reached through
src/c_call.rs	them would be exactly the divergence arc C was built to stop:
src/c_call.rs	vector carries a count (@PLN128 arc D).
src/c_call.rs	which caller asked for it — arc B's rule, and the lazy path keeps it by
src/c_call.rs	whole of arc G's interpreter half.
src/c_call.rs	widens the search across all of them (arc B: one resolver, one meaning per
src/c_shim.rs	! @PLN24 arc D — build the ANSI-C shim a `#c` library ships.
src/c_shim.rs	@I74 — CDylib extension loader: the ANSI-C shim `#c` libraries compile (@PLN24 arc D).
src/c_shim.rs	consumer went looking for a temporary that no longer existed and Windows
src/c_signature.rs	! @PLN24 arc A — the C signature a `#c` binding declares, and the one place it
src/c_signature.rs	! Nothing calls a `#c` function yet (arc A lands inert). This module parses,
src/c_signature.rs	! `extern "C"` emission, and each target arc E adds — derives its call from
src/c_signature.rs	(@PLN128 arc E) — and that resolution belongs here, where the target
src/c_signature.rs	...and a BARE pointer where it does not (@PLN128 arc D). This is what
src/c_signature.rs	@I74 — CDylib extension loader: the `#c` signature every caller derives from (@PLN24 arc A).
src/c_signature.rs	@PLN128 arc B — by POINTER, not by bit pattern. loft has no
src/c_signature.rs	@PLN128 arc B — these used to prescribe "a shim taking the bit
src/c_signature.rs	@PLN128 arc C — **32, and it binds both backends.** It was 12, enforced on
src/c_signature.rs	@PLN128 arc D — the Fortran shape, and the reason this enum exists.
src/c_signature.rs	@PLN128 arc E — a `vector` reaches C as a pointer into loft's OWN element
src/c_signature.rs	@PLN128 arc E — a float RETURN is no longer refused. The two halves
src/c_signature.rs	@PLN128 arc E — a vector crosses as loft's own element bytes, so the
src/c_signature.rs	@PLN128 arc E — the float pair, ahead of every shape-based arm,
src/c_signature.rs	@PLN128 arc E — the pointer is right and the ELEMENT is
src/c_signature.rs	block (@PLN24 arc C).
src/c_signature.rs	cost two slots each, so it needs 26" — which arc D corrected. A `vector`
src/c_signature.rs	float→bits conversion, so the bit-pattern advice this used to give
src/c_signature.rs	is arc E's subject, not a width question.
src/c_signature.rs	it is what a `vector` argument is checked against (@PLN128 arc E). It is
src/c_signature.rs	straight past the end of the vector (@PLN128 arc E).
src/c_signature.rs	then refused with the cure. The RETURN is neither (@PLN128 arc E): the
src/cache.rs	! foundation. The retired Phase D cache (removed in the integer-i64
src/cache.rs	2 — @PLN127 arc D grew `DbField` by a `nullable` byte (stride 28 → 29). The
src/cache.rs	@PLN11 arc E — SHA-256 of a file's bytes, or `None` if unreadable. Used to
src/cache.rs	@PLN11 arc E — the `(bundle, manifest)` paths for the whole-program cache of
src/cache.rs	`.manifest`) pairs are removed. Best-effort.
src/cache.rs	every bundle idle longer than `ttl` (the primary policy); phase 2 is the
src/cache.rs	loft that no longer exists. Fingerprinting that stale file made
src/cache.rs	stale artifact with the NEW fingerprint (probed 2026-06-12 on
src/cache.rs	this run. **Default ON** (the 3–3.6× warm-start win, no longer hidden behind
src/cache_gc.rs	! An age bound was tried here and removed. "Older than the running loft binary" reads
src/cache_gc.rs	form of the `rm -rf` the docs used to prescribe. Scoped to what this module
src/calc.rs	B2-runtime (2026-04-13): a unit variant in a mixed struct-enum
src/calc.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/codegen_runtime.rs	#620 — this used to be gated on `target_arch = "wasm32"` alone, which also
src/codegen_runtime.rs	(@P327 native). Writes the yielded value's bytes into `dest` and
src/codegen_runtime.rs	2026-05-13 to close the unimplemented set so any loft
src/codegen_runtime.rs	@P290 — clear the soft free-protection.
src/codegen_runtime.rs	@P290 — soft free-protect marker for the fn-call deep-copy bracket.
src/codegen_runtime.rs	@P295 — deep-copy a keyed collection (`sorted`/`hash`/`index`) into a
src/codegen_runtime.rs	@P305 — `coll[key] = value` insert-or-replace into a keyed collection.
src/codegen_runtime.rs	@P307 — clear a keyed collection (`sorted`/`hash`/`index`) held in a
src/codegen_runtime.rs	@P317 — LOFT_LOG=copy_check (native): warn on nested-length divergence.
src/codegen_runtime.rs	@P372: size the target record to the struct, not a fixed 8 words — see
src/codegen_runtime.rs	@PLAN12 phase 3.5a (2026-05-24) — cr_rand_seed / cr_rand_int /
src/codegen_runtime.rs	@PLAN12 phase 3.5a (2026-05-24) — n_rand / n_rand_seed /
src/codegen_runtime.rs	@PLAN12 phase 3.5a (2026-05-24).
src/codegen_runtime.rs	@PLAN49 T1 — refresh the shared breadcrumb at every native fn
src/codegen_runtime.rs	@PLAN51 Cluster II — true alias copy is a no-op. Same rationale
src/codegen_runtime.rs	@PLN119 arc F — `lib/git`'s single native, so a COMPILED program can ask git too.
src/codegen_runtime.rs	@PLN119 arc F — the same four under the names the loft surface uses now
src/codegen_runtime.rs	@PLN127 arc B — `type_of(x)` on the native backend.
src/codegen_runtime.rs	@PLN127 arc C — `type_named(name)` on the native backend, onto the same
src/codegen_runtime.rs	@PLN129 arc A — same miss path as the interpreter, so both backends
src/codegen_runtime.rs	@PLN24 arc G — `c_library_available`, for generated Rust.
src/codegen_runtime.rs	Plan 09 phase 01 step 1.5: migrated to the no-stores ABI
src/codegen_runtime.rs	Plan 09 phase 01 step 1.5: migrated to the no-stores ABI — body
src/codegen_runtime.rs	Plan 09 phase 01 — Cell/Stores ABI consolidation
src/codegen_runtime.rs	Plan-06 phase 1b: each worker reserves a per-worker output Store
src/codegen_runtime.rs	Plan-06 phase 3b.1 — shared epilogue for the native par result store.
src/codegen_runtime.rs	Plan-06 phase 3b.1 — shared prologue for the native par result store.
src/codegen_runtime.rs	Plan-09 phase 09: shape-trait-driven parallel-for runtime.
src/codegen_runtime.rs	Plan-57 Phase C: single-ownership (ref-count removed) — close the file
src/codegen_runtime.rs	Removed the last element: signal end-of-iteration.
src/codegen_runtime.rs	Return milliseconds since the Unix epoch (1970-01-01T00:00:00 UTC).
src/codegen_runtime.rs	Unified store-backed yield channel (@P327 native; designed to absorb
src/codegen_runtime.rs	`iterator<vector<T>>` / any DbRef-shaped yield (@P326).
src/codegen_runtime.rs	fault that is about to be reported through arc C's channel instead.
src/codegen_runtime.rs	for owned-free struct/variant payloads — plan-06 phase 2b refinement).
src/codegen_runtime.rs	helpers; phase 01 migrated all 11 entries to `Cell` (8) or `None` (3).
src/codegen_runtime.rs	mirroring `coroutine_next_i64` (@P326).
src/codegen_runtime.rs	n_rand_indices removed. All three live in lib/random/native/'s
src/codegen_runtime.rs	template (rayon work-stealing pool, plan-06 phase 1.5), mirroring the
src/codegen_runtime.rs	the `external::` module that wrapped them all removed. random's
src/codegen_runtime.rs	── Plan-09 phase 06: parallel-queue runtime (P202 close) ──────────────────
src/compile.rs	@P381 — fixes the "Claim on read-only store (locked by: compile.rs
src/compile.rs	@PLN24 arc B — wire every `#c` declaration into the same static-call
src/compile.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/compile.rs	re-derive relative offsets after ops are inserted or removed (the jump
src/compile.rs	sentinel as the ref-count is removed.
src/copy_manifest.rs	a write through `var` no longer reaches the container. Reported unconditionally: a silent
src/coroutine_layout.rs	! phase 02).
src/crash_report.rs	Plan-07 phase 1 step 1.20 / phase 3 — look up the source position
src/crash_report.rs	Plan-07 phase 1 step 1.20 / phase 3 — pc → source-position table
src/crash_report.rs	Plan-07 phase 1 step 1.20 / phase 3 — publish a snapshot of the
src/crash_report.rs	Plan-07 phase 3 — emit `at file:line:col` when the source
src/crash_report.rs	Plan-07 phase 3 — source-position lookup for the panic hook +
src/crash_report.rs	Plan-07 phase 3 — try to resolve the offending pc to a loft
src/crash_report.rs	The report used to print a bare `op=249`, which names nothing a reader can act
src/data.rs	! Plan-07 phase 6.1 — `Type::name()` must produce loft-surface
src/data.rs	(e.g. the @P344 loop-var reuse check fired on `for f in a {…}` then
src/data.rs	(phase-1 step 1.D), which phase 3's pc→span table consumes.
src/data.rs	@P352: two `Type::Function`s compare by SHAPE (params + return),
src/data.rs	@P379 — a library-qualified database name for a struct/enum-value
src/data.rs	@P379 — the native backend emits each loft function as a flat
src/data.rs	@PLAN22 — local scalar bindings captured-and-mutated by an inner lambda.
src/data.rs	@PLAN22 — names of captured bindings mutated inside this lambda body.
src/data.rs	@PLAN28 Step 2 (C3) — rebuild the derived lookup indices from
src/data.rs	@PLN102 arc C — `#superseded "Y"`: the bare name of the successor symbol
src/data.rs	@PLN102 arc C — is `source` the compilation's OWNED entry project (vs a
src/data.rs	@PLN102 arc C — is the source CURRENTLY being compiled owned (see
src/data.rs	@PLN11 arc D — load a `Data` from a file-backed IR store written by
src/data.rs	@PLN11 arc D — serialize this `Data` to a file-backed IR store at
src/data.rs	@PLN114 A4 — one alignment table. This used to carry an inline copy
src/data.rs	@PLN12 phase 02 — transactional rollback for the REPL statement parser.
src/data.rs	@PLN125 arc B — the scope-end hook declared for a type, or `u32::MAX` when it has
src/data.rs	@PLN24 arc A — the C signature exactly as declared, e.g. `"int(void*)"`.
src/data.rs	@PLN24 arc A — the C symbol a `#c` binding names, e.g. `"PQstatus"`.
src/data.rs	@PLN24 arc D — every C library a loaded package declares, and whether it
src/data.rs	@PLN24 arc D/G — one C library a package declared, as every consumer of the
src/data.rs	@PLN24 arc G — must a `#c` symbol declared in `file` be resolved at RUN
src/data.rs	@PLN24 arc G — the package that owns `file`, among those that declared C
src/data.rs	@PLN25 — the base type with any `Optional` wrapper removed (the agnostic peel).
src/data.rs	@PLN25 — the nullability-aware signature type-name used to KEY an overload: a `τ?`
src/data.rs	@PLN26 phase 1 — body-less `#native` symbols declared by 2+ distinct
src/data.rs	Detection-only at phase 02d-i: the field is populated but
src/data.rs	Phase 09 phase 00 step 0.7 — RawExpr is a codegen-internal
src/data.rs	Plan 09 phase 00 step 0.7 — codegen-internal "raw expression"
src/data.rs	Plan-06 phase 4d (P193): null fn-ref defaults are needed
src/data.rs	Plan-06 phase 4d (P193): tuple struct fields default to
src/data.rs	Plan-06 phase 4d.A.2 — fn-ref element types route to
src/data.rs	Plan-06 phase 4d.A.2 — fn-ref vector elements use 4-byte
src/data.rs	Plan-06 phase 4d.C step 1 — register the synthetic struct
src/data.rs	Plan-06 phase 5a (DESIGN.md D8.1) — purity classification of a
src/data.rs	Plan-06 phase 5a (DESIGN.md D8.1) — purity classification set
src/data.rs	Plan-06 phase 5a (DESIGN.md D8.1) — sub-classification of
src/data.rs	Plan-06 phase 5b' (DESIGN.md D12) — every user fn that calls
src/data.rs	Plan-06 phase 5b' (DESIGN.md D12) — every user-defined
src/data.rs	Plan-06 phase 5b' (DESIGN.md D12) — lazy caller-graph cache.
src/data.rs	Plan-06 phase 5b' (DESIGN.md D12) — recursive walk of a `Value`
src/data.rs	Plan-07 phase 1 — Span is transparent in pretty-print.
src/data.rs	Plan-07 phase 1 — wrap a fault-prone IR construction with its
src/data.rs	Plan-07 phase 1, step 1.1 acceptance: construct a Span, clone it,
src/data.rs	Plan-07 phase 1, step 1.B.0 acceptance: `unspan()` returns the
src/data.rs	Plan-07 phase 1, step 1.B.0 — see through any number of nested
src/data.rs	Plan-07 phase 5 suggestions. Find a similar type name —
src/data.rs	Plan-07 phase 6.1 — explicit user-facing rendering for the
src/data.rs	Plan-14 phase 07 (P234 runtime): does this type carry a lifetime
src/data.rs	Plan-22 phase 01 — names of captured bindings whose value is
src/data.rs	Plan-22 phase 02d-i — names of LOCAL bindings in THIS
src/data.rs	Skips `Value::CallRef` (runtime function reference) — phase 5e
src/data.rs	Source span wrapper (plan-07 phase 1, decision A).
src/data.rs	This definition is referenced by a specific definition, the code is used to update this
src/data.rs	Used to construct `--extern` flags for `rustc`.
src/data.rs	`#superseded "Y"` (@PLN102 arc C): the bare successor-symbol name this
src/data.rs	`Data::forced_size(alias_d_nr)`. When phase 2 routes
src/data.rs	`Purity::Unknown` (no annotation provided); phase 5b's
src/data.rs	`intger` used to suggest nothing.
src/data.rs	`n_<name>`. Turn that silent shadow into a clear error (owner 2026-07-15). It is NOT a
src/data.rs	bare name whose second binding a rollback removed.
src/data.rs	lib_plan-29 W1c: WASM bridge package directories — (`crate_name`,
src/data.rs	lib_plan-29 W1c: WASM bridge routes — loft `#native` symbol
src/data.rs	lib_plan-29 W2: absolute paths to per-library JS host-imports
src/data.rs	module) used to lose its global `(name, 0)` binding on a warm start —
src/data.rs	no longer collides, so a second call is a no-op).
src/data.rs	only required libraries emits what arc C emitted, byte for byte, so the
src/data.rs	packing this used to do described neither side — the callee's frame was
src/data.rs	par-safety analyser (phase 5e) iterates over for the
src/data.rs	phase 3 (codegen rework) have a single place to call.
src/data.rs	phase 5e's analyser pessimises any caller of a CallRef-routed
src/data.rs	reached it — @PLN119 arc F's `engine_host::turn() -> Turn` — and
src/data.rs	registry instead of leaving arc A inert.
src/data.rs	so phase 3's pc→span lookup can run.
src/data.rs	the named remainder of the plan-58 / loft#437 / #457 / #483 family —
src/data.rs	this now resolves used to fall through to the `n_` global lookup below unchanged.
src/data.rs	used to make every `use`d library name unresolvable for the rest of the session
src/data.rs	vector leaves `attr_names` claiming a position that no longer exists, and the next
src/data.rs	widening here no longer costs a byte on the heap.
src/data.rs	─── @PLN11 arc C — store-backed-field read seam ───────────────────────
src/data_store.rs	(@PLN11 arc D). A freshly-opened file-backed store's first `claim(16)`
src/data_store.rs	Lower-level than the named accessors above: the arc B materializer writes
src/data_store.rs	The block used to be inlined here, which cost every `Node` in the image the
src/data_store.rs	rather than silently reading the wrong bytes. `pub(crate)` so the arc B
src/data_store.rs	record's field, for the arc B materializer's root).
src/data_store.rs	─── Write side (arc B materializer) ─────────────────────────────────────
src/database/allocation.rs	(@P290/@P306/@P318/@P309) lived in the per-dispatcher copies of this
src/database/allocation.rs	@P290 — currently unused but kept available).
src/database/allocation.rs	@P309 — claim by element COUNT (header word + one 4-byte rec-id
src/database/allocation.rs	@P317 debug — `LOFT_LOG=copy_check` (or `LOFT_COPY_CHECK=1`): after a
src/database/allocation.rs	@P317 — enrich the tripwire: this fires when the free-bitmap and the
src/database/allocation.rs	@P317 — entry point for the `copy_check` validator. Compares the
src/database/allocation.rs	@P317 — the previous per-store `N(bc:created_at)` listing (truncated to 5 by
src/database/allocation.rs	@P318 — deep-copying a struct whose `hash<T[k]>` field is populated must
src/database/allocation.rs	@P318 — re-INSERT each entry into an emptied destination via `hash::add`
src/database/allocation.rs	@PLAN38 — bind the Store at `slot` to a file at `path`, returning
src/database/allocation.rs	@PLAN38 — build the Store byte image to persist, exactly `target_words`
src/database/allocation.rs	@PLAN53 cluster 3: sound disjoint-borrow cross-store copy
src/database/allocation.rs	@PLN118 arc D — LOFT_NO_SLOT_REUSE=1: never reclaim a freed slot; always
src/database/allocation.rs	@PLN129 arc A — a worker inherits NO lazy binding, deliberately. A
src/database/allocation.rs	@PLN129 arc A — bind a COLLECTION to a lazy source, so a lookup that
src/database/allocation.rs	@PLN129 arc A — the MISS path: the one place the outside world is
src/database/allocation.rs	@PLN129 arc B step 1 — through the SOURCE seam. Which source this is
src/database/allocation.rs	@PLN129 arc B step 7 — a rebind re-decides the schema too: it
src/database/allocation.rs	@PLN129 arc C — a failed fetch is TWO different facts, and answering
src/database/allocation.rs	@PLN129 arc C — acknowledge this collection's fetch failures. True when
src/database/allocation.rs	@PLN129 arc C — how many fetches for this collection could not reach its
src/database/allocation.rs	@PLN129 arc C — why a fetch could not reach this collection's
src/database/allocation.rs	@PLN129 arc D — has the source moved under this binding since it was
src/database/allocation.rs	@PLN129 arc D — pin the source's identity at bind. Rebinding RE-pins,
src/database/allocation.rs	@PLN129 arc D — the source must be the one this collection was pinned
src/database/allocation.rs	@PLN135 arc H — a slot holds an arena INDEX for a hash that owns its
src/database/allocation.rs	@PLN135 arc H — an entry is a SLOT at a known stride, so its extent is
src/database/allocation.rs	@PLN135 arc H — an entry is a SLOT in the arena, not a record of
src/database/allocation.rs	@PLN135 arc H — an entry is a SLOT, so `owning_elem` is `None` and the
src/database/allocation.rs	@PLN97 arc G / #632 — the keyed-collection type a paged loader should use,
src/database/allocation.rs	@PLN97 arc G Phase 0 — load a persisted store IMAGE over HTTP(S) from a
src/database/allocation.rs	@PLN97 arc G Phase 0 — load a whole store IMAGE over HTTP(S)/`file://` from
src/database/allocation.rs	@PLN97 arc G Phase 0.
src/database/allocation.rs	@PLN97 arc G Phase 1 (#522).
src/database/allocation.rs	@PLN97 arc G Phase 2 — load a store IMAGE from a local file that may be
src/database/allocation.rs	@PLN97 arc G Phase 3a — load the requested integer keys' entries from a
src/database/allocation.rs	@PLN97 arc G Phase 3b.6 — load ONE TEXT-keyed entry from a persisted
src/database/allocation.rs	@PLN97 arc G Phase 4 / 3b.7 — load the entries with integer key in
src/database/allocation.rs	A kind that becomes servable and is not removed from here keeps refusing a
src/database/allocation.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/database/allocation.rs	O(n log n) key sort. Used to feed `for e in h par(...)`: the parallel
src/database/allocation.rs	Plan-06 phase 2 step 2b — narrow rebase path for structs whose
src/database/allocation.rs	Plan-57 Phase C: the Stores ref-count is removed. Every non-pinned
src/database/allocation.rs	The historical `@P290`/`@P306`/`@P318`/`@P309` bugs were all in this walk,
src/database/allocation.rs	`@P290`/`@P306`/`@P318`/`@P309` family.
src/database/allocation.rs	`index_body`, `array_body`, and `seq_vector` (folded H10, 2026-07) — so the
src/database/allocation.rs	`tree::add`) stays per-kind: unifying it is how @P318/@P309 come back.
src/database/allocation.rs	always comfortably above the mark. `store_reclaim` (arc A) trims
src/database/allocation.rs	back out of it, so the caller no longer has to know which of the two it is.
src/database/allocation.rs	bare message did during the @P311/@P313/@P317 store-ownership hunts.
src/database/allocation.rs	byte 8; a hash entry is a slot inside an arena chunk (@PLN135 arc H).
src/database/allocation.rs	directly (e.g. `kt=68 ChunkKey×6026`) is what pinpointed the @P317 native
src/database/allocation.rs	free_named. There is no ref-count (plan-57 phase C removed it):
src/database/allocation.rs	header for N and fill N-1 slots (the @P309 shape) — the nightly
src/database/allocation.rs	high-water (plan-57).
src/database/allocation.rs	loft#710: a persisted store used to be the arena's whole CAPACITY, and the
src/database/allocation.rs	loft#783 had just removed (109 → 1 222 on the guard) while measuring no further
src/database/allocation.rs	no longer has.
src/database/allocation.rs	of its own. A hash entry is an arena SLOT (@PLN135 arc H): its width is the
src/database/allocation.rs	rather than record numbers — a hash's arena entries (@PLN135 arc H).
src/database/allocation.rs	re-claimed it promptly; @PLN135 arc H made a hash's first insert claim an
src/database/allocation.rs	re-derived (@PLN135 arc H).
src/database/allocation.rs	rehash invariants, subsuming the earlier @P317 length-word and @P290
src/database/allocation.rs	reinitialise a store that was previously locked by a const
src/database/allocation.rs	remaining @PLN97 arc G work. Hash-as-field IS supported (Hash never
src/database/allocation.rs	sentinel + the `ref_count > 1` guard below as the ref-count is removed.
src/database/allocation.rs	silent pass: an unreachable source is already arc C's business.
src/database/allocation.rs	stays per-kind, including the @P309 length header written above.
src/database/allocation.rs	tail we removed. An eighth keeps ordinary post-bind activity off
src/database/allocation.rs	type, not from heap pointers — so the walk itself cannot fault). Used to NAME the
src/database/allocation.rs	whole. A writable source keeps it CO-LOCATED, and that case used to be
src/database/descriptor.rs	@PLN127 arc D — was this field DECLARED nullable?
src/database/descriptor.rs	entries are slots in a chunked arena and have no record of their own, @PLN135 arc H).
src/database/io.rs	@P321(c): a PNG `--html` auto-discovered beside the entry file is
src/database/io.rs	@P334 fix (2026-05-29): widen the active-impl cfg from
src/database/io.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/database/io.rs	Plan-06 phase 4d.C step 2: see read_data's DbRef arm.
src/database/io.rs	Plan-06 phase 4d.C step 2: stored DbRef pointer is
src/database/io.rs	containment refusal used to skip filling the struct at all, and a handle
src/database/journal.rs	@PLN16.J phase 3 — the `Stores` recording drain. Structural ops buffer per-store
src/database/journal.rs	RAM (mmap is optional and not taken here); the blob is removed on drop.
src/database/journal.rs	The append-only payload file. Always a file (never a store), removed on drop for
src/database/lazy.rs	@PLN129 arc B step 7 — why this schema cannot serve this collection, or
src/database/lazy.rs	@PLN129 arc B step 8 — fetch a KEY RANGE from this collection's bound
src/database/lazy.rs	@PLN129 arc B — the SOURCE seam: what a lazy collection consults on a miss.
src/database/lazy.rs	@PLN129 arc B2 — run an explicit predicate against this collection's
src/database/lazy.rs	Record a refusal against a collection, through arc C's channel.
src/database/lazy.rs	The `.store` image source — arc A's fetch, unchanged.
src/database/lazy.rs	Through arc C's channel rather than a return code: a count cannot carry a
src/database/lazy.rs	be there, and a query that does not run reports through arc C.
src/database/lazy.rs	outcomes, because two of them are the reason arc C exists.
src/database/lazy.rs	which is arc A's rule unchanged: this never hands a record back
src/database/mod.rs	@P275 fix.
src/database/mod.rs	@P356 — Stores-side counterpart of `State::raise_recoverable`. Logs a
src/database/mod.rs	@PLAN53 cluster 2 / S4: pop the value's STEPPED span (8-rounded in
src/database/mod.rs	@PLAN53 cluster 2 / S4: push the value's STEPPED span so a native
src/database/mod.rs	@PLN127 arc D — was this field DECLARED nullable?
src/database/mod.rs	@PLN129 arc A — a collection bound to a LAZY source, keyed by the
src/database/mod.rs	@PLN129 arc B step 7 — the verdict on a database schema loft does not own.
src/database/mod.rs	@PLN129 arc B step 7 — whether this source's schema can serve this
src/database/mod.rs	@PLN129 arc C — why a fetch for this collection could not reach its
src/database/mod.rs	@PLN129 arc D — a collection's lazy source, plus the identity it was PINNED
src/database/mod.rs	@PLN14 arc A — move a `Store` OUT of this table, leaving a freed sentinel
src/database/mod.rs	@PLN14 arc B — **the materialize chokepoint**: give the value rooted at
src/database/mod.rs	A lexical `..` refusal used to live on this path: it rejected
src/database/mod.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/database/mod.rs	Plan-06 phase 1 — marker telling the parent which slot in this
src/database/mod.rs	Plan-06 phase 2 — extract every worker-allocated Store
src/database/mod.rs	Plan-06 phase 2b prereq — adopt all stores a worker allocated
src/database/mod.rs	Plan-06 phase 4d.C step 2: 12-byte stored DbRef pointer (store_nr
src/database/mod.rs	Plan-07 phase 4 — typed runtime error captured by the most recent
src/database/mod.rs	Plan-07 phase 4c — Stores-side counterpart of
src/database/mod.rs	Plan-07 phase 4c — Stores-side counterpart of `State::raise`.
src/database/mod.rs	Plan-07 phase 4e.3 — `LOFT_FORMAT_BARE_NULL=1` opt-out for the
src/database/mod.rs	Plan-07 phase 4e.3 — fault-kind tag for the next format-string
src/database/mod.rs	Plan-07 phase 4e.3 — read + clear the format-fault tag.
src/database/mod.rs	Plan-07 phase 4e.3 — set the next-format-render fault tag.
src/database/mod.rs	Plan-07 phase 4g.3 — `--dev-soft-halt` mirror of the
src/database/mod.rs	`vec_get_or_raise_runtime`, etc. @P275 fix.
src/database/mod.rs	allocations. See plan-06 DESIGN.md D2.1 for the rationale.
src/database/mod.rs	either sees one world or is told it did not, which is the same contract arc C
src/database/mod.rs	first push) died on "Using a freed store" (the @P317
src/database/mod.rs	value, nested-struct field, collection entry). NOTE (corrected 2026-07-08): this is a
src/database/mod.rs	watermark** plan-57 tracks (the peak the `LOFT_STORES=log` trace prints
src/database/search.rs	@P305 — insert-or-replace a value into a keyed collection
src/database/search.rs	@P306 — before linking a new keyed record, drop any EXISTING record
src/database/search.rs	@P317 — LOFT_LOG=copy_check: warn if the keyed deep copy changed any
src/database/search.rs	@PLN135 arc H — a HASH entry is a slot in a chunked arena, not a record
src/database/search.rs	@PLN135 arc H — a hash entry's storage goes back to its ARENA; a
src/database/search.rs	@PLN135 arc H — two entries of the same hash now share a chunk RECORD, so
src/database/search.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/database/search.rs	`other_indexes` analogue of the @P305 `keyed_field_is_linked` update-only
src/database/search.rs	already exists it is removed first (so the key stays unique — dedup),
src/database/search.rs	leak the record. `Array` is left out deliberately: it is removed by
src/database/snapshot.rs	! @PLAN28 Design A — JSON codec for the database **schema** (`Stores.types`
src/database/sql_query.rs	@PLN129 arc B — deriving the query from the store's own schema.
src/database/sql_source.rs	(QUERIES.md § arc B). Resolving the symbols from Rust needs none of that, and
src/database/sql_source.rs	@PLN129 arc B step 4 — the database source that EXECUTES.
src/database/sql_source.rs	@PLN129 arc B step 7 — can this schema serve this query? `Ok(())` or the
src/database/sql_source.rs	database still maps nothing (@PLN24 arc G's property, kept).
src/database/sql_source.rs	read-only — both of which are arc C's *unreachable*, never an absence.
src/database/sql_source.rs	replaces, so the handle is opened once and reused — which is also what arc D
src/database/structures.rs	(COPY_DIAGNOSTICS.md phase 2).
src/database/structures.rs	@P305 — true when the keyed field at byte offset `byte_off` in
src/database/structures.rs	@P306 — replace any existing record with this key (dedup).
src/database/structures.rs	@P306 — replace any existing record with this key (dedup);
src/database/structures.rs	@P357: an EMPTY JSON array must still zero the collection
src/database/structures.rs	@P366: an unknown JSON key has no matching struct field.
src/database/structures.rs	@P373: write the default to the COLLECTION FIELD's slot,
src/database/structures.rs	@P376 — Array(content) → Array(content) append. When the content
src/database/structures.rs	@P376 — when `known` is "linked" (multiple containers share this
src/database/structures.rs	@PLAN53 cluster 3: sound cross-store byte copy. Copies `len` bytes from
src/database/structures.rs	@PLAN53 cluster 3: sound disjoint-borrow cross-store copy.
src/database/structures.rs	@PLAN53 cluster 3: two different data structures in distinct stores —
src/database/structures.rs	@PLN135 arc H — a hash's entries live packed in a chunked arena instead
src/database/structures.rs	@PLN90 phase 1 — make the copy visible. A non-empty source means `vector_add`
src/database/structures.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/database/structures.rs	Plan-06 phase 4d.C step 2: default for a 12-byte
src/database/structures.rs	Plan-06 phase 4d.C step 2: stored DbRef pointer has no
src/database/structures.rs	an unknown tag (e.g. a variant removed by a schema edit)
src/database/structures.rs	crate; the legacy hand-rolled scanner was removed when
src/database/structures.rs	per-element offsets). Used to populate the `at` field of
src/database/structures.rs	so filling it does not rebuild the table on the way (@PLN135 arc C).
src/database/structures.rs	the @P357 case), so that path is preserved.
src/database/types.rs	(which used to disrupt largest-first packing and pull the
src/database/types.rs	@P375: a vector field carries no nullable flag — it is always at
src/database/types.rs	@P375: only an UNSET text (str_rec 0) is null; an allocated
src/database/types.rs	@PLN127 arc D — mark a field as DECLARED nullable.
src/database/types.rs	Comparing counts alone would miss a rollback that removed one name and
src/database/types.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/database/types.rs	Plan-06 phase 2 — true iff `tp` (a struct or struct-enum-variant)
src/database/types.rs	Plan-06 phase 2b refinement — per-variant ownership for
src/database/types.rs	Plan-06 phase 4d.C step 2 — register the 12-byte raw `DbRef` storage
src/database/types.rs	Plan-22 phase 02c (2026-05-12): override the alignment from the
src/database/types.rs	only half left the schema holding names whose definitions no longer
src/database/types.rs	the containing struct's size/align both 0. Symptom: phase 02c's
src/database/types.rs	those indices, when used to look up fields, return EXACTLY
src/debugger.rs	6 character), used to render the value.
src/debugger.rs	@PLN140 arc B/C — the loft-level sampler, when `LOFT_PROFILE` /
src/debugger.rs	history used to be dropped wholesale at every step, silently discarding edits that
src/debugger.rs	in place. Never mutated or removed after a push, so the pointers stay valid.
src/debugger.rs	survives frame exit). A stack watch is dropped once its frame is no longer live.
src/debugger.rs	the message when the entry can no longer be applied.
src/debugger.rs	user wrote (`i`) is now in the frame, so its hidden counter is no longer the
src/diagnostic_render.rs	! Plan-07 phase 2 — pretty diagnostic renderer.
src/diagnostic_render.rs	! Plan-07 phase 4 will wire runtime errors through the same
src/diagnostic_render.rs	! renderer; phase 5 adds suggestions; phase 6 adds typed-mismatch
src/diagnostic_render.rs	Number of decimal digits in `n` (used to size the gutter).
src/diagnostic_render.rs	full unicode-width handling deferred to phase 4 if needed).
src/diagnostics.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/diagnostics.rs	Plan-07 phase 5: suggestion with a short-name guard.
src/diagnostics.rs	The same line with its `[code]` tag removed, for comparing against prose written before
src/documentation.rs	File stem of this page (`"05-float"`), used to build the canonical URL.
src/documentation.rs	and `;` removed — so `pub struct Rect {` → `pub struct Rect`,
src/engine_host.rs	! @PLN18 phase 01 — the **engine-host kernel natives**: the Rust mechanics behind
src/engine_host.rs	! Scope: the **events class** (WS text frames, queue-to-empty) from phase 01,
src/engine_host.rs	2026-06-10: the game developer is never bothered with transport). Browsers
src/engine_host.rs	@PLN119 arc F — the four the loft surface now reaches through a WRAPPER.
src/engine_host.rs	Measurement-only loss injection (@PLN18 phase 04, the loss%
src/engine_host.rs	The editor's stop for a SWAPPED game (no longer its child).
src/engine_host.rs	The wire-schema-as-data table, minimal form (user-directed 2026-06-10):
src/engine_host.rs	── The BROWSER kernel (@PLN18 phase 07) — the same loft script on a phone ──
src/extensions.rs	(loft#739's neighbour — see `load_c_library`, which used to do exactly this).
src/extensions.rs	@P370: a plain loft `integer` is 64-bit (8-byte slot) — it must
src/extensions.rs	@P370: plain loft `integer` is 64-bit → I64; only explicit narrow
src/extensions.rs	@P388: serialise on-demand native builds ACROSS PROCESSES. Parallel test
src/extensions.rs	@PLAN12 Phase 6b — compute the target dir for a package's
src/extensions.rs	@PLAN12 Phase 6b — target root via the shared helper (redirects
src/extensions.rs	@PLAN12 phase 3.5a (2026-05-24) — added so the `--native` codegen
src/extensions.rs	@PLN118 arc F — non-perturbing attribution of the shared-return orphan.
src/extensions.rs	@PLN119 arc A: a process-placed library's symbol is served by a worker
src/extensions.rs	@PLN24 arc B — the same lookup, for the `#c` caller: a C binding resolves
src/extensions.rs	@PLN24 arc D — `dlopen` a C library a package declared with `[c] libs`, and
src/extensions.rs	@PLN24 arc G — an OPTIONAL library that does not open is an ordinary
src/extensions.rs	@PLN26 phase 3 — cross-build a `#native` package's crate to a wasm `target`
src/extensions.rs	Neither the registry nor `try_dlsym` (phase 1) found it: the owning
src/extensions.rs	P244-windows fix #2 (2026-05-12): use PathBuf::join, not
src/extensions.rs	Pre-2026-05-12 this function checked ONLY the cdylib before
src/extensions.rs	`try_dlsym` to look up symbols from previously loaded cdylibs *and* report
src/extensions.rs	and panics at call time — the legacy raw-ptr marshaller it used to fall back
src/extensions.rs	attribution instrument to check the retbuf-vs-return orphan (@PLN118 arc F).
src/extensions.rs	candidate loop used to accept a path on `exists()` alone, so on macOS a
src/extensions.rs	covers the latent end-user-parallelism half of @P388.) The `--locked`
src/extensions.rs	here is a BRIDGE fault, and it used to read exactly like a program fault:
src/extensions.rs	loaded after one that used the protocol no longer false-positives, while
src/extensions.rs	serving. Read by the `#c` dispatcher (@PLN24 arc B) for the same reason the
src/extensions.rs	to has been removed now that every native library ships bridges.
src/fuzz_keyed.rs	! surviving key looks up its value, removed/absent keys look up `null`, and
src/fuzz_keyed.rs	Per-key lookups: survivors return their value, removed keys return null.
src/fuzz_keyed.rs	prefix match recognised used to read as a REJECTION (@PLN131).
src/fuzz_keyed.rs	removes — plus `(n, removed, survivors)` for the caller's tail. Shared by
src/fuzz_oracle.rs	prefix match recognised used to read as a REJECTION (@PLN131).
src/generation/calls.rs	@P275 fix.
src/generation/calls.rs	@P299 — a text-returning fn-ref call (`CallRef`) lowers to
src/generation/calls.rs	@P304 — fire for ANY non-`Op` text-returning call, USER
src/generation/calls.rs	@P321b — `OpSetText` with a NULL value must store the null pointer
src/generation/calls.rs	@P361 — a `#rust` body that names the loft-internal pub modules
src/generation/calls.rs	@P386: a text-result `Value::Block` argument whose body
src/generation/calls.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/generation/calls.rs	Phase 09 phase 00 step 0.4: dispatches through
src/generation/calls.rs	Plan 09 phase 01 added per-fn ABI tagging via the
src/generation/calls.rs	Plan-07 phase 4 — RuntimeErrorKind enum lives in
src/generation/calls.rs	Plan-07 phase 4c — Stores-side counterparts of the
src/generation/calls.rs	State::text_char_or_raise helpers (added 2026-05-11 by 4a).
src/generation/calls.rs	`output_block`'s @P321e/@P323 materialisation at
src/generation/calls.rs	rewrite is NO LONGER done here per-call-site: it missed an emission
src/generation/calls.rs	time (`OpGetVector` @P321d, again @P338, then `reserve` on a hash), each
src/generation/coroutine.rs	- `state_idx`: the state number for this segment (used to name the struct field)
src/generation/coroutine.rs	@P326 — Reference / Vector / struct-enum yields are DbRef-shaped.
src/generation/coroutine.rs	@P326 — Reference-yielding generators override `next_dbref` (not
src/generation/coroutine.rs	@P326 — also pre-declare `__ref_*` work-vars used by
src/generation/coroutine.rs	@P326 — for-body factory must use the DbRef channel for
src/generation/coroutine.rs	@P327 / @P328 native — tuple-of-(integer|float) AND fn-ref yields
src/generation/coroutine.rs	@P328 native — pack the fn-ref `(u32, DbRef)`
src/generation/coroutine.rs	@PLAN16 phase 02 — a tuple whose every element classifies into a
src/generation/coroutine.rs	@PLAN16 phase 02 — layout-driven flatten-walk.
src/generation/coroutine.rs	The tail used to be dropped on the floor: `pre` accumulated it and the function returned
src/generation/coroutine.rs	`DbRef::NULL`, NOT `null_named` — the same @P317 rule the ordinary local path
src/generation/dispatch.rs	(cell), not `&mut Stores` (stores). Plan 09 phase 01 added
src/generation/dispatch.rs	@P283 — source is a `RefVar(Text)` argument (`&mut String`).
src/generation/dispatch.rs	@P290 — evaluate the call BEFORE touching the destination. The
src/generation/dispatch.rs	@P298 / @P297 (native half) — free the callee's return store
src/generation/dispatch.rs	@P302 reassignment / `s = []` clear: the slot already holds a
src/generation/dispatch.rs	@P302 — capture first-vs-reassign BEFORE the `declared` insert below
src/generation/dispatch.rs	@P317 — a struct `Reference` local with no resolvable
src/generation/dispatch.rs	@P321g: unspan `to` first. The parser wraps an assignment RHS in
src/generation/dispatch.rs	A coroutine-persistent field used to be excluded too, on the reasoning that
src/generation/dispatch.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/generation/dispatch.rs	Phase 09 phase 00 step 0.6: registry-first dispatch. When a
src/generation/dispatch.rs	`"a".to_string()` wrap on the inner literals (plan-14 phase 02).
src/generation/dispatch.rs	`first == false` is an @P302 keyed-collection reassignment (`s = []`
src/generation/dispatch.rs	keeps the @P302 in-place `s = []` clear from leaking the old store.
src/generation/dispatch.rs	this, plan-14 phase-02 cells like
src/generation/emit.rs	(String, …) tuple-return literal (@P330) emit the bare name.
src/generation/emit.rs	@P321e / @P323 — a TEXT value-block's `_ret` is typically a
src/generation/emit.rs	@P321e — also route through scratch when the return
src/generation/emit.rs	@P323 materialisation at line ~1382) when the block holds a
src/generation/emit.rs	@P386: a text-result if-expression where any branch is a Block
src/generation/emit.rs	@P386: when an if-branch is a Text-result Block whose
src/generation/emit.rs	@PLAN52 cluster I iteration 2 (2026-05-30): force the
src/generation/emit.rs	@PLAN52 cluster I/VI helper: walk a Block's operators (recursively
src/generation/emit.rs	@PLAN52 cluster VI (2026-05-30): closures (and
src/generation/emit.rs	@PLAN52 cluster VI (2026-05-30): closures returning text
src/generation/emit.rs	@PLAN52 cluster VII: when the if-result is `Text`, branches can
src/generation/emit.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/generation/emit.rs	E0308 a variable thread count used to hit — and would also mint `i32`
src/generation/emit.rs	P205 (plan-09 phase 07): if the function returns
src/generation/emit.rs	P205 (plan-09 phase 07): when this function returns
src/generation/emit.rs	P208 (plan-17 phase 01 follow-up): the same redundancy
src/generation/emit.rs	P240 fix (2026-05-11): bounded-generic T-stubs that return a
src/generation/emit.rs	Phase 09 phase 00 step 0.7 — fn-ref dispatch routes each
src/generation/emit.rs	Plan-06 phase 4d: fn-ref field read emits the (u32, DbRef)
src/generation/emit.rs	Plan-07 phase 4 — pre-bind the body's text
src/generation/emit.rs	Plan-14 phase 07 (P234 runtime closure). Function
src/generation/emit.rs	here became dead code and was removed.
src/generation/emit.rs	phase 07 / P234) does NOT fire for generic
src/generation/emit.rs	tail-call bug, feedback 2026-07-02). parse_return-created
src/generation/emit.rs	variable is marked `skip_free`. Used to gate scratch-buffer
src/generation/mod.rs	(`extensions::compute_sig`, @P370): only an explicit
src/generation/mod.rs	- @PLN24 arc D — a **`#c` binding** returning a C `char *`. Its wrapper owns
src/generation/mod.rs	@P296 — keyed-collection types reachable only as a struct/enum FIELD
src/generation/mod.rs	@P299 — a fn-ref stored into a struct FIELD lowers to
src/generation/mod.rs	@P302 in-place clear, which must NOT re-run `null_named` (that would orphan the store).
src/generation/mod.rs	@P310 regression: the FFI element pointer width must track the vector's
src/generation/mod.rs	@P310: the FFI data-pointer element width must match the
src/generation/mod.rs	@P321c (2026-05-25) — a struct `Reference` ARG (not just a
src/generation/mod.rs	@P321c LoftRef convention (the interpreter's `ArgT::Ref`,
src/generation/mod.rs	@P321c browser-WASM (2026-05-29): skip the host-import
src/generation/mod.rs	@P321c browser-WASM fix (2026-05-29): for store-mutating `#native` fns
src/generation/mod.rs	@P326 twin for the eager-collect emitter: the generator yields a
src/generation/mod.rs	@P328 — `yield <fn-ref>` for a non-capturing closure emits as
src/generation/mod.rs	@P353: an empty `vector<fn(…)>` literal registers its
src/generation/mod.rs	@P353: flush the content bare type if it is registered
src/generation/mod.rs	@P353: the narrow element type may have a higher
src/generation/mod.rs	@P379 — emit the REGISTERED database name (library-qualified on a
src/generation/mod.rs	@PLAN12 phase 2 step 2 (2026-05-24): check `def.native` FIRST.
src/generation/mod.rs	@PLAN12 phase 3.5a (2026-05-24) — LoftStore forwarding for
src/generation/mod.rs	@PLAN12 phase 3.5a (2026-05-24) — removed the `mod external {…}`
src/generation/mod.rs	@PLAN37 phase 10.3 fix.
src/generation/mod.rs	@PLAN52 cluster IV-Vec-nested-field-push secondary (2026-05-30):
src/generation/mod.rs	@PLN128 arc E — a float return. The extern already declares `f64`
src/generation/mod.rs	@PLN24 arc C — a `#c` binding: call the C symbol directly,
src/generation/mod.rs	@PLN24 arc C — emit the body of a `#c` binding: marshal the loft
src/generation/mod.rs	@PLN24 arc C — one typed `extern "C"` declaration per `#c` symbol.
src/generation/mod.rs	@PLN24 arc D — a `char *` return, copied into an owned `String`
src/generation/mod.rs	@PLN24 arc E — except on a wasm target, where nothing is declared and
src/generation/mod.rs	@PLN24 arc E — is this target one a `#c` binding cannot reach?
src/generation/mod.rs	@PLN24 arc E — the refusal, at the only site that knows a `#c`
src/generation/mod.rs	@PLN24 arc G — a symbol from a package that declares an OPTIONAL
src/generation/mod.rs	@PLN26 phase 0.2 — declare `u8` (loft's boolean storage
src/generation/mod.rs	@PLN26 phase 1 — `#native` symbols exported by 2+ packages. A *reachable*
src/generation/mod.rs	@PLN26 phase 1 — a `#native` symbol exported by 2+ packages
src/generation/mod.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/generation/mod.rs	D-html-vec (2026-07-04): a `vector<T>` arg IS a valid host import —
src/generation/mod.rs	Plan-06 phase 4d: tuple struct fields inline the
src/generation/mod.rs	Plan-12 phase 1a (2026-05-23) — `is_crypto_runtime_symbol` removed.
src/generation/mod.rs	Plan-22 phase 02c (P258 native fix, 2026-05-12): auto-Reference
src/generation/mod.rs	Register one field in the generated `init()`, then — @PLN127 arc D —
src/generation/mod.rs	Routed via lib_plan-29: each library's `[wasm.bridge].routes`
src/generation/mod.rs	The store-mutating shapes keep their graceful @P321c stub —
src/generation/mod.rs	The three ways this used to end, all measured on one tree: a
src/generation/mod.rs	Used to emit Rust labeled breaks for `Value::Break(n)` with n > 0.
src/generation/mod.rs	Used to inject `cr_call_push` / `CallGuard` for shadow call stack support.
src/generation/mod.rs	`Str` via `Deref<Target=str>` (the @P304 path — same as the
src/generation/mod.rs	a Reference arg (@P321c made that combination reachable).
src/generation/mod.rs	arc E.
src/generation/mod.rs	as the @P299 (`OpSetInt4(field, …)`) and @P328 (`yield`) recoveries.
src/generation/mod.rs	as the @P299 fix for `OpSetInt4(field, pos, Int(d_nr))`;
src/generation/mod.rs	backends or on neither (@PLN128 arc D).
src/generation/mod.rs	because a `#c` def never carries `native` (arc A leaves it
src/generation/mod.rs	before calling into C — fail to compile under `--html` (@PLN24 arc E).
src/generation/mod.rs	called through a `u64` trampoline — that is arc C's whole
src/generation/mod.rs	caller bridges `String` → `Str` via `Deref` (the @P304 path). The
src/generation/mod.rs	cross-reference in phase 2 is a backward reference to an already-bound
src/generation/mod.rs	dispatch and the call panicked `invalid fn-ref`. Same shape as the @P299
src/generation/mod.rs	here, both bufferless: `no_work_buffer` (@P205 generic monomorphs, excluded
src/generation/mod.rs	indexes into an empty Vec and panics. @P275 fix.
src/generation/mod.rs	lib_plan-29 W1c (2026-05-29): the table is built from each
src/generation/mod.rs	library it came from (@PLN24 arc G), so listing the package's
src/generation/mod.rs	matched free no longer covered (a store leak — crawler's
src/generation/mod.rs	parameter for it (@PLN128 arc D).
src/generation/mod.rs	so this pointer is valid FOR THE CALL and no longer — the
src/generation/mod.rs	structured log call vanished on `--native`. Measured before the fix, with a
src/generation/mod.rs	the interp `fill_database` default-arm recursion, @P373).
src/generation/mod.rs	type minted only for a LOCAL var (@P296) is referenced by
src/generation/mod.rs	used to check that, so a type created one position early or late
src/generation/mod.rs	which arc D deliberately made indistinguishable from a declared
src/generation/mod.rs	whose own id falls AFTER the struct (the @P353 inversion: an
src/generation/mod.rs	would shadow the real implementation. Plan 09 phase 01
src/generation/ops/coroutine.rs	- 1 = unified `next_into(stores, &mut [i64; N])` channel (@P327 native)
src/generation/ops/coroutine.rs	8 → `i64` / 1 → `bool` / 12 → `DbRef` (@P326) /
src/generation/ops/coroutine.rs	@P328 native — unified channel, fn-ref rebuild. Native
src/generation/ops/coroutine.rs	@PLAN16 phase 02 — unified channel, layout-driven rebuild.
src/generation/ops/coroutine.rs	not occur post-@PLAN16) decodes as the legacy all-i64
src/generation/ops/coroutine.rs	size_of::<DbRef>() == 12 — Reference-yielding generator (@P326).
src/generation/ops/default.rs	! After phase 00 step 0.5, both `output_call_user_fn` and
src/generation/ops/int_compare.rs	! Plan 09 phase 10 step 10.3 — integer-comparison emitter family.
src/generation/ops/key_ops.rs	! Plan 09 phase 04 — key-keyed Op emitters.
src/generation/ops/key_ops.rs	! `OpClearKeyed` (clear a keyed-collection field, @P307) — are plain
src/generation/ops/key_ops.rs	! `OpSetKeyed` (`coll[key] = value` insert-or-replace, @P305) and
src/generation/ops/key_ops.rs	! phase 04 deletes the legacy arms.
src/generation/ops/key_ops.rs	`OpClearKeyed` emitter — @P307 clear a keyed-collection struct field
src/generation/ops/key_ops.rs	`OpSetKeyed` emitter — @P305 keyed insert-or-replace (`coll[key] = value`).
src/generation/ops/mod.rs	! After phase 00 (this scaffold + step 0.7b's let-bind-on-repeat already
src/generation/ops/mod.rs	! Smoke-test the `OpEmitter` trait shape so phase 00's claim that
src/generation/ops/mod.rs	! before phase 05 lands the first real one.
src/generation/ops/mod.rs	@P283 refvar->Stack rewrite + the sub-match internally). Registered for
src/generation/ops/mod.rs	@P283 — Op-name rewrite for `RefVar(Text)` destinations.
src/generation/ops/mod.rs	Plan 09 phase 00's forwarding-smoke emitters were retired by
src/generation/ops/mod.rs	during the @PLN6 dogfood: @P305 OpSetKeyed, @P307 OpClearKeyed,
src/generation/ops/mod.rs	phase 00's runtime smoke test, so they ARE registered).
src/generation/ops/mod.rs	phase 03's for-par emitter for the queue protocol: `n_parallel_queue`
src/generation/ops/mod.rs	plan-12 phase 02 — their job (proving the registry → emit_op →
src/generation/ops/mod.rs	priority over the special-case match arms (phase 00 step 0.6).
src/generation/ops/parallel.rs	! Plan 09 phase 03 — parallel-for emitter family.
src/generation/ops/parallel.rs	! The emitter takes over because of phase 00 step 0.6's registry-
src/generation/ops/parallel.rs	@PLAN59: hidden heap DESTINATION attrs (ref_return promotion /
src/generation/ops/parallel.rs	A literal / bufferless text worker (@P205 nwb) returns an owned `String`
src/generation/ops/parallel.rs	fns introduced in plan-09 phase 06 (P202 close).
src/generation/ops/text_ops.rs	! and depend on the @P283 refvar→`Stack` rewrite: the ORIGINAL op name
src/generation/ops/text_ops.rs	@P283 — mirror src/state/codegen.rs: a first arg that is a Var of type
src/generation/pre_eval.rs	@P274 guard: refuse the hoist when any intervening free-op
src/generation/pre_eval.rs	@P312 — a bare user-fn call STATEMENT is wrapped in `Span` (source-position
src/generation/pre_eval.rs	@P312 — also pre-eval any arg that READS a variable which another
src/generation/pre_eval.rs	@P312 — same ref-alias hoist as the user-fn Call arm below.
src/generation/pre_eval.rs	@P312 — the set of variables that some argument passes as a fresh
src/generation/pre_eval.rs	@P364: if `op` is a scope-exit free (`OpFreeRef` / `OpFreeText` /
src/generation/pre_eval.rs	@P364: the collapse HOISTS the Set's Call from `set_idx` (which
src/generation/pre_eval.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/generation/pre_eval.rs	The @P274 use-after-free guard MUST see every free flavour —
src/generation/pre_eval.rs	True if `v` reads any variable in `vars`, at any depth. Used by @P312 to
src/generation/pre_eval.rs	and avoid double-borrow. Plan-07 phase 4 added more
src/generation/pre_eval.rs	copy where `Return(Null)` is replaced by `Return(expr)` and `expr` is removed from
src/generation/pre_eval.rs	ret_pos shifted by -1 because we removed one element before it.
src/generation/pre_eval.rs	the @P274 path where `parse_append_text` (in vectors.rs) keeps
src/generation/text.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/git_query.rs	@PLN119 arc F — the git natives behind `lib/git`.
src/hash.rs	@PLN135 arc B — a probe asks only *is this the key*, about the SAME key every
src/hash.rs	A bucket slot holds a 1-based ARENA INDEX, not a record number (@PLN135 arc H).
src/hash.rs	The two are one step: a bucket table that is no longer the hash's table is
src/hash.rs	growth and [`reserve`] on a non-empty hash — used to do only the first half, so every
src/hash.rs	so filling it does not repeatedly rebuild the table (@PLN135 arc C).
src/hash.rs	that used to release an entry record.
src/host.rs	! void**, plus — @PLN119 arc B — a **struct or vector**, which does not fit in a
src/host.rs	(@PLN119 arc B).
src/host.rs	@PLN119 arc B — a struct / vector value, named by the record it lives in.
src/host.rs	can serve? Used to refuse a bad call at load rather than at first use.
src/install.rs	@PLN24 arc D — build the package's ANSI-C shim now, at install.
src/install.rs	chain (@PLAN12 Phase 6.6).
src/introspect.rs	! Plan-08 phase 01: introspection CLI.
src/ir_node.rs	`try_text_dest_pass`). Removed at M5 when that cluster iterates
src/ir_node.rs	not-yet-converted recursion (`self.generate(&Value)`). Removed at M5,
src/ir_read.rs	! @PLAN28 JSON re-encode comparison, and needing no JSON at all).
src/ir_read.rs	! @PLN11 arc C — store → native IR reader (the read path).
src/ir_read.rs	! [`Box::leak`], exactly as the @PLAN28 JSON decoder does (a loaded image holds
src/ir_read.rs	@PLN102 arc C step 1 — `#superseded "Y"` parses to the bare successor name
src/ir_read.rs	@PLN102 arc C step 2 — `source_is_owned` / `caller_source_is_owned`
src/ir_read.rs	@PLN102 arc C step 3 — the recommended-idiom STEER fires for a call to a
src/ir_read.rs	@PLN102 arc C step 4 — the fold lint: a `#superseded "Y"` symbol must
src/ir_read.rs	@PLN102 arc C — a stdlib fn (parsed under `self.default`, i.e. `STD_SOURCE`)
src/ir_read.rs	@PLN11 arc D micro-bench — wall-clock of producing the native stdlib
src/ir_read.rs	@PLN11 arc D probe — the full mmap loop end-to-end: materialize the
src/ir_read.rs	@PLN11 arc D step D1 — the public `Data::save` / `Data::open` API:
src/ir_read.rs	@PLN11 arc E — reject a missing / non-store / truncated / corrupt bundle
src/ir_read.rs	@PLN24 arc A — a `#c` binding survives the store round-trip.
src/ir_read.rs	[`crate::ir_store::save_data`] (@PLN11 arc D): mmap the file
src/ir_read.rs	as the @PLAN28 JSON decoder does.
src/ir_read.rs	can call a GENERIC stdlib fn. Before the fix the generic-instantiation block
src/ir_read.rs	codegen reads both on the load path; see plan README § arc C).
src/ir_read.rs	consumes the field yet, is the cheap moment: arc B and arc C would each
src/ir_read.rs	definitions. This is arc C's read-path milestone: a store-materialised
src/ir_read.rs	file; integrity-checked loading is arc E's drift-detection job (Q4).
src/ir_read.rs	generic `min_of` — the exact shape that used to fail.
src/ir_read.rs	half of the @PLAN28 schema-rebuild wall, resolved by caching the schema.
src/ir_read.rs	re-derived via [`Data::rebuild_indices`], mirroring the @PLAN28 JSON loader.
src/ir_schema.rs	! Interim native-IR ↔ JSON codec — @PLAN28 startup-cache bootstrap.
src/ir_schema.rs	! arc B's native→store materializer mirrors, and as arc B's **equivalence
src/ir_schema_gen.rs	! The store schema for the compiler IR (@PLN11 arc A): registers every
src/ir_store.rs	! (`plans/11-data-as-store` § arc B).
src/ir_store.rs	! @PLAN28 `ir_schema::data_to_json` walk with its JSON sink swapped for a
src/ir_store.rs	! @PLN11 arc B — native IR → store materializer (the write path).
src/ir_store.rs	23-field store subset — the same set the @PLAN28 codec serialises).
src/ir_store.rs	@PLN127 arc D — carried through the round trip, or a schema read back
src/ir_store.rs	Serialize `data` to a file-backed IR store at `path` (@PLN11 arc D).
src/ir_store.rs	The file is removed first so the root lands at the well-known first record.
src/ir_store.rs	Written **atomically** (@PLN11 arc E): materialized into a `<path>.<pid>.tmp`
src/ir_store.rs	`vector<DbType>` field at `off` of `parent` (@PLN11 arc D / D2a) so a
src/ir_store.rs	strongest arc B write-path validation short of a store→native reader).
src/json.rs	entry (RFC 8259 permits this). A BOM-prefixed document used to fail
src/json.rs	values > 2⁵³ used to round through `f64`). A number with a fraction or
src/keys.rs	(@PLN135 arc H). It used to be the constant 8, which is the same fact as a
src/keys.rs	**FLIPPED DEFAULT-ON (2026-07-11, @PLN102 the null-flow cutover):** this is now the DEFAULT.
src/keys.rs	@P355: `Str::str()` must NOT silently truncate a legitimately-large
src/keys.rs	@P355: the cheap pointer guards stay (a null or low/dangling ptr —
src/keys.rs	@PLN118 arc B refinement — depth of record deep-copies (`copy_record`/`finish_record`)
src/keys.rs	@PLN118 arc B — the free site keyed by `(slot, post-free generation)`. For a slot
src/keys.rs	@PLN25 F2 — the RANGE RECONCILIATION, FLIPPED DEFAULT-ON (2026-07-02): a plain
src/keys.rs	@PLN85 D-own-1 — THE FLIP (2026-07-02): the `deps`-driven ownership fixes
src/keys.rs	Java / Node hash-DoS, CVE-2011-4815 et al.). An attacker cannot
src/keys.rs	`LOFT_COPY_DUMP=1` (@PLN90 phase 1) — print one line per executed deep STRUCTURE copy
src/keys.rs	`LOFT_NO_BRIDGE_ORPHAN_FREE=1` — @PLN118 arc F opt-out / differential switch. The shared-store
src/keys.rs	`LOFT_NO_SLOT_REUSE=1` — @PLN118 arc D: never reclaim a freed store slot (always
src/keys.rs	`LOFT_NO_STEER=1` (@PLN102 arc C — the recommended-idiom steer channel) — DEFAULT ON, opt OUT.
src/keys.rs	arc B).
src/keys.rs	is a no-op, so the stdlib no longer LOADS gate-OFF — the escape hatch is retired.
src/keys.rs	is actually written in, and it used to fall through to the random default in
src/keys.rs	loft#827 — this used to be `std::hash::DefaultHasher`, whose algorithm std
src/keys.rs	populated) preserves the P253 hash-DoS defense (2026-05-11): without a
src/keys.rs	struct reference, struct-enum, and — plan-25 (@PLN25) — vector.
src/lexer.rs	@PLAN49 T1 breadcrumb — refresh every 256 iterations so a
src/lexer.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/lexer.rs	Create a link to the current lexer position, it can be used to revert to
src/lexer.rs	Expect code with spaces, line ends and remarks removed.
src/lexer.rs	whose name is within the shared edit-distance cap. Used to turn a mistyped
src/lib.rs	@PLAN12 phase 3.5a (2026-05-24) — re-export `extensions::native_call`
src/lib.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/lib.rs	`now()`/`ticks()` used to return a hardcoded 0: a frame timer read the
src/lib.rs	loft#851 — the page's FILESYSTEM. `--html` used to bind none at all: every
src/lib.rs	std stdout, so its `print` branch uses `print!` (mirrors the @P334 FS
src/lib_placement.rs	! # What arc A carries, and what it does not
src/lib_placement.rs	! a message naming arc B, and the function stays in-process; it never becomes a
src/lib_placement.rs	@PLN119 arc A — out-of-process libraries: placement as policy, the store as the wire.
src/lib_placement.rs	call arena travels as bytes on it (@PLN119 arc E).
src/lib_placement.rs	than being re-encoded into a second wire vocabulary (@PLN119 arc B).
src/lib_placement/arena.rs	@PLN119 arc B — the call arena: the store a compound value crosses in.
src/lib_placement/arena.rs	mapping (@PLN119 arc E).
src/lib_placement/dispatch.rs	! @PLN119 arc B — a **struct or vector**, plus a void return. A function
src/lib_placement/dispatch.rs	! it, and push nothing. Approximating it, which is why arc A refused text
src/lib_placement/dispatch.rs	@PLN119 arc A — routing a placed library's calls from the interpreter to its worker.
src/lib_placement/dispatch.rs	@PLN119 arc C — and the caller has to be going to FREE it.
src/lib_placement/dispatch.rs	The functions of the library at `pkg_dir` that arc A can route, marked with
src/lib_placement/dispatch.rs	arc A chose over approximating the protocol and returning a wrong value.
src/lib_placement/dispatch.rs	arc C makes the fault isolation structural rather than incidental.
src/lib_placement/wire.rs	! arc A does and does not carry across the boundary.
src/lib_placement/wire.rs	@PLN119 arc E — reach a library that is already running somewhere else.
src/lib_placement/wire.rs	@PLN119 arc E — serve one library's calls over a socket, so a consumer can
src/lib_placement/wire.rs	@PLN119 arc E — the socket crossing.
src/lib_placement/wire.rs	Not a [`crate::store::Store`]: arc A carries scalars, so it needs a frame
src/lib_placement/wire.rs	The call arena, one store per direction (@PLN119 arc B).
src/lib_placement/wire.rs	The store-numbering base the worker reported — arc B translates against
src/lib_placement/wire.rs	and not a second opinion derived here (@PLN119 arc C). A `View` return
src/lib_placement/wire.rs	arc B's layout gate). Answered once per function at install, never on a call.
src/lib_placement/wire.rs	at handshake anyway because the alternative — adding it when arc B needs
src/lib_placement/wire.rs	differs between `process` and `remote` (@PLN119 arc E).
src/lib_placement/wire.rs	generous; arc B sizes it from the argument graph instead.
src/lib_placement/wire.rs	it. Present here so attaching does not change when arc B lands.
src/lib_placement/wire.rs	the syscall we just removed for cache-line ping-pong.
src/lib_placement/wire.rs	— arc D extends this to a worker that dies outright.
src/live_reload.rs	! @PLN18 phase 02 — tier 0 of the execution tiers: **live per-function
src/log_config.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/log_config.rs	Plan-22 phase 02d-vii follow-up — central check for the
src/log_config.rs	tree for the named function. Used to verify "did the parser
src/logger.rs	Convert a count of days since the Unix epoch (1970-01-01) to `(year, month, day)`.
src/logger.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/logger.rs	Example: `"2026-03-13T14:05:32.417Z"`
src/logger.rs	Plan-07 phase 4 — log a typed runtime event (`RuntimeError`)
src/logger.rs	Shift epoch from 1970-01-01 to 0000-03-01
src/logger.rs	`main_loft_file` is used to determine the default log directory.
src/lsp.rs	A local tag (`@P`/`@PLAN`) with no references and no title is unknown.
src/lsp.rs	A single trailing lowercase (e.g. `@P259a`) when word-bounded.
src/lsp.rs	Tags the scanner deemed BROKEN (its `broken` array — `@P`/`@PLAN` that
src/lsp.rs	The `broken` array is `[{"tag":"@P999","refs":[…]}, …]`. <!--noindex-->
src/lsp.rs	Whether the scanner flagged `tag` as broken — a `@P`/`@PLAN` reference to
src/lsp.rs	removed.
src/lsp.rs	suffix (`@P259a`). Deep segment forms (`@PLAN22-2d`) resolve to their base.
src/main.rs	(@P382). That synthesis also tripped a CONST_STORE re-lock by
src/main.rs	2026-07-29 yields one per crate. See `dep_search_dirs`.
src/main.rs	@P282 fix: when `--no-warnings` is set, suppress
src/main.rs	@P282 — diagnostics (warnings + errors) go to STDERR,
src/main.rs	@P296-sibling (Windows-only): `canonicalize()` returns an
src/main.rs	@P321(c) Phase 3a: auto-discover *.png siblings of the entry .loft
src/main.rs	@P321(c) Phase 3a: raw base64 of *.png siblings of the entry .loft
src/main.rs	@P321(c) Phase 3b: decode base64 PNG assets to RGB bytes before
src/main.rs	@P337: a missing wasm-opt is NOT a cosmetic "larger output"
src/main.rs	@P350: self-validate the emitted wasm BEFORE writing the HTML so a
src/main.rs	@P363: join with a path separator (Path::join) instead of string
src/main.rs	@P379 — qualify native symbols for functions whose name
src/main.rs	@P379 — qualify native symbols for functions whose name collides
src/main.rs	@PLAN12 Phase 6.11 — `loft bundle export <outdir>` writes a
src/main.rs	@PLAN12 Phase 6.11 — `loft bundle import <indir>` installs a
src/main.rs	@PLAN12 Phase 6.11 — offline bundle export/import.
src/main.rs	@PLAN12 Phase 6.16 — `loft publish` author helper.
src/main.rs	@PLAN12 Phase 6.16 — author-side publish helper.
src/main.rs	@PLAN12 Phase 6.6 — `loft list-installed` enumerates packages
src/main.rs	@PLAN12 Phase 6.6 — `loft pin <script>` writes a sidecar
src/main.rs	@PLAN12 Phase 6.6 — enumerate ~/.loft/registry/<pkg>-<ver>/
src/main.rs	@PLAN12 Phase 6.6 — write a sidecar lockfile next to
src/main.rs	@PLAN12 Phase 6.7 — `loft audit` walks every installed package
src/main.rs	@PLAN12 Phase 6.7 — explicit deep scan: every cached
src/main.rs	@PLAN12 Phase 6.7a — `loft yank` author helper.
src/main.rs	@PLAN12 Phase 6.7a — author-side yank workflow.
src/main.rs	@PLAN12 Phase 6.8 — `loft update` driver.
src/main.rs	@PLAN12 Phase 6.8 — knob set for the `loft update` command.
src/main.rs	@PLAN12 Phase 6.8 — refresh lockfile entries to latest
src/main.rs	@PLAN12 — `loft new <name>` scaffolds a fresh library
src/main.rs	@PLAN12 — `loft new <name>` scaffolds a fresh loft library
src/main.rs	@PLAN28 debug/validation hook — when `LOFT_DUMP_SNAPSHOT=<path>` is set,
src/main.rs	@PLAN49 T1+T3 — arm the execution-timeout watchdog from the env
src/main.rs	@PLAN49 T3 — `--timeout <secs>` arms the watchdog. The
src/main.rs	@PLAN49 T3 — default the timeout ON under `loft test` / `--tests`.
src/main.rs	@PLN11 arc E / D2b / track 1 — the whole-program startup cache mmaps the
src/main.rs	@PLN11 arc E — a whole-program warm load already holds every definition;
src/main.rs	@PLN11 arc E — on a cold run with the program cache enabled, write the
src/main.rs	@PLN119 arc A — a library that declared `placement = "process"` runs in a
src/main.rs	@PLN119 arc A — a platform without the placement transport runs a placed
src/main.rs	@PLN119 arc A — mark each process-placed library's routable functions
src/main.rs	@PLN119 arc A — say goodbye to each placed library's worker rather than
src/main.rs	@PLN119 arc A — this process is the worker holding one process-placed
src/main.rs	@PLN119 arc E — this process SERVES one library over a socket, so a
src/main.rs	@PLN12 phase 01: introspection mode (flag or bare subcommand).
src/main.rs	@PLN12 phase 04 — interactive `loft>` prompt. Prompts + errors go
src/main.rs	@PLN12 phase 04 — no file and no subcommand: drop into the interactive
src/main.rs	@PLN12 phase 04 — resolve the stdlib dir and run the interactive REPL, then
src/main.rs	@PLN128 arc C — NOT gated on the backend any more. While this ran only
src/main.rs	@PLN140 arc B/C — arm the loft-level sampler before the program starts, so
src/main.rs	@PLN16 M5d phase 2 — `loft debug --rpc`: the NDJSON wire-protocol server on stdio.
src/main.rs	@PLN18 phase 02 — tier-0 live reload (opt-in): watch the program
src/main.rs	@PLN24 arc B — the interpreter calls `#c` bindings for real now; what
src/main.rs	@PLN24 arc D — and the C libraries `#c` bindings resolve against.
src/main.rs	@PLN24 arc E — wasm32-wasip2. It links a libc, so a `#c` binding
src/main.rs	@PLN26 phase 4 — Windows has no RPATH, so a C-ABI-linked native-package
src/main.rs	@PLN78 step 0 removed from the catalogue.
src/main.rs	@lib_plan-29 W2: apply each library's host.js-registered extension
src/main.rs	@lib_plan-29 W2: concatenate every used library's
src/main.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/main.rs	Days since 1970-01-01 (Unix epoch).
src/main.rs	Host target only: wasm has no C ABI to link (arc E).
src/main.rs	One path representation everywhere (@P296 / #460): on Windows,
src/main.rs	PKG.REG R4 (2026-05-24): the `loft install <name>` entry point in
src/main.rs	Plan-07 phase 1 step 1.20 / phase 3 — chain a Rust panic hook
src/main.rs	Plan-07 phase 2: --errors=compact|pretty CLI flag (overrides
src/main.rs	Plan-07 phase 2: --errors=compact|pretty selects the
src/main.rs	Plan-07 phase 4 — render typed runtime errors through the
src/main.rs	Plan-07 phase 4g.3 — demote dev-mode raises to
src/main.rs	Plan-08 phase 01: --introspect mode collects per-section
src/main.rs	Plan-08 phase 01: --introspect short-circuits everything.
src/main.rs	SAME `State`, so per-call store-lifetime leaks (the @P377
src/main.rs	Staged beside nothing else of ours, and removed on every exit path: a
src/main.rs	They no longer pass against their OWN source on this loft, so they judge nothing.
src/main.rs	calling `compile::byte_code` a second time (@P381, fixed by
src/main.rs	default port 8770 (distinct from the plan-35 viewer's 8765).
src/main.rs	lib_plan-29 W1c (2026-05-29): link each used library's wasm
src/main.rs	probe used to fire; doing it here means a warm cache hit
src/main.rs	rlib dep (@P229 G3, an unbuilt package).
src/main.rs	silently corrupts every native struct read/write. @P321c.
src/main.rs	takes one. `loft debug --lib dir prog.loft:12` used to take `--lib` as the
src/main.rs	that is the pressure that gets a floor ignored or a gate removed.
src/main.rs	to a sysroot symbol used to build and then trap at the call; the
src/manifest.rs	(@PLN102 arc B-semantic). The `contract` axis is a monotone integer
src/manifest.rs	@PLAN12 phase 3.5b (2026-05-24) — path-deps in `loft.toml`
src/manifest.rs	@PLN102 arc B — the composition matrix (plan README § Composition
src/manifest.rs	@PLN102 arc B-semantic — the `contract` axis (a monotone integer,
src/manifest.rs	@PLN119 arc A — `[library] placement = "inproc" | "process"`: where this
src/manifest.rs	@PLN24 arc D — `[c] libs = "liblc_types.so, libpq.so.5"`: the C shared
src/manifest.rs	@PLN24 arc D — `[c] shim = "src/shim.c"`: ANSI-C sources this package
src/manifest.rs	@PLN24 arc G — `[c] optional-libs = "libduckdb.so, libpq.so.5"`: C
src/manifest.rs	Positive control: the one cell that fired even before arc B.
src/manifest.rs	category-S failure @PLN102 arc B removes. The payload is the reason.
src/manifest.rs	lib_plan-29 W1c: bridge crate from `[wasm.bridge] crate = "..."`.
src/manifest.rs	lib_plan-29 W1c: loft `#native` symbol → bridge `pub fn` name from
src/manifest.rs	lib_plan-29 W2: per-library JS host-imports file from
src/manifest.rs	reject the two kinds of failure with different diagnostics (@PLN102 arc B).
src/manifest.rs	── @PLN102 arc B-semantic — the compatibility `contract` axis ───────────────
src/native.rs	**JObject walk (2026-04-14):** for each JsonField, copies the
src/native.rs	@P290 — clear the soft free-protection set by `n_protect_store_frees`.
src/native.rs	@P290 — soft "don't free" marker for the fn-call deep-copy bracket.
src/native.rs	@PLAN38 phase 01b — interpreter handler for `store_durable_check`.
src/native.rs	@PLAN38 phase 01b — interpreter handler for `store_durable_seal`.
src/native.rs	@PLAN38 — interpreter handler for `store_persist_bind`. Pops a
src/native.rs	@PLAN59: classify worker attrs by TYPE, not name prefix. The
src/native.rs	@PLN119 arc F — the single native behind `lib/git`. Native targets only:
src/native.rs	@PLN127 arc B: build a `TypeInfo` for one type id.
src/native.rs	@PLN127 arc C: `type_named(name)` — reflection with no value in hand.
src/native.rs	@PLN18 phase 05a — the state-sync UDP channel. (No cookie native: the
src/native.rs	@PLN18 phase 07 — the BROWSER kernel: the connector role's natives over
src/native.rs	@PLN18 phase 07 — the browser kernel (see KERNEL_FUNCTIONS_WASM).
src/native.rs	@PLN24 arc G — `c_library_available(soname)`.
src/native.rs	@PLN24 arc G — the optional-C-library availability query.
src/native.rs	@PLN97 arc G Phase 0. Available on native (`registry`) AND the browser
src/native.rs	@PLN97 arc G Phase 1.
src/native.rs	@PLN97 arc G Phase 3a.
src/native.rs	Args pop in reverse: key, path, local. @PLN97 arc G Phase 3b.6.
src/native.rs	Args pop in reverse: sha256, url, local. @PLN97 arc G Phase 0.
src/native.rs	B7 (2026-04-13): when called with method syntax (`v.len()`),
src/native.rs	Bonus: per-call dests retire the @P354 sibling-aliasing scratch hazard.
src/native.rs	Empty object: same @P357 fix as the empty-array arm — zero the
src/native.rs	HTTP client glue removed — n_http_do and n_http_body are now auto-marshalled.
src/native.rs	Interpreter handler for `store_bind_lazy` — @PLN129 arc A. Bind a COLLECTION
src/native.rs	Interpreter handler for `store_lazy_clear` — @PLN129 arc C. Acknowledge the
src/native.rs	Interpreter handler for `store_lazy_error` — @PLN129 arc C. Why the last
src/native.rs	Interpreter handler for `store_lazy_faults` — @PLN129 arc C. How many
src/native.rs	Interpreter handler for `store_lazy_query` — @PLN129 arc B2. Run an explicit
src/native.rs	Interpreter handler for `store_lazy_range` — @PLN129 arc B step 8. Pull a
src/native.rs	Plan-06 ARC.md A4 (closed 2026-05-07) — companion to
src/native.rs	Plan-06 ARC.md A4 (closed 2026-05-07) — the legacy materialised
src/native.rs	Plan-06 phase 4d.A — typed worker-input dispatch. Replaces the
src/native.rs	Plan-06 phase 4d.A.2: also returns `Some(vec![first.typedef])`
src/native.rs	Plan-07 phase 4 — route through Logger::log_runtime_kind
src/native.rs	Plan-07 phase 4 — same routing as n_assert; ensures the
src/native.rs	Plan-07 phase 4 — typed runtime error. Replaces the legacy
src/native.rs	Plan-07 phase 4 — typed runtime error. Same shape as n_assert
src/native.rs	Plan-12 phase 1a (2026-05-23) — crypto `n_*` impls moved to
src/native.rs	Plan-12 phase 1a (2026-05-23) — crypto `n_*` symbols
src/native.rs	Return milliseconds since the Unix epoch (1970-01-01T00:00:00 UTC).
src/native.rs	Step 4 fourth slice (2026-04-14) — recurse into nested
src/native.rs	Step 4 second + fourth slices (2026-04-14): non-empty
src/native.rs	Step 4 third + fourth slices (2026-04-14): non-empty
src/native.rs	Used to freeze `ticks()` and `now()` for deterministic snapshot tests —
src/native.rs	`Store::get_single` in `src/store.rs:1406-1421`: those write /
src/native.rs	fused for-par over text-returning workers no longer allocates a
src/native.rs	host import (same synchronous loft API as native). The verified one used to be
src/native.rs	import bridge. It used to return a hardcoded 0, which is the whole second
src/native.rs	nothing but `sha2` and now sits in `crate::integrity`, so the pair no longer
src/native.rs	pop in reverse: hi, lo, path, local. @PLN97 arc G Phase 4.
src/native.rs	pop in reverse: key, path, local. @PLN97 arc G Phase 3a.
src/native.rs	pop in reverse: path, local. @PLN97 arc G Phase 2.
src/native.rs	status / summary / progress lines. Driver: @PLAN37 phase 07's
src/native.rs	the items-vector handle. @P357 — `jv_alloc` claims a RECYCLED
src/native.rs	── Crypto built-ins moved to lib/crypto/native (plan-12 phase 1a) ──────
src/native.rs	── WebSocket + TCP + OpenGL + random glue removed ─────────────────────
src/native_gate.rs	@P387: a text-returning fn's work-buffer is now `hidden` too (so it rides
src/native_gate.rs	@PLAN59: every heap-kind hidden destination (Reference / Vector /
src/native_gate.rs	popping its stack slots can no longer disagree.
src/native_lib.rs	@PLN118 arc F — after writing the return, free every bridge-allocated
src/native_lib.rs	@PLN118 arc F — hidden dests the bridge allocated as a FALLBACK (caller
src/native_lib.rs	@PLN26 phase 2 — a cdylib that calls a `[native] crate` package emits
src/native_lib.rs	@PLN26 phase 2 — a shared-store library cdylib that USES a `[native] crate`
src/native_lib.rs	@PLN26 phase 2 — the rustc link flags for ONE native package's resolved cdylib
src/native_lib.rs	`-L dependency=` / per-crate pinning is needed and two packages no longer
src/native_lib.rs	`dlopen` would fail. Harmless while nothing removed artifacts; `prune_artifacts`
src/native_lib.rs	appears whenever a consumer's type table differs — and nothing ever removed the
src/native_lib.rs	caller's (@PLAN59: a lib-side constant id produced `claim(size=0)` /
src/native_lib.rs	layout, which is the case that used to corrupt, and false when it cannot be
src/native_lib.rs	stable digest). Used to detect "did this library's source change since the last
src/native_lib.rs	sweep used to take every `.so` in the directory, and `native-auto/` is not
src/native_lib.rs	write the tmpfs can no longer back faults with a Bus error) reads like a
src/native_utils.rs	([`missing_host_imports`]), and the stub emitter — and they used to carry a copy
src/native_utils.rs	* **per-unit** (cargo 1.99.0-nightly 2026-07-29 and later) — each crate gets its own
src/native_utils.rs	@P229 (G2): harvest build-script `rustc-link-search` dirs from
src/native_utils.rs	@P341: also fold each native PACKAGE rlib's path + content, so rebuilding
src/native_utils.rs	@P350: parse the import section of a `loft --html` wasm and verify every
src/native_utils.rs	@P359: on a clean checkout the package-under-test's rlib may not
src/native_utils.rs	@P360: this is a dev convenience — rebuild loft's OWN runtime rlib when
src/native_utils.rs	@PLAN12 Phase 6b — native target via the shared helper:
src/native_utils.rs	@PLN24 arc D — link the C libraries the program declared with `[c] libs`, so
src/native_utils.rs	@PLN24 arc G — an OPTIONAL library gets no flag at all. On the link
src/native_utils.rs	@PLN26 phase 0.1 — two RPATH entries: the build/prebuilt dir
src/native_utils.rs	@PLN26 phase 0.4 — resolve the `.so` exactly as the interpreter does
src/native_utils.rs	@PLN26 phase 0.4b — the cdylib is named after the `[library] native`
src/native_utils.rs	@PLN26 phase 3 — a wasm cross-build keeps the package's PROC-MACRO deps
src/native_utils.rs	@PLN26 phase 3 — a wasm target needs the package's rlib (wasm links statically;
src/native_utils.rs	@PLN26 phase 4 — Windows links a DLL through its IMPORT
src/native_utils.rs	@PLN26 phase 4 — stage every native-package DLL beside a just-built Windows
src/native_utils.rs	@PLN26 phase 4 — the C-ABI path (import-library linking + DLL staged beside
src/native_utils.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/native_utils.rs	P244-windows fix #2 (2026-05-12): use single-segment joins,
src/native_utils.rs	build's content fingerprint, so `--html` / `--native-wasm` no longer need a
src/native_utils.rs	cargo 1.99.0-nightly (2026-07-29) gives each crate its own
src/native_utils.rs	cargo adopted on 2026-07-29 that directory does not exist, `read_dir` returned
src/native_utils.rs	dir one level deeper; before the fix that resolved the project root to the
src/native_utils.rs	test — a symptom three steps removed from the cause.
src/ops.rs	@PLAN12 phase 3.5a (2026-05-24) — `RNG` thread-local + the
src/ops.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/ops.rs	NOT a silent null" contract (these tests formerly asserted `should_panic`).
src/ops.rs	Plan-07 phase 4e.3 — `format_long` with an optional fault-tag
src/ops.rs	Text inequality — see [`op_eq_text`]. @P347 / #622.
src/ops.rs	Text ordering compare `<=` — see [`op_lt_text`]. @P347.
src/ops.rs	`no_i64_sentinel_in_int_functions` removed in C54 Phase 2c. Int
src/ops.rs	provenances. @P347.
src/ops.rs	removed. random's drain to lib/random/native/ makes the cdylib
src/ops.rs	return the sentinel — debug and release then AGREE (release used to wrap by coincidence).
src/ownership_cfg.rs	@P302/@PLN25 materialisation: a var RE-MINTED via `OpDatabase` on ANY path is a materialised
src/ownership_cfg.rs	A self-borrow `Borrowed(v)` for var `v` is NOT a real borrow — it is the @P302
src/ownership_cfg.rs	enclosing loop/function — i.e. not one consumed by a nested `Loop`? Used to decide
src/ownership_cfg.rs	self-dep work-ref `[v]` (@P302 ownership marker, not a borrow); a `??` null-coalesce temp
src/ownership_cfg.rs	the shipped `get_free_vars` @P302 carve-out; you cannot borrow from yourself).
src/package.rs	The two used to answer "what does a package consist of" separately, and drifted
src/paged_reader.rs	! @PLN97 arc G Phase 2 — a read-only, transient PAGED view over a store image.
src/paged_reader.rs	64 pages; loft#785 removed the cap and n became the working set.
src/paged_reader.rs	This is the sibling of the allocation that `read_into` removed, and it is
src/paged_reader.rs	a table, and the hot path used to do TWO for a single four-byte read: `ensure_page`
src/paged_reader.rs	change removed real round trips. The BROWSER pays it undisguised: it is
src/paged_reader.rs	consecutive 64 KiB pages, and it used to ask for them one at a time.
src/paged_reader.rs	of the exact amplification loft#785 removed — to save 1 MB of a 17 MB heap.
src/paged_reader.rs	pages; loft#785 removed the cap, so n became the whole working set and the
src/paged_reader.rs	the allocation `read_into` removed. Every one of them hashes a `u64` key and probes
src/paged_reader.rs	⚠ **THE READ PATH USED TO HASH THE SAME KEY TWICE.** `ensure_page` asked
src/parallel.rs	**Currently dead code** — phase 3b lands the dispatcher that
src/parallel.rs	@PLAN59 witness-free (mirrors the plain-call
src/parallel.rs	@PLN117 arc C — instrument the struct/ref/vector return path too, so the
src/parallel.rs	ARC.md A4 (closed 2026-05-07) — Stitch::Concat removed.
src/parallel.rs	Currently unused at runtime — phase 2b's narrow path
src/parallel.rs	Currently unused at runtime — phase 2b's reference-path switch is
src/parallel.rs	Plan-06 phase 1 G2 — read a 1/4/8-byte primitive from a row
src/parallel.rs	Plan-06 phase 1 G3 — read the `&str` slice that the input row's
src/parallel.rs	Plan-06 phase 1.5 — use rayon's global work-stealing pool
src/parallel.rs	Plan-06 phase 1b: workers write text results into a per-worker
src/parallel.rs	Plan-06 phase 2 — type-driven recursive walk that translates every
src/parallel.rs	Plan-06 phase 3a (DESIGN.md D1a) — selects the per-call stitch
src/parallel.rs	Plan-06 phase 3b.1 — merge per-thread batches into one ordered vector.
src/parallel.rs	Plan-06 phase 3c: production paths now use `run_parallel_direct`
src/parallel.rs	Plan-06 phase 4b': the loft-surface `parallel_for_int` (string-based
src/parallel.rs	Plan-06 phase 4d.A — wide-inline element reader for tuple,
src/parallel.rs	See plan-06 DESIGN.md D2.1 (slot-marker design) and DESIGN.md D11c
src/parallel.rs	The unified runtime entry point that phase 3b lands will be
src/parallel.rs	Used by phase 7's fused for-par when the body references the worker
src/parallel.rs	`par_for_each`. Lands in plan-06 phase 7.
src/parallel.rs	analysis). Runtime lands in phase 3e; surface in phase 7e.
src/parallel.rs	effects. Runtime lands in phase 7b.
src/parallel.rs	must be par-safe (D8 rule, enforced by phase 5b').
src/parallel.rs	no production caller. The variants exist so that phase 3b/3d/3e have
src/parallel.rs	par-safety + side-effect-correctness via phase 5's analyser before
src/parallel.rs	previously inlined the same 5-line loop.
src/parallel.rs	result, and by phase 10's value-position lowering (`let r =
src/parallel.rs	return type; phase 3 collapses those into one polymorphic dispatcher
src/parallel.rs	sequential fallback (@PLN117 arc D). Same bounds as `ThreadPool::install`,
src/parallel.rs	── Plan-06 phase 1 step 4.5 — shared run_parallel_* template ─────────────────
src/parallel.rs	── Plan-06 phase 2 — store-rebase infrastructure ─────────────────────────────
src/parallel.rs	── Plan-06 phase 3a — Stitch policy enum (DESIGN.md D1) ─────────────────────
src/parser/builtins.rs	ARC.md A4 (closed 2026-05-07) — `check_light_eligible` and its
src/parser/builtins.rs	ARC.md A4 (closed 2026-05-07) — `n_parallel_for_light` was
src/parser/builtins.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/builtins.rs	`collect_callees`) were removed when the light Concat path
src/parser/builtins.rs	offset-ref coercion sub-references `payload` (NOT S's first field, which is no longer
src/parser/builtins.rs	store: it used to read empty (silent-wrong) or, mis-ordered, slot-panic at
src/parser/collections.rs	(parser/collections.rs:1492-1499) converts to false
src/parser/collections.rs	@P305 — `coll[key] = value` insert-or-replace for a KEYED
src/parser/collections.rs	@P308 — a KEYED-collection FIELD whole-assignment `s.h = expr`
src/parser/collections.rs	@P311: do NOT set the 0x8000 "free source" bit here. Unlike the
src/parser/collections.rs	@P327 / @P328 native — yield-channel dispatch via packed
src/parser/collections.rs	@P327 — for-yield types without a single-value null sentinel
src/parser/collections.rs	@P343: a `vector<fn(...)>` loop terminates by testing the
src/parser/collections.rs	@P343: vector-of-fn-ref element read. Mirror the
src/parser/collections.rs	@P345: a loop variable's type is fully determined by the
src/parser/collections.rs	@P376 — `Type::Never` is the poison an errored struct
src/parser/collections.rs	@PLAN16 phase 02 — a tuple whose every element classifies into a
src/parser/collections.rs	@PLAN16 phase 05 — closures (`Type::Function`) join tuples on
src/parser/collections.rs	@PLN135 arc C — a `hash` reserves its BUCKET TABLE, which is a different
src/parser/collections.rs	ARC.md A4 (closed 2026-05-07) — `n_parallel_for_light` was
src/parser/collections.rs	B5 (2026-04-13): for a mixed struct-enum element type
src/parser/collections.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/collections.rs	Phase 09 phase 00 step 0.7 — RawExpr is a codegen-internal
src/parser/collections.rs	Plan-06 phase 1 G2.1 — narrow-integer vector inputs
src/parser/collections.rs	Plan-06 phase 1 G4 — accept Type::Function returns
src/parser/collections.rs	Plan-06 phase 1 G4 — fn-ref returns (Type::Function, 20 bytes)
src/parser/collections.rs	Plan-06 phase 1 G6: vector<T> returns also route here —
src/parser/collections.rs	Plan-06 phase 4d.A.2 — fn-ref vector storage is
src/parser/collections.rs	Plan-06 phase 4d.B — materialise a keyed-collection input
src/parser/collections.rs	Plan-06 phase 4d.B — par-over-keyed-collection
src/parser/collections.rs	Plan-06 phase 5b' — par-safety DEEP check at ERROR level.
src/parser/collections.rs	Plan-07 phase 1 — Span is transparent; recurse into the
src/parser/collections.rs	Plan-07 phase 4 step 4.6 — for-loop iteration uses
src/parser/collections.rs	Plan-07 phase 4 step 4.6 — fused-for-par iteration site →
src/parser/collections.rs	Plan-07 phase 4 step 4.6 — par-worker iteration over the
src/parser/collections.rs	Plan-07 phase 4 step 4.8 — for-loop iteration over text uses
src/parser/collections.rs	Plan-07 phase 6 (partial) — name the offending type
src/parser/collections.rs	The nested case used to read `element_stack_size(inner).max(4)` — the
src/parser/collections.rs	`b` is no longer a runtime variable — each reference expands
src/parser/collections.rs	`iter_next` is consumed by `for_next` — @P327 needs it for the
src/parser/collections.rs	copy_claims is unimplemented (per @P295), so `keyed_field_kt`
src/parser/collections.rs	corruption — matching the pre-@P305 behaviour for that case).
src/parser/collections.rs	gate no longer needs to filter by input kind — every
src/parser/collections.rs	is a 4-byte d_nr (@P343), a NESTED vector is a 4-byte handle, a plain
src/parser/collections.rs	is a total substitution: after it, `from` no longer appears anywhere.
src/parser/collections.rs	is no longer a valid (callable, > 0) function reference.
src/parser/collections.rs	parse_for:1289-1346. The variables persist through the
src/parser/collections.rs	phase 1 G1: struct-enum returns (Enum variants with
src/parser/collections.rs	positive. Used to give a clear diagnostic instead of the dangling-ref panic.
src/parser/collections.rs	subscript (@PLN125 arc C) and an operator all say so — and formatting
src/parser/collections.rs	the OOB null sentinel) no longer ends the loop early. The length is
src/parser/collections.rs	user's loop-variable name can no longer collide on a single
src/parser/collections.rs	work_checkpoint + clean_work_refs removed — see comment at the
src/parser/control.rs	(6) @PLAN51 Cluster II — extend the substitution backwards to
src/parser/control.rs	2026-07-03: 0 firings suite-wide (a non-site-value work ref only ever
src/parser/control.rs	@P314 — narrow-aware element type (see `append_elem_tp`).
src/parser/control.rs	@P327 — same encoding as the for-loop's `iterator()` path
src/parser/control.rs	@P330: a tuple local must NOT hoist to a parameter — call sites
src/parser/control.rs	@P365: a `return [ … ]` vector literal needs the function's return
src/parser/control.rs	@P374: mirror block_result's tuple→synthetic-struct rewrite for an
src/parser/control.rs	@P377 / S1 — parse-time NRVO for the intermediate-local return shape.
src/parser/control.rs	@P377 / S1: collapse `cv = inner_call(...); cv` so the inner
src/parser/control.rs	@P387 zero-cost: hidden like the text_return buffer above, so the
src/parser/control.rs	@P387 zero-cost: mark the work-buffer HIDDEN so it rides the
src/parser/control.rs	@PLAN16 phase 02 — same packed encoding as the
src/parser/control.rs	@PLAN51 probe 39 — Reference parallel of the Vector
src/parser/control.rs	@PLAN59/H1 NRVO — rename the signature-time `__retbuf` ATTR to this
src/parser/control.rs	@PLN127 arc B — `type_of(x)`: the declared shape of x's TYPE.
src/parser/control.rs	A tuple LOCAL (@P330) — no hoist; the B5-L3 temp covers the copy.
src/parser/control.rs	B2-runtime / B3 / B7 unification (2026-04-13): struct-enums
src/parser/control.rs	B5 remaining half (2026-04-14): a HEAP match-arm
src/parser/control.rs	Bind-site analogue of `do_if_acc`'s tail promotion (the @P323 sibling, 2026-07-10).
src/parser/control.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/control.rs	P206 + plan-18: emit the missing-arrow diagnostic, then
src/parser/control.rs	Plan-07 phase 1, step 1.13 — wrap intrinsic-keyword calls
src/parser/control.rs	Plan-07 phase 1, step 1.13 — wrap user-typed Call / CallRef
src/parser/control.rs	Plan-07 phase 4d.2 — defensive-check flow-analysis. When
src/parser/control.rs	Plan-07 phase 4d.2 — walks `ops` looking for adjacent
src/parser/control.rs	Plan-07 phase 4f.5 — float / single div / mod peers.
src/parser/control.rs	Plan-07 phase 6 — a pattern whose type can never match the subject is a
src/parser/control.rs	Plan-14 phase 07 (P234 runtime): rewrite a body-tail
src/parser/control.rs	Plan-14 phase 07 (P234 runtime): when the function's expected
src/parser/control.rs	Plan-22 phase 02d-iii.e + @P319 — skip the
src/parser/control.rs	SkipTupleLocal (@P330): the dep drops on purpose — the
src/parser/control.rs	Soundness floor for `parallel {}` (plan-57 Bug 2). An arm runs in an
src/parser/control.rs	`__ret_N` temp before the local frees (the @P329
src/parser/control.rs	`block_result` used to inline (OWNERSHIP_MODEL.md: ownership read once, not
src/parser/control.rs	`next()` no longer diverges (it dropped float/single/enum →
src/parser/control.rs	and spins (PROBLEMS.md plan-18 phase 01 finding).
src/parser/control.rs	attribute — i.e. `do_tret_bind` promoted it on a PRIOR pass. Used to gate
src/parser/control.rs	buffer) — giving them one (the reverted @P387 option A) broke par
src/parser/control.rs	directly) no longer owns a store, so suppress its eager buffer
src/parser/control.rs	fall through to the raising peer + log path; phase 4e's
src/parser/control.rs	hide the host inside a read-projection chain. (See the plan-57 capture
src/parser/control.rs	injects the buffer. Zero-cost @P387: the fn-ref dispatch no longer
src/parser/control.rs	is @P387-adaptive, so the wiring must keep it uniform.
src/parser/control.rs	lambda (@P319).
src/parser/control.rs	normal block-scope sweep (it is no longer the escaping return var). A
src/parser/control.rs	once needed is deleted (@PLAN59 phase 2).
src/parser/control.rs	remaining owner — that's the @P377 leak.
src/parser/control.rs	return the base var being projected. Used to decide whether the
src/parser/control.rs	scope analysis (the @P377 `render_struct(p) -> Canvas` shape). Marking
src/parser/control.rs	server/client library) — see the plan-57 deferred-follow-ups.
src/parser/control.rs	that survives frame teardown, @P293), NOT a hidden `&text` buffer — so
src/parser/control.rs	the explicit-`return []` type-threading (@P365). The fresh-vector-in-arm double
src/parser/control.rs	three inline branches the vector arm of `block_result` used to carry.
src/parser/control.rs	through); a block diverges when any operator in it does. Used to tell a tail that
src/parser/control.rs	used to report `not null` violations at the tuple literal site.
src/parser/control.rs	vantage during match-pattern debugging (P206, plan-18).
src/parser/control.rs	wrote into `w`'s buffer — the @P377 no-op shape every consumer
src/parser/definitions.rs	8-byte storage. Type::Long is no longer produced.
src/parser/definitions.rs	@P296-sibling — for a qualified `lib::Type` reference, `tp_nr`
src/parser/definitions.rs	@PLAN12 — bare `#native` defaults the symbol to the
src/parser/definitions.rs	@PLAN12 — if the explicit string EQUALS the canonical
src/parser/definitions.rs	@PLAN59 / H1 phase 1 — the unconditional heap-return buffer:
src/parser/definitions.rs	@PLN102 arc C — `#superseded "Y"` marks this callable as
src/parser/definitions.rs	@PLN125 arc A step A2b — `Self.X`, an interface's ASSOCIATED TYPE used in one
src/parser/definitions.rs	@PLN125 arc A step A2b — resolve a `Self.X` associated-type reference.
src/parser/definitions.rs	@PLN125 arc A — an ASSOCIATED TYPE declaration: the interface names a
src/parser/definitions.rs	@PLN125 arc B — check a just-parsed `OpDrop`, the hook a type runs at scope end.
src/parser/definitions.rs	@PLN125 arc C — subscripting is spelled `op [] (self: Self, i: τ)`.
src/parser/definitions.rs	@PLN24 arc A — `#c "<symbol>" "<c-signature>"` binds this
src/parser/definitions.rs	@PLN24 arc A — `#c "<symbol>" "<c-signature>"`.
src/parser/definitions.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/definitions.rs	I5 (phase 1): factory methods (Self in return without self: Self first param)
src/parser/definitions.rs	P196 follow-up (2026-05-12): exclude tuples that contain a
src/parser/definitions.rs	Phase 2c round 10c: `long` has been removed as a user-facing
src/parser/definitions.rs	Plan-06 phase 4d.A — accept tuple as the inner type of
src/parser/definitions.rs	Plan-06 phase 4d.A.2 — recognise `fn` as a type-keyword inside
src/parser/definitions.rs	Plan-06 phase 4d: register the synthetic `__tuple<…>`
src/parser/definitions.rs	Plan-06 phase 4d: tuple-typed struct fields are now
src/parser/definitions.rs	Plan-06 phase 5a (DESIGN.md D8.1):
src/parser/definitions.rs	Plan-06 phase 5a (DESIGN.md D8.1): `#pure`
src/parser/definitions.rs	Plan-07 phase 4e.2 — undefended fault-site warning.
src/parser/definitions.rs	Plan-14 phase 07 (P234 runtime): when the declared return type
src/parser/definitions.rs	Plan-17 phase 01 (B) — bound resolution + t-stub creation now
src/parser/definitions.rs	Plan-22 phase 02d-iii.e — ACTIVATE the type
src/parser/definitions.rs	The @PLAN59 twin of the I9-text arm: a concrete method
src/parser/definitions.rs	`capture_context`, so phase 02c's auto-Reference
src/parser/definitions.rs	de-globalize lands (step 4), when def_nr no longer keys variants.
src/parser/definitions.rs	explicit `par_to_vec(...)` (planned phase 11).
src/parser/definitions.rs	plan-15 matrix CLOSED `vector<capturing fn>`; a struct
src/parser/definitions.rs	single-payload-refactor.md § "DESIGN DECISION (2026-06-20)".)
src/parser/expressions.rs	(objects.rs:1306-1361).
src/parser/expressions.rs	- @P292: when rhs later goes out of scope its storage is freed → v
src/parser/expressions.rs	- @P394: when this is v's FIRST assignment (its own store not yet
src/parser/expressions.rs	@P277 — `local_keyed += [literal]`. Without this branch, the
src/parser/expressions.rs	@P287 — same materialisation, but the LHS is a struct field
src/parser/expressions.rs	@P292 / @P394 — `local_v = other_var_v` where the RHS is a bare vector
src/parser/expressions.rs	@P295 — `local_s = keyed_expr` where the LHS is a KEYED-collection
src/parser/expressions.rs	@P300 — prepend `Set(v, Null)` so the destination keeps a
src/parser/expressions.rs	@P314 — narrow-aware element type (see `append_elem_tp`).
src/parser/expressions.rs	@P328 — when yielding a NON-CAPTURING closure into an
src/parser/expressions.rs	@P376 follow-up — formerly emitted `n_set_store_lock(p, true)`
src/parser/expressions.rs	@P376 — POISON an errored whole-RHS that resolves to `Unknown` (pass 2)
src/parser/expressions.rs	@P390 — `v = v[a..b]` self-slice-assign. The plain materialise emits
src/parser/expressions.rs	@P390 — does `val` reference `Value::Var(target)` anywhere (a READ of the
src/parser/expressions.rs	@PLAN48 P2: `x: i32 = some_integer` narrows (loses data) but integer and
src/parser/expressions.rs	@PLAN51 Cluster IV: also null-init caller-side hidden-
src/parser/expressions.rs	@PLAN52 cluster IV (Vec/Hash/Sorted/Index `??`): the original
src/parser/expressions.rs	@PLAN52 cluster IV-Vec-nested-field-push (2026-05-30): Vector
src/parser/expressions.rs	@PLAN52 cluster IV-Vec-nested-field-push (2026-05-30): strict
src/parser/expressions.rs	@PLAN52 cluster IV-Vec-nested-field-push: STRICT rule — concat is
src/parser/expressions.rs	@PLN135 arc A — `local_keyed += Struct { … }` (no brackets) builds the entry
src/parser/expressions.rs	@PLN24 arc D — a `#c` binding returning text is a value-position
src/parser/expressions.rs	@PLN25 — GATE FLIP LANDED (2026-06-20): nullable-by-default is ON for all user files +
src/parser/expressions.rs	Context: phase 02d-iii.b's auto-deref wraps every read of
src/parser/expressions.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/expressions.rs	LHS REMOVED from this branch. Vector `+= elem` is ambiguous
src/parser/expressions.rs	P261 (2026-05-13): the third case — `s.v = [literal items]` —
src/parser/expressions.rs	Phase 09 phase 00 step 0.7 — codegen-internal.
src/parser/expressions.rs	Plan-07 phase 1 — Span is transparent; recurse into the
src/parser/expressions.rs	Plan-07 phase 1: compare via unspan() so the same
src/parser/expressions.rs	Plan-07 phase 1: unspan() so the wrap on `.` (step 1.12) does
src/parser/expressions.rs	Plan-07 phase 1: unspan() so wraps on `.` (step 1.12) don't
src/parser/expressions.rs	Plan-22 phase 02a (2026-05-12): also save body in pass 1
src/parser/expressions.rs	Plan-22 phase 02d-iii.c — build the rewrite IR for an
src/parser/expressions.rs	Plan-22 phase 02d-iii.c — preserve the boxed scalar's
src/parser/expressions.rs	Plan-22 phase 02d-iii.d — prepend cell allocation when a
src/parser/expressions.rs	Plan-22 phase 02d-iii.e — wrap a first-set boxed-
src/parser/expressions.rs	Plan-22 phase 02d-v — extract the boxed-scalar `v_nr`
src/parser/expressions.rs	Plan-22 phase 02d-vi — bypass the text-special branch
src/parser/expressions.rs	The deep-copy gives `s` its OWN store, so it no longer borrows
src/parser/expressions.rs	Used to find which block statement first assigns an inline-ref temporary.
src/parser/expressions.rs	`addr_mut`. See PROBLEMS.md @P376 follow-up + PLANNING.md S22
src/parser/expressions.rs	`s.v = fresh` (RHS is a vector variable/expr) used to silently drop
src/parser/expressions.rs	alias would dangle when a loop-local RHS is freed (the @P292 bug,
src/parser/expressions.rs	consuming it? Used to decide, BEFORE the right-hand side is parsed, that it is a
src/parser/expressions.rs	dangling-on-scope-exit @P292 hazard, and the no-own-store @P394
src/parser/expressions.rs	field-read used to take the whole pass-2-only branch below, so on
src/parser/expressions.rs	into it (the clear+append delivery the @P390 / @P287 paths use),
src/parser/expressions.rs	lookup at lines 1783-1822. The struct-field twin at lines
src/parser/expressions.rs	match. (@P287)
src/parser/expressions.rs	proven @P287 struct-field pattern), then `OpClearVector + OpAppendVector`
src/parser/expressions.rs	record rather than into a throwaway one (@PLN135 arc A).
src/parser/expressions.rs	release builds too (S22 — previously guarded by #[cfg(debug_assertions)]).
src/parser/expressions.rs	right-hand side used to fall through to a plain `Set(v, …)` that codegen
src/parser/expressions.rs	shapes that phase 02d-iii.b's `auto_deref_boxed_scalar`
src/parser/expressions.rs	slice / range / collection-iteration shape). (@P287)
src/parser/expressions.rs	still takes the clear+refill path below. (Mirrors the @P295
src/parser/expressions.rs	the for-loop destructure in collections.rs:1289-1304).
src/parser/expressions.rs	the same shape that lines ~1020-1033 below build for
src/parser/expressions.rs	used to hand `assign_text` a target that names no variable — which
src/parser/expressions.rs	variant soundly. Used to detect a self-aliasing slice-assign (`v = v[a..b]`),
src/parser/expressions.rs	version; pass 1's body is only consulted by phase 01's
src/parser/expressions.rs	~1159-1194 handles the same shape for fields after-the-fact;
src/parser/fields.rs	(same reason plan-17 (B) bounded-T dispatch runs on both
src/parser/fields.rs	@P278/P281 — pass-1 Unknown receiver: consume the
src/parser/fields.rs	@P281 — when the dot-access is followed by `(args)`
src/parser/fields.rs	@P285 — a keyed-collection lookup RESULT is nullable (an absent
src/parser/fields.rs	@P285 — see the Hash/Radix arm above; the lookup result is nullable.
src/parser/fields.rs	@P376 — `Type::Never` is the poison an errored struct construction
src/parser/fields.rs	@PLAN58 cluster-I (boolean outer-handle stride): a vector-typed element
src/parser/fields.rs	@PLN125 arc C — `x[i]` on a library type is the call the type declared.
src/parser/fields.rs	@PLN125 arc C — a library type CAN be subscripted now, so a struct or enum
src/parser/fields.rs	@PLN125 arc C — a library type that defines `OpIndex` indexes like a built-in
src/parser/fields.rs	@PLN125 arc C — the `OpIndex` a library type defines for `x[i]`, or `u32::MAX`.
src/parser/fields.rs	B2-runtime (2026-04-13): `Sig.Idle` on a mixed struct-enum
src/parser/fields.rs	Before the fix, `s = x.to_text()` (where `x: T`,
src/parser/fields.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/fields.rs	Likely also closes plan-17 (A) caveat (implicit
src/parser/fields.rs	Plan-07 phase 4 step 4.6 — this is the for-loop
src/parser/fields.rs	Plan-07 phase 4h — count this read for the
src/parser/fields.rs	Plan-17 phase 01 (B) — bounded-T method dispatch must run
src/parser/fields.rs	Plan-19 phase 03 — method-on-parent-enum dispatch. When
src/parser/fields.rs	arms below, @P285) so `v[i] ?? default` is NOT flagged
src/parser/fields.rs	documented at parser/collections.rs:1492-1499.
src/parser/fields.rs	refusal below, which is what used to be the only answer for a struct.
src/parser/fields.rs	vantage during method-dispatch debugging (plan-17 B).
src/parser/mod.rs	#675 / @PLAN59 — give every heap-returning function a `__retbuf` attribute
src/parser/mod.rs	#675 / @PLAN59 — reserve the heap-return buffer for the functions whose
src/parser/mod.rs	(formal/binding.md B-Ref-Reshape, C79 revisited 2026-08-05). Unlike the removal
src/parser/mod.rs	(mirrors the parse-time concrete path at `vectors.rs:1532-1535`).
src/parser/mod.rs	@P296-sibling (Windows) — build the package paths with
src/parser/mod.rs	@P301 — struct-enums are heap records like
src/parser/mod.rs	@P314 — the element STORAGE type id to pass as the `OpAppendVector` /
src/parser/mod.rs	@P320: keyed-remove `coll[key] = null` lowers to
src/parser/mod.rs	@P320: keyed-remove `coll[key] = null` → `OpHashRemove(coll, …)`;
src/parser/mod.rs	@P387 — a captured-var number can arrive from a DIFFERENT var table
src/parser/mod.rs	@PLAN12 Phase 6.6 — auto-install on `use`.
src/parser/mod.rs	@PLAN12 Phase 6.7 — dedupe set for the per-invocation advisory
src/parser/mod.rs	@PLAN12 Phase 6.7 — per-invocation advisory classifier.
src/parser/mod.rs	@PLAN12 phase 2 step 2 (2026-05-24) — make
src/parser/mod.rs	@PLAN12 phase 2 step 2 (2026-05-24) — same manifest-driven
src/parser/mod.rs	@PLAN12 phase 3.5b (2026-05-24) — for path-deps
src/parser/mod.rs	@PLAN48 P2: implicitly narrowing a loft `integer` to a smaller explicit
src/parser/mod.rs	@PLAN48 P2: literal exemption — true when `code` is a constant integer that
src/parser/mod.rs	@PLAN48 P2: render an integer type with its explicit narrow alias
src/parser/mod.rs	@PLAN48 P2: true when converting `src` → `dst` narrows a loft integer to a
src/parser/mod.rs	@PLAN49 T1 — set the breadcrumb phase + initial file/line so
src/parser/mod.rs	@PLAN51 Cluster IV: tag this work-ref so parse_code's
src/parser/mod.rs	@PLAN59 arity cascade (signatures freeze at declaration) this is an
src/parser/mod.rs	@PLAN59 phase 2: the `__rref_N` recursive-self counter dance is
src/parser/mod.rs	@PLN102 arc B-semantic — the compatibility `contract` axis (a
src/parser/mod.rs	@PLN102 arc B: a constraint the loader cannot honour is
src/parser/mod.rs	@PLN102 arc C step 3 — the recommended-idiom STEER. A resolved call FROM
src/parser/mod.rs	@PLN102 arc C — generic instantiation is CALLER-SOURCE-AGNOSTIC: a
src/parser/mod.rs	@PLN11 arc E — paths of every source file parsed (stdlib + lazily-loaded
src/parser/mod.rs	@PLN11 arc E — record the input file for the whole-program cache key.
src/parser/mod.rs	@PLN11 arc E — set by the driver (`main.rs`) only when the whole-program
src/parser/mod.rs	@PLN119 arc A — libraries that declared `[library] placement = "process"`,
src/parser/mod.rs	@PLN119 arc A — note a library that asked to run out of process.
src/parser/mod.rs	@PLN12 phase 02 — does `input` end mid-construct, so a REPL should read
src/parser/mod.rs	@PLN12 phase 02 — parse one REPL input against the live session.
src/parser/mod.rs	@PLN125 arc A step A2b — the interface declares `Self.X`, an ASSOCIATED type.
src/parser/mod.rs	@PLN128 arc D — a `#c` binding's parameter list is the C function's,
src/parser/mod.rs	@PLN13 phase 6 (diagnostics slice): the name may simply be
src/parser/mod.rs	@PLN24 arc D — register a package's C libraries: the ones it DECLARES
src/parser/mod.rs	@PLN24 arc G — the optional list joins here, flagged. A shim is never
src/parser/mod.rs	@PLN26 phase 1 — two native packages exporting the SAME `#native` symbol
src/parser/mod.rs	ADVICE on both paths (loft#785-era audit, @PLN131). This used to be
src/parser/mod.rs	Bare calls into a library still do not RESOLVE — that is the rest of phase 6,
src/parser/mod.rs	Case (c): emit the deferred error. `string` used to be special-cased
src/parser/mod.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/mod.rs	Mirrors `vectors.rs:1161-1178` for perf parity with
src/parser/mod.rs	Mirrors `vectors.rs:1532-1535` — `database.vector(content_db_type)`
src/parser/mod.rs	P239 fix (2026-05-11): the for-loop iter-termination
src/parser/mod.rs	P241 fix (2026-05-11) — slice 2: integer-only. POST-PASS that
src/parser/mod.rs	P241 fix (2026-05-11) — slice 2: integer-only. Walks a Block's
src/parser/mod.rs	P241 fix (2026-05-11): post-substitution rewrite of the
src/parser/mod.rs	P251 fix (2026-05-11): for `Value::Var(v)` where `v`
src/parser/mod.rs	P252 fix (2026-05-11): also recognise `OpGetVectorNullable`
src/parser/mod.rs	Plan-06 phase 4d.A.2 — fn-ref vector elements store the
src/parser/mod.rs	Plan-06 phase 4d: emit the per-element OpSet* sequence for a
src/parser/mod.rs	Plan-06 phase 4d: nested tuple element — recurse into
src/parser/mod.rs	Plan-06 phase 4d: tuple struct field assignment.
src/parser/mod.rs	Plan-06 phase 4d: tuple struct field read. Each
src/parser/mod.rs	Plan-06 phase 4d: tuple-to-tuple convert is element-wise.
src/parser/mod.rs	Plan-07 phase 1: unspan() so wraps on `[` / `.` (steps 1.11
src/parser/mod.rs	Plan-07 phase 4h — emit `not null` field-reminder hints
src/parser/mod.rs	Plan-07 phase 4h — per-(struct_d_nr, attr_idx) read counter.
src/parser/mod.rs	Plan-07 phase 4h — set of (struct_d_nr, attr_idx) that have at
src/parser/mod.rs	Plan-07 phase 4h — site of the most recently parsed field
src/parser/mod.rs	Plan-07 phase 4h — walk user struct definitions and emit
src/parser/mod.rs	Plan-07 phase 5 suggestions. Find a similar field name on
src/parser/mod.rs	Plan-07 phase 5 suggestions. Find a similar type name —
src/parser/mod.rs	Plan-07 phase 5 suggestions. Find a similar user-defined
src/parser/mod.rs	Plan-07 phase 6 (partial) — "expected E, got G on context"
src/parser/mod.rs	Plan-17 phase 01 (A) — predict the substituted return type of a
src/parser/mod.rs	Plan-17 phase 01 (A) — propagate the substituted return type
src/parser/mod.rs	Plan-17 phase 01 — substitute through tuple element types so a
src/parser/mod.rs	Plan-22 phase 02b (2026-05-12): auto-Reference field
src/parser/mod.rs	Registry-less build: no index to consult, so no hint (@PLN13 phase 6).
src/parser/mod.rs	Spine step 7 (DONE 2026-04-29): emits `Level::Error` on
src/parser/mod.rs	The flag is backend-agnostic — it no longer forces interpret-only.
src/parser/mod.rs	The two-pass contract (H5, @PLAN59): pass 1 registers definitions
src/parser/mod.rs	Trace point: full instantiation result. Used during plan-17
src/parser/mod.rs	Used to validate constant names
src/parser/mod.rs	`None` when no published package exports such a free function (@PLN13 phase 6,
src/parser/mod.rs	`arg_pos` no longer aligns; fall back to the cursor for the rare
src/parser/mod.rs	`par_to_vec(...)` helper (planned phase 11).
src/parser/mod.rs	`parse_vector` (which no longer relies on this convert failing).
src/parser/mod.rs	`ref_return`'s promotion no longer re-finds it by work-ref NAME and therefore
src/parser/mod.rs	`set_field_check` (line 3202-3216).
src/parser/mod.rs	`tests/`; otherwise `""`. Used to locate the project-root `lib/`
src/parser/mod.rs	`u16::MAX` for a field the layout does not have, and that answer used to flow
src/parser/mod.rs	a library `n_*` symbol in src/ — the @PLAN12 extraction-hygiene gate
src/parser/mod.rs	and FINAL signatures — since @PLAN59 every body-carrying plain fn's
src/parser/mod.rs	arg → null/empty fill) was REMOVED (owner decision 2026-07-17): it was a footgun —
src/parser/mod.rs	attribute in PASS 2 instead — the cross-pass arity growth @PLAN59 exists to
src/parser/mod.rs	bytecode dispatch (`state/codegen.rs:2207-2217`, which
src/parser/mod.rs	concrete-T dispatch in `parser/vectors.rs:1560-1599`.
src/parser/mod.rs	function that already got its buffer no longer has one by that name.
src/parser/mod.rs	inference of generic call sites. Used during plan-17 (A)
src/parser/mod.rs	instead of OpCopyRecord. P255 (2026-05-12): also handle
src/parser/mod.rs	it used to be spelled `vector_of(content)`, which was the same id only
src/parser/mod.rs	lib_plan-29 W1c (2026-05-29) — register WASM bridge crate +
src/parser/mod.rs	lib_plan-29 W2 (2026-05-29) — resolve `[wasm.bridge].host_js`
src/parser/mod.rs	local-persistence path is the remaining increment, paired with phase 03's
src/parser/mod.rs	mirrors the @P266-style guard below: only populate defs
src/parser/mod.rs	narrow-int dispatch mirrors `vectors.rs:1576-1586`.
src/parser/mod.rs	no longer depends on `__ref_N` numbering being pass-stable. A counter-equality
src/parser/mod.rs	path that always got this right. (lib_plan-29 W1c bridge crate + routes,
src/parser/mod.rs	per fn — was dissolved by H1 (@PLAN59), but NOT for the reason recorded here
src/parser/mod.rs	promotion re-find no longer depends on work-ref numbering.
src/parser/mod.rs	reassignment of `v` inside the branch removes it (the proof no longer holds).
src/parser/mod.rs	routes + host_js even with NO `[native]`. This used to sit INSIDE the
src/parser/mod.rs	so @PLAN59's reservation never fired), and it is now fixed at the
src/parser/mod.rs	temporary. Used to reject `&<temporary>` at a `&` binding. The accessor
src/parser/mod.rs	that declares it), which is why this used to read only the function. An interface's
src/parser/mod.rs	the fs/env surface is no longer flagged by the coverage lint
src/parser/mod.rs	two-errors-for-one-typo cascade #376 removed.
src/parser/mod.rs	— plan-07 phase 4 step 4.6 swapped the for-loop iter step
src/parser/objects.rs	(@P376); adding a second one would bury the root error.
src/parser/objects.rs	(`Type::Enum`) or via a `Type::Reference` to an enum def. Used to seed
src/parser/objects.rs	(copy_claims panics for it, per @P295).
src/parser/objects.rs	(per C66 + DESIGN_DECISIONS 2026-05-11). Walk the
src/parser/objects.rs	(set by phase 02d-iii.a's flip helper for mutated-
src/parser/objects.rs	@P279 — when pass-1 sees an Unknown value being assigned
src/parser/objects.rs	@P295). Mirrors the keyed-LOCAL `keyed_kt` logic in
src/parser/objects.rs	@P308 — a keyed-collection field (hash/sorted/index)
src/parser/objects.rs	@P308 — the specific keyed-collection db type id (the id
src/parser/objects.rs	@P335: functions are stored mangled as `n_<name>` and are reached
src/parser/objects.rs	@P335: the RAW fallback resolves names stored un-prefixed,
src/parser/objects.rs	@P376 — POISON the errored construction, PASS 2 ONLY.
src/parser/objects.rs	@P376 — the format expression resolved to `Unknown`: a directly
src/parser/objects.rs	@P387 / @P383 — the user-facing argument types of a fn used as a
src/parser/objects.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/objects.rs	HASH-only while @P309 — a deep-copy data-loss/hang when `index<T>`
src/parser/objects.rs	Plan-07 phase 4e.1 — format strings are the user's
src/parser/objects.rs	Plan-07 phase 5: skip suggestions for very short names
src/parser/objects.rs	Plan-07 phase 6 (partial) — name the value side first
src/parser/objects.rs	Plan-22 phase 02d-iii.b — read auto-deref for boxed
src/parser/objects.rs	Plan-22 phase 02d-iii.b — wrap a resolved variable read
src/parser/objects.rs	bare DbRef — phase 02d-ii's silent gap for exotic
src/parser/objects.rs	but `s` is a real, non-hidden arg — @P387 case 2).
src/parser/objects.rs	once variants are no longer globally keyed.
src/parser/objects.rs	phase 02d-iii.a's flip).
src/parser/objects.rs	that closed @P281 / @P278 (same architectural fix:
src/parser/objects.rs	the @PLAN59 signature-time `__retbuf` after it's renamed to the returned
src/parser/objects.rs	this branch used to refuse it because the stub alone made the
src/parser/objects.rs	working once variants are no longer globally keyed (step 4).
src/parser/operators.rs	(unspanned) no longer is `Var(orig_var)`.
src/parser/operators.rs	@P313: do NOT set 0x8000 for an inline struct-literal `Block "Object"`
src/parser/operators.rs	@P316 — pick the coalesce result type. When the value and default are
src/parser/operators.rs	@P368 follow-up — when the dividend is float / single and the divisor
src/parser/operators.rs	@P368 — a literal divisor is statically known; only a *non-zero* literal
src/parser/operators.rs	@P368 — wording: not "integer" (the same `/` warning covers float
src/parser/operators.rs	@PLAN48 P2: an explicit `as <narrow-int>` is the sanctioned way to narrow
src/parser/operators.rs	@PLAN52 cluster I iteration 2 (2026-05-30): name `__ncc_N`
src/parser/operators.rs	@PLAN52 cluster IV interpret (2026-05-30): heap-DbRef types
src/parser/operators.rs	@PLAN52 cluster IV interpret (2026-05-30, iteration 3): extend
src/parser/operators.rs	@PLAN52 cluster IV-Tuple (2026-05-30): Tuple values are not heap-DbRef
src/parser/operators.rs	@PLN125 arc C — `x[i]` on a library type lowers to that type's
src/parser/operators.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/operators.rs	E0308 @P316 guards against (a wider-width *variable* default) cannot
src/parser/operators.rs	Plan-07 phase 1, step 1.B.1 — capture the operator's source
src/parser/operators.rs	Plan-07 phase 1, step 1.B.1 — wrap binary fault-prone
src/parser/operators.rs	Plan-07 phase 1, steps 1.11 + 1.12 — capture the chaining
src/parser/operators.rs	Plan-07 phase 1, steps 1.11 + 1.12 — wrap the chained
src/parser/operators.rs	Plan-07 phase 1: unspan() so wraps on `.`
src/parser/operators.rs	Plan-07 phase 4d — vector / text indexing followed by `??`.
src/parser/operators.rs	Plan-07 phase 4e.1 — recursive variant of
src/parser/operators.rs	Plan-07 phase 4e.2 — entry point. Called from `parse_function`
src/parser/operators.rs	Plan-07 phase 4e.2 — undefended fault-site compile-time warning
src/parser/operators.rs	Plan-07 phase 4f.5 — float / single div / mod by zero
src/parser/operators.rs	Plan-07 phase 4f.5 — float / single div / mod peers.
src/parser/operators.rs	Plan-07 phase 4h — if the `??` LHS is the just-emitted
src/parser/operators.rs	Plan-22 phase 02d-iv — character / text cell writes
src/parser/operators.rs	Self-concat reassign `v = v + [...]` (sibling of @P390's
src/parser/operators.rs	True when `src` is a `len(<Var>)` call — used to recognise the
src/parser/operators.rs	`DESIGN_DECISIONS.md` 2026-05-11: format strings are the user's
src/parser/operators.rs	answered the FALLBACK for a PRESENT element, losing the scalar half with
src/parser/operators.rs	churn). Same shape as the @P311 OpSetKeyed fix, here on the
src/parser/operators.rs	matching the @P390 / @P287 temp precedent.
src/parser/operators.rs	on both parser passes — pass-stable, no @P384 alloc divergence.
src/parser/operators.rs	plan-57 cluster II — a literal / comprehension / struct-vector
src/parser/operators.rs	prefix instead). Sibling of @P390's self-slice. Materialise
src/parser/operators.rs	sequencing. Tracked in plan-07 phase 4e.3 row.
src/parser/operators.rs	to swap the inner op. @P356: this list previously covered only
src/parser/operators.rs	warning no longer fires on a statically-safe float division.
src/parser/operators.rs	while the full width keeps the branches the same native type (no @P316 E0308).
src/parser/operators.rs	would override — arc B; no marker exists yet). `emit_variant_value`
src/parser/vectors.rs	! 3729-3755) handles today's `p86_lambda_capture_*`
src/parser/vectors.rs	! Plan-22 phase 01 — verify `collect_mutated_captures` populates
src/parser/vectors.rs	! Plan-22 phase 02d-i — verify the parent-function
src/parser/vectors.rs	! Plan-22 phase 02d-ii — verify `synthesize_cell_structs`
src/parser/vectors.rs	! Plan-22 phase 02d-iii.a — verify
src/parser/vectors.rs	! Plan-22 phase 02d-iii.b — verify
src/parser/vectors.rs	! Plan-22 phase 02d-iii.c — verify
src/parser/vectors.rs	! Plan-22 phase 02d-iii.d — verify
src/parser/vectors.rs	! be removed without a regression.
src/parser/vectors.rs	! representative shapes phase 02 will consume.
src/parser/vectors.rs	! representative shapes phase 02d-iii will consume. No
src/parser/vectors.rs	"Scalar" set (per phase 02d design):
src/parser/vectors.rs	(exotic integer widths) — phase 02d-ii's silent gap.
src/parser/vectors.rs	- Text: phase 02d-vi added a bypass guard in
src/parser/vectors.rs	2 GiB word limit (`src/store.rs:643`). Mirror the @P327 fix in
src/parser/vectors.rs	@P213/@P214) and currently CRASHES at runtime ("Write to read-only
src/parser/vectors.rs	@P274 — non-text/non-character parts (integer / float /
src/parser/vectors.rs	@P274 — see RefVar branch above.
src/parser/vectors.rs	@P314 — narrow-aware element type (see `append_elem_tp`).
src/parser/vectors.rs	@P315 — `declared` is true when the element type comes from a typed
src/parser/vectors.rs	@P315 — the element type is DECLARED (typed local / struct
src/parser/vectors.rs	@P325 — coroutine comprehensions `[for v in gen() { … }]` had NO
src/parser/vectors.rs	@P380 (generalized, plan-58 cluster II): a freshly-created
src/parser/vectors.rs	@P380 handle-zero is now hoisted above (after the element
src/parser/vectors.rs	@P395 — a parenthesised vector concat consumed by a trailing
src/parser/vectors.rs	@PLAN58 III-a: propagate the declared element type `in_t` into the
src/parser/vectors.rs	@PLAN58 cluster IV: a NESTED comprehension's body is a vector (a 12-byte
src/parser/vectors.rs	@PLAN58 cluster IV: use the SAME element-type `known` as the proven
src/parser/vectors.rs	@PLAN58 cluster-I (boolean outer-handle stride): for a nested
src/parser/vectors.rs	A typed standalone empty `[]` (the @P365 fall-through above) has no
src/parser/vectors.rs	Case B (Reference) — `s` is a struct; phase 02c handles
src/parser/vectors.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/parser/vectors.rs	Int(1)])`; phase 02d-v's `extract_boxed_var_from_lhs`
src/parser/vectors.rs	P260 (2026-05-13): ALL Reference captures store as a 12-byte `Parts::DbRef`
src/parser/vectors.rs	Plan-06 phase 4d.A.2 — fn-ref in a vector stores as
src/parser/vectors.rs	Plan-07 phase 1: unspan() so wraps on `.` (step 1.12) don't
src/parser/vectors.rs	Plan-22 phase 01 — walk the lambda body's IR and identify
src/parser/vectors.rs	Plan-22 phase 02c (2026-05-12): collect mutations BEFORE
src/parser/vectors.rs	Plan-22 phase 02c (2026-05-12): for each mutated Reference
src/parser/vectors.rs	Plan-22 phase 02c (2026-05-12): the captured-name list comes
src/parser/vectors.rs	Plan-22 phase 02d-i (2026-05-12): accumulate scalar
src/parser/vectors.rs	Plan-22 phase 02d-i — accumulate the names of scalar-typed
src/parser/vectors.rs	Plan-22 phase 02d-ii — canonical cell-struct name for a scalar
src/parser/vectors.rs	Plan-22 phase 02d-ii — canonical type for the cell's `value`
src/parser/vectors.rs	Plan-22 phase 02d-ii — ensure a `__cell_<T>` struct
src/parser/vectors.rs	Plan-22 phase 02d-ii — ensure a `__cell_<T>` struct exists
src/parser/vectors.rs	Plan-22 phase 02d-iii.a — at the start of pass 2 for the
src/parser/vectors.rs	Plan-22 phase 02d-iii.e / 02d-v / 02d-vi — all
src/parser/vectors.rs	Plan-22 phase 02d-iii.e — pre-box `captured_names`
src/parser/vectors.rs	Plan-22 phase 02d-iii.e — replace `captured_names` entries
src/parser/vectors.rs	Plan-22 phase 02d-vi — text is now flipped via the
src/parser/vectors.rs	Plan-22 phase 02d-vii's blanket "skip text when the parent returns text" is
src/parser/vectors.rs	Returns the conventional name `__cell_<T>` used to box a scalar
src/parser/vectors.rs	SUCCEEDS, so the "cannot store" diagnostic below no longer fires here.
src/parser/vectors.rs	Struct (Reference) captures are handled by phase 02c's
src/parser/vectors.rs	The non-empty dep activates phase 02b's auto-Reference
src/parser/vectors.rs	This case used to fall through, deferred on the premise that a boolean cell
src/parser/vectors.rs	`Reference(__cell_int, _)` from creation, so phase 02c's
src/parser/vectors.rs	`s` is a struct (Reference type) — handled by phase 02c
src/parser/vectors.rs	back through the auto-Reference path (phase 02b/02c encoding).
src/parser/vectors.rs	capture (per phase 01's `mutated_captures` walker, populated
src/parser/vectors.rs	cell struct via the same machinery phase 02d-ii uses.
src/parser/vectors.rs	for proper liveness tracking. For phase 02c's case-B
src/parser/vectors.rs	in pass 1 by phase 02a), the attribute type is
src/parser/vectors.rs	names queued by phase 02d-i's accumulator).
src/parser/vectors.rs	other paths (Reference: phase 02c; Function: phase 02c via
src/parser/vectors.rs	queued. `r` (read-only) is excluded by phase 01's
src/parser/vectors.rs	ran first — the original phase 01 path) OR from the lambda's
src/parser/vectors.rs	silently skipped — phase 02d-iii's outer-binding rewrite
src/parser/vectors.rs	skips it. That disagreement is what plan-22 02d-vii papered over with "skip text
src/parser/vectors.rs	the explicit `Mutable<T>` path in phase 05).
src/parser/vectors.rs	type threaded in (@P365) — falls through to the normal construction
src/placement.rs	! # What a token cannot cover, and why it no longer has to (loft#827)
src/placement.rs	! derivation, arc H replaces the bucket array with an inline entry array. A store
src/placement.rs	! widens a bucket slot to `(rec, hash)`, arc E changes the hash function and the index
src/placement.rs	`2` is @PLN135 arc H. A slot used to hold the record number of an entry that was a
src/platform.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/platform.rs	Used to normalise incoming paths that may carry the foreign separator.
src/platform.rs	double compilation is removed — see PERFORMANCE.md § Design: BUILD1.
src/platform.rs	recorded name alone, so consumers went looking for a `.tmp` that no longer
src/png_store.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/profiler.rs	@PLN140 arc B/C — the loft-level profiler (CPU hot spots, and paths to allocations)
src/profiler.rs	Not a hypothetical: the arc C oracle
src/profiler.rs	The defect the arc C oracle caught: a fixed period only ever samples one phase
src/profiler.rs	and `LOFT_ALLOC_PATHS` (arc C, `=<n>` sets the allocation sample rate).
src/profiler.rs	arc C — an op allocated `stores` stores; sample one in `alloc_interval` and
src/profiler.rs	── arc C: paths to ALLOCATIONS ──────────────────────────────────────────────
src/radix_db.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/radix_db.rs	The BOX surface no longer composes the two ([`box_walk`] walks the box itself,
src/radix_db.rs	it used to be this call plus [`in_box`], and the `till` form is what that pairing
src/radix_db.rs	match, so a same-bucket sibling is never removed by mistake).
src/radix_tree.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/radix_tree.rs	R6b — a removed node's id is reused rather than minted afresh.
src/radix_tree.rs	`get` no longer re-scans the key: it trusts `d`, the bit at which `seek` saw the
src/registry.rs	The part after the `:` in a status field (e.g. `"CVE-2026-001"` from `"yanked:CVE-2026-001"`).
src/registry_advisories.rs	! "published": "2026-05-30T08:00:00Z",
src/registry_advisories.rs	! "updated": "2026-05-31T12:00:00Z",
src/registry_advisories.rs	! @PLAN12 Phase 6.7 — security advisory channel.
src/registry_index.rs	@P332: resolve the home base from `LOFT_HOME` FIRST, falling back to
src/registry_index.rs	@PLAN12 Phase 6.11 — support `file://` URLs for offline
src/registry_index.rs	malformed entry used to take `loft install` down for everyone:
src/registry_index.rs	the "you probably meant `pkg::name`" diagnostic (@PLN13 phase 6, the
src/registry_keys.rs	! removed in a subsequent loft release.
src/registry_keys.rs	Trust root bootstrapped 2026-06-13 (REGISTRY_BOOTSTRAP.md § Step 1.5) —
src/registry_signing.rs	binary, so a test can no longer reach the empty case through it.
src/repl.rs	! @PLN12 phase 03 — interactive REPL session.
src/repl.rs	**Text is the exception (@P293):** reading a text return off the entry stack
src/repl.rs	@P293 — a **text** value can't be captured by returning it from a synthetic
src/repl.rs	@PLN120 E3a — SAY WHAT HAPPENED. This used to print a fixed
src/repl.rs	@PLN14 arc A — **the binding environment**: name → where that binding's value
src/repl.rs	@PLN14 arc A — **the session store**: one detached `Store` holding a
src/repl.rs	@PLN14 arc A — file the shadow env entry under the
src/repl.rs	@PLN14 arc A — initial word size of the session store. It grows on demand
src/repl.rs	@PLN14 arc A — one binding's home in the session store.
src/repl.rs	@PLN14 arc A — read a binding back **from the session store** (not from the
src/repl.rs	@PLN14 arc A — the names that currently have a store-resident value.
src/repl.rs	@PLN14 arc A — the session store's slot in `state`, adopting the carried
src/repl.rs	@PLN14 arc B — copy `root` into the session store and park the resulting
src/repl.rs	@PLN14 arc B — the value the most recent capture materialized, waiting to be
src/repl.rs	@PLN14 arc C — **scalars at rest**: box `v` into a 1-field record in the
src/repl.rs	@PLN14 arc C — a scalar in the exact form it left the stack in. Kept raw
src/repl.rs	@PLN14 arc C — how a boxed scalar is read back out of the session store.
src/repl.rs	@PLN14 arc C — the wrapper above materialized a `vector<text>`, but
src/repl.rs	@PLN14 arc D — **the frame-seed**: load prior names from the session store
src/repl.rs	@PLN14 arc D — one binding's frame-seed result, as the differential Step 4 is
src/repl.rs	@PLN14 arc E — **the flip**: when on, observing a store-resident binding
src/repl.rs	@PLN14 arc E — THE FLIP: a bare name with a store-resident value is
src/repl.rs	@PLN14 arc E — a binding's value read from the session store and rendered
src/repl.rs	@PLN14 arc E — answer from the session store when EVERY bound name has a
src/repl.rs	@PLN14 arc E — the own-format read (`value_of`) is answered from the
src/repl.rs	@PLN14 arc E — turn store-backed observing on or off for this session.
src/repl.rs	@PLN14 arc E — whether a fresh session starts with store-backed observing on.
src/repl.rs	@PLN14 arc E — whether observing currently reads the session store.
src/repl.rs	@PLN14 arc F — restore a [`save_session_image`](Self::save_session_image).
src/repl.rs	@PLN14 arc F — resume-image header: a magic so a foreign file is refused, and
src/repl.rs	@PLN14 arc F — the layout key this session's stored values depend on.
src/repl.rs	@PLN14 arc F — the outcome of loading a resume image. Every non-`Loaded`
src/repl.rs	@PLN14 arc F — write the session's store-resident values to `path` as a
src/repl.rs	@PLN14 arc G — how many records the session store currently holds (`0`
src/repl.rs	@PLN14 arc G — release a session-store entry whose name has been re-bound.
src/repl.rs	@PLN14 arc H — evaluate one line and **return** its rendered value instead
src/repl.rs	@PLN16 phase 2 — evaluate `expr` against the paused frame and render the result
src/repl.rs	@PLN16 phase 2 — run `input` as an observing statement (the RPC `run` request),
src/repl.rs	@PLN16 phase 2 — the wire protocol's `setBreakpoints` unit: a `file:line`
src/repl.rs	A scalar boxed into a 1-field record (arc C).
src/repl.rs	Abandon a paused debug session, dropping the held state — used to recover the
src/repl.rs	Entries skipped because they no longer parse/run — a stale or corrupt
src/repl.rs	It used to print a fixed category line and drop the `Box<dyn Any>`, so every
src/repl.rs	Stored as the single-element `vector<text>` the @P293
src/repl.rs	The two eval attempts used to be `catch_unwind(…).unwrap_or(None)`, so a compiler
src/repl.rs	The wrapper is an implementation detail of the @P293 work-around, not
src/repl.rs	WHICH edit is no longer undoable.
src/repl.rs	[`eval`](Self::eval) with recording suppressed; an entry that no longer
src/repl.rs	`:slots` commands (reuses phase 01's `introspect`).
src/repl.rs	`capture_binding` builds to dodge @P293. The characters ARE store-resident
src/repl.rs	`n` used to run `:next` — and typing `c` ran `:continue`, resuming the
src/repl.rs	already a record (arc B copies it), a scalar is not (arc C boxes it).
src/repl.rs	arc B materializes a [`Captured::Heap`] ref, arc C boxes a
src/repl.rs	arc G collects it.
src/repl.rs	arc G — a re-bind orphans the old record; release
src/repl.rs	call-returned-owned text that survives the frame teardown (@P293-safe) —
src/repl.rs	channel as arc B's diagnostics (so the interactive prompt prints it AND the
src/repl.rs	in scope on this line used to get the same "couldn't evaluate" as a typo,
src/repl.rs	instead is exactly what @P293 forbids — a borrowed text read off the
src/repl.rs	live-frame eval follow-up (P1b) — but it no longer poisons unrelated expressions.
src/repl.rs	refused. `None` when a referenced type no longer resolves, which is itself
src/repl.rs	sidesteps the @P381 CONST_STORE re-lock and isolates a runtime
src/repl.rs	text, which arc C stores inside the vector. Left to Step 5.
src/repl.rs	that observing a store-resident binding no longer advances this counter,
src/repl.rs	type id (arc B).
src/repl.rs	what the debugger's abandon path used to do — collapses every distinct runtime
src/repl.rs	when no longer paused.
src/rpc.rs	! @PLN16 M5d phase 2 — the **`--rpc` debug server**: an NDJSON request/response + event
src/runtime_error.rs	! Plan-07 phase 4 — typed runtime errors.
src/runtime_error.rs	! fault sites. After phase 4, every fault attributable to user code is
src/runtime_error.rs	Plan-07 phase 4g.1 / 4g.2 — call-chain at raise time
src/sandbox.rs	`def.native()`, which arc A leaves EMPTY on a `#c` definition on
src/scopes.rs	(@P297), the result is `Insert([Set(__lift_N, …), final])` — a
src/scopes.rs	(plan-57 cluster I) so the block-exit `free_vars` sweep frees its store
src/scopes.rs	11 sites used to re-derive (A.3): a callee with a ref param that
src/scopes.rs	@P297 — a USER struct-returning call (`n_*` with a body) passed
src/scopes.rs	@P302 — a keyed-collection local backed by its OWN store
src/scopes.rs	@P303 / #490 — a user fn returning a heap struct-enum that the
src/scopes.rs	@P322 — when the function body ends with a nested
src/scopes.rs	@P329: tuple-of-text return — when an element is a non-literal
src/scopes.rs	@P376 follow-up — no const-param unlock emitted anymore (matching
src/scopes.rs	@P378(a) — INVERSE of `paired_witness` for the case where the
src/scopes.rs	@P378(a) — `v` is an inner-scoped witness whose store
src/scopes.rs	@P378(a) — witness `v` is INNER-scoped (e.g.
src/scopes.rs	@P378(b) — a heap-returning tail `If` whose CONDITION lifted a
src/scopes.rs	@P393 (t9) — a loft-source fn OR `t_` method returning an OWNED vector
src/scopes.rs	@PLAN52 cluster I iteration 2 (2026-05-30): honor skip_free
src/scopes.rs	@PLAN53 cluster 2 / S4: in aligned mode each arg + the return-address
src/scopes.rs	@PLAN53 cluster 2: remap the var-numbers these IR nodes carry through
src/scopes.rs	@PLAN53 — the aligned V2 allocator is the ONLY allocator. Compute the
src/scopes.rs	@PLN125 arc B — the scope-end hook `v`'s type declares, as a call on `v`.
src/scopes.rs	@PLN125 arc B — the scope-end hook fires where the BINDING's life
src/scopes.rs	A `__cell_<T>` is minted FOR this closure (plan-22 boxes a mutated scalar /
src/scopes.rs	ARC.md A4 (closed 2026-05-07) — `parallel_for_light` was
src/scopes.rs	Currently no production caller — phase 5b proper hooks it.
src/scopes.rs	Currently no production caller — phase 5b proper hooks the
src/scopes.rs	Currently no production caller — phase 5b' wires this in place
src/scopes.rs	EXCEPTION (@P302): keyed collections — `s = []` lowers to
src/scopes.rs	Plan-04 close-out (2026-04-22): V1 remains the slot
src/scopes.rs	Plan-06 phase 5b minimal — purity-driven `is_par_safe` classifier.
src/scopes.rs	Plan-06 phase 5b' G5 — calling a ParentWrite stdlib fn
src/scopes.rs	Plan-06 phase 5b' G5 — for a `ParentWrite` callee, treat the
src/scopes.rs	Plan-06 phase 5b' G5 — heap-typed return values are passed
src/scopes.rs	Plan-06 phase 5b' — DEEP parent-write check for par() workers.
src/scopes.rs	Plan-06 phase 5b' — precise shallow par-safety check.
src/scopes.rs	Plan-06 phase 5d (DESIGN.md D8 diagnostic shapes) — explains
src/scopes.rs	Plan-06 phase 5e — monotonic fixed-point over the call graph.
src/scopes.rs	Used by phase 5b proper's codegen integration: when
src/scopes.rs	`free_vars` sweep frees the store there. Empty on phase 1; populated from
src/scopes.rs	`gen_set_first_ref_var_copy` (state/codegen.rs:1025-1033)
src/scopes.rs	`trigger` allocates. Their scope-exit `OpFreeRef` is REMOVED, not kept as an
src/scopes.rs	and no longer called `__retbuf`: `ref_return` renames the attr to the
src/scopes.rs	and set each variable's scope. Runs once normally; plan-57 cluster I re-runs
src/scopes.rs	direct calls are checked. Plan-06 phase 5b' (eventual)
src/scopes.rs	empty / garbage bytes. See PROBLEMS.md @P329.
src/scopes.rs	fn-ref` in native codegen, @P258/@P259 territory).
src/scopes.rs	in the dep list (`src/parser/mod.rs:1924-1928`) so the
src/scopes.rs	it no longer owns. When that source is an inline call it is lifted into a
src/scopes.rs	loft#849 / @PLN139 — vars that no longer OWN what they hold, so their scope end must
src/scopes.rs	pitfall `scan_set` was patched for under @P198 (`value.unspan()`).
src/scopes.rs	rather than a borrow (@P302).
src/scopes.rs	retired and the sentinel is removed (the reliable checks subsume
src/scopes.rs	runs (maker, 2026-08-05). It is the rustc bargain in loft's spelling: where rustc refuses
src/scopes.rs	scope-exit free can no longer strand the free's `var_…` reference out
src/scopes.rs	shape upstream or belongs to the @P329 tuple-of-text branch, and neither
src/scopes.rs	site: the plain scope-exit free, the @P317 tag-checked free, and
src/scopes.rs	suppression. A debug sentinel used to scream when the old read
src/scopes.rs	the single-valued `dep` no longer records because its holding local was
src/scopes.rs	there instead of at function exit. `confined` is empty on phase 1, so
src/scopes.rs	trick — phase 5e replaces with proper monotonic fixed-point).
src/scopes.rs	valid witness into the @P378(a) branch below.
src/scopes.rs	── Plan-06 phase 5b — par-safety analyser (DESIGN.md D8) ────────────────────
src/scopes.rs	── Plan-06 phase 5b' shallow check (precise, no false positives) ────────────
src/scopes.rs	── Plan-06 phase 5d — par-safety diagnostic helpers (DESIGN.md D8) ──────────
src/scopes.rs	── Plan-06 phase 5e — fixed-point par-safety (DESIGN.md D8 phase 5e) ────────
src/self_update.rs	The plan called 30 and @PLAN12 §6.7 "half a system" each: 6.7 produces the yank
src/self_update.rs	removed. Refusing here would wall off precisely the people `--from` exists for.
src/self_update.rs	that no longer exist, so `verify-self` would report a permanent, meaningless
src/siphash.rs	! the inversion this module buys: what used to be silent data corruption is now a red
src/spatial.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/stack.rs	@PLAN53 S4 — the gap between what a fn-ref op MOVES and what its stdlib
src/stack.rs	@PLAN53 cluster 2 / S4 — codegen mirror of `State::stack_step`: one
src/stack.rs	@PLAN53 cluster 2 / S4: a mutable param / the return value each
src/stack.rs	Both terms are `size()`-derived (see @PLAN53 cluster 2 / S1):
src/stack.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/startup_cache.rs	! @PLN11 arc D / D2b — opt-in stdlib startup cache wiring.
src/startup_cache.rs	! skipping the parse of `default/` entirely (~12× faster, see § arc D probe).
src/startup_cache.rs	─── whole-program cache (arc E) ─────────────────────────────────────────────
src/state/codegen.rs	(reassignment — the @P302 `s = []` clear): the slot already holds a
src/state/codegen.rs	2026-04-28 element_offsets update). Reaches this branch only
src/state/codegen.rs	@P283 — the same dispatch is needed for `OpAppendText`
src/state/codegen.rs	@P290 — protect caller args from being freed by the
src/state/codegen.rs	@P290 — soft free-protect over the deep-copy; writes / claims
src/state/codegen.rs	@P302 — `s = []` on a populated keyed local clears it IN PLACE
src/state/codegen.rs	@P305 — keyed collections passed by `&` are DbRef-backed
src/state/codegen.rs	@P327 — `value_size` packs two fields (encoding from
src/state/codegen.rs	@P328 — a non-capturing closure has no closure variable; push
src/state/codegen.rs	@P346: assigning "" to a RefVar(Text) is NOT a no-op — it
src/state/codegen.rs	@P356: an EXPRESSION if whose true branch produces a value but whose
src/state/codegen.rs	@P377-CORRUPTION: skip when this is an S1-substituted call
src/state/codegen.rs	@PLAN51 Cluster II Step 2 — collect caller-hidden-buf
src/state/codegen.rs	@PLAN51 Cluster II Step 2 — collect caller-hidden-buf work-ref
src/state/codegen.rs	@PLAN51 Cluster II Step 2 — post-wrap free + sentinel
src/state/codegen.rs	@PLAN51 Cluster II Step 2 — post-wrap free + sentinel reset
src/state/codegen.rs	@PLAN51 Cluster II Step 2 — post-wrap sentinel reset.
src/state/codegen.rs	@PLAN51 Cluster II/III — also skip the pre-Set OpFreeRef
src/state/codegen.rs	@PLAN53 S4: 20B fn-ref push − 16B `text` account.
src/state/codegen.rs	@PLAN53 S4: both occupy stepped spans.
src/state/codegen.rs	@PLAN53 S4: the arg occupies a stepped span on the stack.
src/state/codegen.rs	@PLAN53 S4: the block result occupies a stepped span.
src/state/codegen.rs	@PLAN53 cluster 2 / S4 (2b+2c): OpIterate pushes the start+finish
src/state/codegen.rs	@PLAN53 cluster 2 / S4: a DbRef push occupies a stepped span; the
src/state/codegen.rs	@PLAN53 cluster 2 / S4: args were pushed at stepped spans.
src/state/codegen.rs	@PLAN53 cluster 2 / S4: args_size is the runtime frame-base
src/state/codegen.rs	@PLAN53 cluster 2 / S4: each arg + the return-address slot
src/state/codegen.rs	@PLAN53 cluster 2 / S4: each arg occupies a stepped span (Σ step).
src/state/codegen.rs	@PLAN53 cluster 2 / S4: round the frame high-water mark to 8 so the
src/state/codegen.rs	@PLAN53 cluster 2 / S4: stepped DbRef span (see emit_push_sentinel).
src/state/codegen.rs	@PLAN53 cluster 2 / S4: stepped DbRef span; dep_pos shifts by the
src/state/codegen.rs	@PLAN53 cluster 2 / S4: stepped arg span (identity when off).
src/state/codegen.rs	@PLAN53 cluster 2 / S4: the args + the create-stack dest DbRef each
src/state/codegen.rs	@PLAN53 cluster 2 / S4: the iterator-pushed value (+4) and the
src/state/codegen.rs	@PLAN53 cluster 2 / S4: the value occupies a stepped eval span;
src/state/codegen.rs	@PLN129 arc C — always-non-null: "" IS the healthy answer.
src/state/codegen.rs	@PLN24 arc D — a **`#c` binding** returning text: a C `char *` brought back
src/state/codegen.rs	@PLN24 arc D — a `#c` binding returning text. Registered
src/state/codegen.rs	@PLN24 arc D — a `#c` text binding reaching the ORDINARY call emission
src/state/codegen.rs	@PLN24 arc D — the `#c` twin of the line above, keyed on the
src/state/codegen.rs	@PLN24 arc D — the `#c` twin. This is the THIRD place a text producer
src/state/codegen.rs	B7 (2026-04-13): set up the same frame layout the regular
src/state/codegen.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/state/codegen.rs	LAST element fell off the end and was silently lost, and a write onto a
src/state/codegen.rs	OpCopyRecord 0x8000 already freed the source or @P290
src/state/codegen.rs	Phase 09 phase 00 step 0.7 — RawExpr is created only by native
src/state/codegen.rs	Plan-06 phase 4d: nested tuple element — push
src/state/codegen.rs	Plan-07 phase 1 step 1.20 — record the entry pc → source
src/state/codegen.rs	Plan-07 phase 4 step 4.6 — `OpGetVectorNullable` /
src/state/codegen.rs	Slot-move is removed in B.3.h, at which point `slot_offset`
src/state/codegen.rs	The V2 allocator is the ONLY allocator (@PLAN53) — every
src/state/codegen.rs	`n_protect_store_frees` so the two semantics no longer collide.
src/state/codegen.rs	`scan_set` no longer elides keyed `Set(v, Null)`. Without this
src/state/codegen.rs	and the nested-tuple recursion live. This arm used to hand-roll
src/state/codegen.rs	behaviour the over-broad S1 match used to produce.
src/state/codegen.rs	bytecode-level overhead. Once slot-move is removed, the
src/state/codegen.rs	from both neighbours — a `#c` definition carries no `native` symbol (arc A
src/state/codegen.rs	half of @P290). Bails for every other shape
src/state/codegen.rs	or any tuple containing a tuple) previously panicked here
src/state/codegen.rs	parse pass whose final lowering no longer touches it — and a
src/state/codegen.rs	position so phase 3's runtime-error printer can surface
src/state/codegen.rs	push. @PLAN53 S4: the yielded value occupied a stepped span.
src/state/codegen.rs	self). Once slot-move is removed, this becomes the
src/state/codegen.rs	taught so far; the armed-corpus sweep 2026-06-12
src/state/codegen.rs	the dead emit is removed.
src/state/debug.rs	@PLAN53 cluster 2 / S4 — alignment invariant guard (trace path).
src/state/debug.rs	@PLAN53 cluster 2 / S4: match execute_argv's aligned entry base.
src/state/debug.rs	@PLAN53 cluster 2 / S4: the entry frame base is the STEPPED
src/state/debug.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/state/debug.rs	is set; used to emit `[alloc #N at pc=…]` and matching `[free #N
src/state/io.rs	@P295 — deep-copy a keyed collection (`sorted`/`hash`/`index`) into a
src/state/io.rs	@P305 — `coll[key] = value` insert-or-replace into a keyed
src/state/io.rs	@P317 — LOFT_LOG=copy_check: warn if the deep copy changed any nested
src/state/io.rs	@P349: consult VIRT_FS first — `compile_and_run` populates it with
src/state/io.rs	@P372: size the target record to the struct, not a fixed 8 words.
src/state/io.rs	@PLAN51 Cluster II Step 2 — handle u16::MAX sentinel inputs.
src/state/io.rs	@PLAN51 Cluster II — true alias copy is a no-op. When data
src/state/io.rs	@PLAN53 cluster 2 / 2f: get_var adds NO step, so this finish-read
src/state/io.rs	@PLAN53 cluster 2 / 2f: this is the lone non-self-compensating
src/state/io.rs	@PLAN53 cluster 2 / S4 (2b+2c): push the iterator state as ONE
src/state/io.rs	@PLAN53 cluster 2 / S4 (2d): format_db pops the composite value's 12-byte
src/state/io.rs	@PLAN53 cluster 2 / S4 (2d): see format_database — back up by the stepped
src/state/io.rs	@PLN118 arc B — mark the deep-copy so the gen-detector classifies a stale read here
src/state/io.rs	@PLN118 arc B — same copy-context mark as `copy_record` (finish deep-copies the
src/state/io.rs	@PLN129 arc A — the miss path consults a bound source; identical to
src/state/io.rs	@PLN90 phase 1 — make the copy visible. A real record deep-copy is about to
src/state/io.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/state/io.rs	Plan-57 Phase C: single-ownership (ref-count removed) — close the OS file
src/state/io.rs	Removed the last element in iteration order; signal end-of-iteration
src/state/io.rs	`OpGetFileText` — the warning used to live here only, so `--native`
src/state/io.rs	driver. A program with a driver for another type used to reach
src/state/io.rs	is loft code, and the retry after it is arc A's rule
src/state/io.rs	read the same** (@PLN129 arc C): "no such person" is stable and true,
src/state/io.rs	through its `--dir` preopens. `--html` used to need a third arm here
src/state/io.rs	wrong answer arc C exists to prevent.
src/state/mod.rs	"couldn't evaluate" used to collapse.
src/state/mod.rs	(@PLN118 arc B) can name WHERE the store was prematurely freed, not
src/state/mod.rs	(see `src/generation/calls.rs`). @P275.
src/state/mod.rs	@P294: cached byte-capacity of the value-stack store (`stack_cur`).
src/state/mod.rs	@P294: ensure the value-stack store (`stack_cur`) buffer can hold a
src/state/mod.rs	@P294: native lib fns push their results through the stack store
src/state/mod.rs	@P294: the frame-snapshot copy + the Created-status zone
src/state/mod.rs	@P317 — aggregate leaked stores BY TYPE, most-leaked first, so the
src/state/mod.rs	@P356 — record a RECOVERABLE fault (out-of-bounds / negative vector
src/state/mod.rs	@P387 zero-cost: the parser injects exactly ONE text work-buffer for a
src/state/mod.rs	@PLAN49 T1 — runtime phase breadcrumb. One call per
src/state/mod.rs	@PLAN53 cluster 2 / 2h: copy the input bytes as ONE CONTIGUOUS chunk.
src/state/mod.rs	@PLAN53 cluster 2 / 2i: the worker body's frame reserves the tuple arg
src/state/mod.rs	@PLAN53 cluster 2 / S4 (2a): codegen lays the return slot at a STEPPED span
src/state/mod.rs	@PLAN53 cluster 2 / S4 (R4): push the value_size zero bytes as
src/state/mod.rs	@PLAN53 cluster 2 / S4 — alignment invariant guard. In aligned
src/state/mod.rs	@PLAN53 cluster 2 / S4 — homegrown alignment guard (debug + aligned only).
src/state/mod.rs	@PLAN53 cluster 2 / S4 — one eval-TOS / frame-reserve advance, always
src/state/mod.rs	@PLAN53 cluster 2 / S4 — when true (`LOFT_ALIGN=1`), the eval-TOS
src/state/mod.rs	@PLAN53 cluster 2 / S4: 8-rounded stepped spans, mirroring
src/state/mod.rs	@PLAN53 cluster 2 / S4: the entry frame base must be 8-aligned in
src/state/mod.rs	@PLAN53 cluster 2 / S4: the returned value occupies a stepped slot
src/state/mod.rs	@PLAN53 cluster 2 / S4: the returned value sits at the LOW end of
src/state/mod.rs	@PLAN53 cluster 2 / S4: the value slid to `base` occupies a stepped slot.
src/state/mod.rs	@PLAN53 cluster 2 / S4: the value's footprint on the stack is its
src/state/mod.rs	@PLAN53 cluster 2 / S4: the yielded value sits in a stepped slot at TOS.
src/state/mod.rs	@PLAN53 cluster 2: the worker's `copy_result`/`fn_return` advanced TOS by
src/state/mod.rs	@PLAN53 cluster-1 Miri finding) — write through the unaligned
src/state/mod.rs	@PLAN59: the entry fn may carry hidden heap return-buffer attrs
src/state/mod.rs	@PLN118 arc B refinement — a stale read DURING a record deep-copy is the
src/state/mod.rs	@PLN118 arc B — attribute the FREE site. Prefer the CAUSAL free (the one
src/state/mod.rs	@PLN118 arc E — also name the freeing OP (recorded with the free site) so
src/state/mod.rs	@PLN118 arc E — the READING op is the one currently dispatching (this
src/state/mod.rs	@PLN119 arc B — a struct / vector: the 12-byte `DbRef` naming the record
src/state/mod.rs	@PLN14 arc D — the live frame's slot for local `name`, as a [`DbRef`]
src/state/mod.rs	@PLN140 arc A — where the heap went: live store bytes at the run's **peak**,
src/state/mod.rs	@PLN140 arc B — one op of a profiled run: count it, and when it is a sample
src/state/mod.rs	@PLN140 arc B — the sample tick rides the branch that got us here, which is
src/state/mod.rs	@PLN140 arc B/C — arm the loft-level sampler from the environment, if it is
src/state/mod.rs	@PLN140 arc B/C — resolve this run's samples against `data` and add them to
src/state/mod.rs	@PLN140 arc B/C — what this run spent its time on, and what path reached each
src/state/mod.rs	@PLN140 arc C — allocation PATHS. Hoisted out of the loop like `reload_on`
src/state/mod.rs	@PLN140 arc C — the op at `pc` allocated `stores` stores; hand the path that
src/state/mod.rs	@PLN140 arc C — the op just ran; if it took stores, record the path
src/state/mod.rs	@PLN18 phase 02 — tier-0 live reload: a counter-gated poll so a file
src/state/mod.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/state/mod.rs	PKG.1: Replace a previously registered function (e.g. a stub) with a
src/state/mod.rs	Plan-06 phase 1 G2 — primitive-input worker dispatch.
src/state/mod.rs	Plan-06 phase 1 G3 — text-input worker dispatch.
src/state/mod.rs	Plan-06 phase 1 G4 — variable-width-return worker dispatch.
src/state/mod.rs	Plan-06 phase 4d.A — wide-inline-input worker dispatch.
src/state/mod.rs	Plan-07 phase 1 step 1.20 / phase 3 — look up the source position
src/state/mod.rs	Plan-07 phase 1 step 1.20 / phase 3 — pc → source-position table
src/state/mod.rs	Plan-07 phase 1 step 1.20 / phase 3 — publish source_spans
src/state/mod.rs	Plan-07 phase 4 step 4.1 — raise a typed runtime error from a
src/state/mod.rs	Plan-07 phase 4 step 4.6 — bounds-checked variant of
src/state/mod.rs	Plan-07 phase 4 step 4.6 — bounds-checked vector index that raises
src/state/mod.rs	Plan-07 phase 4 step 4.8 — bounds-checked text index. `text[i]`
src/state/mod.rs	Plan-07 phase 4 — typed runtime error halt (mirrors
src/state/mod.rs	Plan-07 phase 4 — typed runtime error halt. Native fns
src/state/mod.rs	Plan-07 phase 4f.12 — stack overflow becomes a typed
src/state/mod.rs	Plan-07 phase 4g.3 — `--dev-soft-halt` (CLI flag /
src/state/mod.rs	Plan-07 phase 4g.3 — read once at first raise. When set
src/state/mod.rs	Publishing used to deep-clone the whole map per entry, which is invisible
src/state/mod.rs	Unaligned by construction — see code_put (@PLAN53 cluster 1).
src/state/mod.rs	Variables from byte code, used to also gain stack position
src/state/mod.rs	anything went wrong. @PLN140 arc C found it by reporting `make` at a line in
src/state/mod.rs	failed fetch — which is the long-running case arc C's sticky
src/state/mod.rs	for a `text` local (a borrowed `Str` arg is @P293-unsafe to push) or an
src/state/mod.rs	reference in the slot; skip anything whose store is no longer allocated
src/state/mod.rs	that were removed at yield. The generator's locals are live again
src/state/mod.rs	the frame no longer holds is rendered as its
src/state/mod.rs	the string is what arc C reports.
src/state/mod.rs	there's no UAF hazard). @P324 narrows this by demoting
src/state/mod.rs	unaligned reference — UB (the @PLAN53 cluster-1 Miri finding) —
src/state/mod.rs	would be @P293-UAF here, so the caller sends ONLY `.to_json()` results
src/state/text.rs	@PLAN53 cluster 2 / S4: N = stepped span of the popped i64+i64+f32
src/state/text.rs	@PLAN53 cluster 2 / S4: stepped char-pop span (4 off, 8 aligned).
src/state/text.rs	@PLAN53 cluster 2 / S4: stepped span of popped i64+i64+f32 (20/24).
src/state/text.rs	@PLAN53 cluster 2 / S4: the char pop occupies a stepped span
src/state/text.rs	@PLAN53 cluster 5: deallocate the String's heap buffer. `clear()` sets
src/state/text.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/state/text.rs	Plan-04 Phase 2h: positional variant of the removed `text()`. Writes a
src/state/text.rs	Plan-07 phase 4e.3 — consume the fault tag set by the
src/state/text.rs	Plan-07 phase 4e.3 — when a fault tag is set on the
src/state/text.rs	sole deallocation point). Miri's leak checker caught it (cluster 5).
src/store.rs	([`crate::database::Stores::load_url_verified`], @PLN97 arc G Phase 0):
src/store.rs	(`pos += 0`; the @PLAN38 hazard `Store::open`'s comment describes) —
src/store.rs	(smaller) store, and the free tree no longer indexes the words that were
src/store.rs	0-size block — the @PLAN38 release infinite loop: must reject, not hang.
src/store.rs	@P284 — guard `rec != 0` like the integer getters do. In release
src/store.rs	@P284 — sibling of `get_float`'s rec=0 guard; needed for
src/store.rs	@P290 — clear the call-bracket free-protection.
src/store.rs	@P290 — mark the store as PROTECTED-FROM-FREE for the duration
src/store.rs	@P294: grow this store's backing buffer to at least `words` words,
src/store.rs	@P317 debug — `LOFT_LOG=zero_claim` (or `LOFT_ZERO_CLAIM=1`) zeroes
src/store.rs	@PLAN38 phase 01 — `size` MUST be read AFTER the resize. The
src/store.rs	@PLAN38 phase 01 — durability-mode tier on this store. Tracks
src/store.rs	@PLAN38 phase 01 — for durable stores, flush mmap + rewrite
src/store.rs	@PLAN38 phase 01 — when `Some(path)`, this store was opened
src/store.rs	@PLAN38 phase 01: this module is now `pub mod store` (was `mod store`).
src/store.rs	@PLAN38 — durable-store support (phases 00 + 01 — IntegrityOnly tier).
src/store.rs	@PLN11 arc E — cheap validity pre-check for a store image file: it
src/store.rs	@PLN119 arc B — re-derive the allocator's cached state from the store's
src/store.rs	@PLN119 arc E — take on a store image another machine sent.
src/store.rs	@PLN14 arc F — this store's whole arena as raw bytes, the counterpart of
src/store.rs	@PLN14 arc G — how many records are currently CLAIMED in this store.
src/store.rs	@PLN97 arc G Phase 2 — `from_bytes` must fail-closed on a crafted buffer:
src/store.rs	@PLN97 arc G Phase 2 — fail-closed structural validation of an
src/store.rs	BEFORE `fl_rebuild`** (@PLN97 arc G Phase 2): a crafted buffer with a
src/store.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/store.rs	Delete a record, this assumes that all links towards this record are already removed
src/store.rs	H6: it used to decode `u16::MAX → null`, which silently nulled a
src/store.rs	Trace helper for @PLAN38 debugging. Writes a line to
src/store.rs	[`from_bytes`](Self::from_bytes). Used to persist the REPL's session store
src/store.rs	`zero_claim` is enabled (@P317 debugging lever). No-op otherwise.
src/store.rs	by a fn-call deep-copy bracket. @P290.
src/store.rs	claim paths (it previously returned WITHOUT `finish_claim`, leaking stale bytes).
src/store.rs	const TIER_SNAPSHOT_EVERY: u16 = 2; // reserved — phase 02
src/store.rs	const TIER_WAL: u16 = 3; // reserved — phase 03
src/store.rs	from `n_set_store_lock(r, false)`. See @P290 for the
src/store.rs	header, not a field — so with debug assertions ON this test used to die at the
src/store.rs	it in the allocation-site ledger (@PLN140 arc A).
src/store.rs	keep legal — @P290's whole point was to stop using `lock_store` here,
src/store.rs	pinned here rather than left for arc A's first caller to discover.
src/store.rs	reclaim", which is indistinguishable from a healthy store, and arc A
src/store.rs	recommended after an untrusted load. @PLN97 arc G Phase 2.
src/store.rs	shipped in phase 01; tiers 2 and 3 land in later phases.
src/store.rs	shrank by, 0 when there was nothing to give. The whole of arc A behind
src/store.rs	stay in memory and never touch the file. @PLN97 arc G Phase 1
src/store.rs	tail hung @PLN119's remote transport (arc E) instead of failing.
src/store.rs	training-port consumer that drives @PLAN38 lives in another repo and
src/store.rs	used to walk a record's fields must sit at this position. Naming it makes
src/store_budget.rs	@PLN140 arc A — live bytes and store count per ALLOCATION SITE, keyed by the
src/test_runner.rs	@P367: a typed runtime fault fired (no Rust panic).
src/test_runner.rs	@P367: assert(false) / panic / divide-by-zero / OOB /
src/test_runner.rs	@P389: each native package's rlib carries its own
src/test_runner.rs	@PLN26 phase 4 — stage native-package DLLs beside the
src/test_runner.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/test_runner.rs	`loft test --native` each exercise exactly one, so a bare "ok" used to be
src/test_runner.rs	here too. It used to engage only on the run path and `loft sandbox-check`,
src/timeout.rs	! @PLAN49 T1+T3 — process-wide execution timeout with a watchdog
src/timeout.rs	@PLAN49 T2 — cooperative graceful exit. A `checkpoint_*` on the
src/tree.rs	Copyright (c) 2023-2025 Jurjen Stellingwerff
src/tree.rs	Should be removed when actually in use
src/trie_db.rs	same-key sibling is never removed by mistake.
src/typedef.rs	@P379 — struct-type registration is a flat table keyed by name. When
src/typedef.rs	@PLN127 arc D — the ONE parse-time site that knows. `a_type` was
src/typedef.rs	B2-runtime (2026-04-13): Before laying out records, retroactively
src/typedef.rs	Copyright (c) 2022-2025 Jurjen Stellingwerff
src/typedef.rs	Plan-06 phase 4d: tuple struct fields inline the
src/typedef.rs	Plan-22 phase 02b (2026-05-12): auto-Reference
src/typedef.rs	`string` used to be special-cased here; it is now one row of the
src/typedef.rs	bare DEF name (shared by both), so the two libs' `__nullable<S>::Null` no longer collide.
src/typedef.rs	corrupts the free path (@P373: a SIGSEGV at scope exit
src/typedef.rs	— phase 02c is the only producer that sets it
src/use_analysis.rs	! EMITTING the bug (the materialisation fix removed the only real-code trigger).
src/use_analysis.rs	! compiler no longer emits the bug, so the positive control is synthetic — without
src/use_analysis.rs	! parser-free — the compiler no longer emits the bug, so the positive control must be
src/use_analysis.rs	**Arc C made this ONE contract.** It used to run on the interpreter only, so
src/use_analysis.rs	@PLN102 arc C step 4 — the FOLD lint (C5.2). For every `#superseded "Y"` symbol X in loft's
src/use_analysis.rs	@PLN24 arc B / @PLN128 arc C — refuse a `#c` binding whose shape exceeds the
src/use_analysis.rs	@PLN90 phase 1 — construction / field-append copies. A field-target append
src/use_analysis.rs	@PLN90 phase 1 — record deep-copies (`OpCopyRecord`): a `v[i] = e` element-slot set,
src/use_analysis.rs	@PLN90 phase 1 — the return-buffer copy. When the single-append target is
src/use_analysis.rs	A reassignment replaces the value, so what was handed off is no longer what
src/use_analysis.rs	Arc B calls C for real, so this is no longer "the interpreter cannot do it";
src/use_analysis.rs	`first_arg_write` set in `parser::find_written_vars`). Used to build `mut_max_pos`,
src/use_analysis.rs	catch — before arc B, `strlen("hello")` compiled under `--interpret` and
src/use_analysis.rs	death releases it. That closed loft#849 — and made a shape that used to LEAK into a
src/use_analysis.rs	living as long as `Data` would answer from a body that no longer exists —
src/use_analysis.rs	pre-order position no longer tracks execution order inside it — bump
src/use_analysis.rs	that is no longer true, and a fix line naming a backend that does not
src/use_analysis.rs	value on one path, so a pending hand-off from before it is no longer certainly
src/use_analysis.rs	write. @PLN128 arc C removed the second fix that used to sit here —
src/variables/intervals.rs	@PLAN53 cluster 2: these three variants all carry child `Value`s
src/variables/intervals.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/variables/intervals.rs	P240 fix (2026-05-11): tuple-literal reads. Without explicit
src/variables/intervals.rs	Plan-07 phase 1: Span carries a source position and wraps a
src/variables/mod.rs	@P295 — keyed collections carry a lifetime dep list too
src/variables/mod.rs	@P376 — assigning the `Never` poison (an errored struct construction,
src/variables/mod.rs	@PLAN28 C4 — borrowed view of the codegen-read fields of one `Variable`,
src/variables/mod.rs	@PLAN28 C4 — owned codegen-read fields of one `Variable`, consumed by
src/variables/mod.rs	@PLAN51 Cluster IV: mark this variable as a caller-side work-ref
src/variables/mod.rs	@PLAN51 Cluster IV: set by `add_defaults` when it synthesises a
src/variables/mod.rs	@PLAN53 cluster 2 / S4 — one eval-TOS / frame-reserve advance step.
src/variables/mod.rs	@PLAN53 — the aligned V2 allocator is the only allocator; scopes.rs drives
src/variables/mod.rs	@PLAN59 (H1): drop a var from the argument set — used to retire the
src/variables/mod.rs	@PLN125 arc B — a value held ONLY for its scope-end hook is never read by
src/variables/mod.rs	Create an exact copy of a variable, used to duplicate them when reused in later scopes.
src/variables/mod.rs	ONE home for a fact three different derivations used to answer separately
src/variables/mod.rs	Plan-07 phase 1: compare via unspan() so the iterated-collection
src/variables/mod.rs	S2 (@PLAN53 cluster 2): the stack-alignment table. Text is align-8
src/variables/mod.rs	S4 (@PLAN53 cluster 2): the eval-TOS step — every advance rounds up to
src/variables/mod.rs	Used to lift inline struct-returning call arguments.
src/variables/mod.rs	`ref_return` no longer re-finds the buffer by work-ref name (@PLAN59 H1), and a
src/variables/mod.rs	aligned reference (@PLAN53 cluster 2).
src/variables/mod.rs	allowed). @PLN40 const-model phase 1; see doc/claude/plans/40-const-fields/.
src/variables/mod.rs	caller. Without that, the timeline recorded deps being ADDED and never REMOVED — so a
src/variables/mod.rs	double-insert those frees (@PLN11 arc D / @PLAN28 C4 insight).
src/variables/mod.rs	else, and a lone SELF dep is the @P302 owned-keyed-local marker.
src/variables/mod.rs	for-loop (including outer loops). Used to detect unsafe mutation during iteration.
src/variables/mod.rs	including outer loops. Used to detect nested same-name loops.
src/variables/mod.rs	information the IR no longer carries. A marker rather than `Type::RefVar` on
src/variables/mod.rs	slot is allowed. The sibling of the `&T` mutable borrow. @PLN40 phase 1.
src/variables/mod.rs	so both spellings lower to byte-identical IR and the `&` used to be dropped as
src/variables/mod.rs	variable at some point in the function. Used to distinguish
src/variables/mod.rs	─── @PLAN28 startup-cache snapshot seam (C4) ────────────────────────
src/variables/slots_v2.rs	! Plan-04 Phase 2 / @PLAN53 cluster 2 — single-pool, scope-blind,
src/variables/slots_v2.rs	@PLAN53 cluster 2 — see the fn doc for the algorithm. This replaced an
src/variables/slots_v2.rs	greedy search avoids. (@PLAN53 cluster 2: without this, V2 placed
src/variables/validate.rs	! plan-04 Phase 2a. Each test constructs a minimal `Function`
src/variables/validate.rs	@PLAN53 cluster 2 — alignment invariant for the **aligned V2 layout only**.
src/variables/validate.rs	Copyright (c) 2024-2025 Jurjen Stellingwerff
src/variables/validate.rs	I8 (@PLAN53 cluster 2): a slot whose byte range exceeds the
src/variables/validate.rs	bloat or a runaway offset (@PLAN53 cluster 2).
src/variables/validate.rs	claim (@PLAN53 cluster 2).
src/variables/validate.rs	class that under the V2-drive attempt (plan-04 retraction) put
src/vector.rs	@P306 — a record with this key already exists: replace it in place
src/vector.rs	@P321(c): bulk-allocate a vector record and copy raw bytes into it
src/vector.rs	@PLN135 arc H — a hash's entries are SLOTS in a chunked arena, so an element is
src/vector.rs	Added in @PLAN37 phase 10.8 to extend `sort()` from
src/vector.rs	Both halves used to be wrong, in units (loft#719). The guard read
src/vector.rs	`(record, offset)` and no longer a record number whose body starts at 8. Its
src/wasm.rs	@PLN18 phase 07 — the browser kernel's loft surface: the SAME lib
src/wasm.rs	Post-Stage B (2026-05-31): web sourced from the fixture clone
src/wasm.rs	Post-Stage B (5b, 2026-05-31): graphics + shapes sourced from
src/wasm_assets.rs	! @lib_plan-29 W1d — generic browser-WASM asset table accessor.
src/wasm_threads.rs	! values (arc D — proven never to break).
