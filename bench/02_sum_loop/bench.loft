// Benchmark 2: Sum loop (10 million integers)
fn main() {
  n = 10000000;
  sum = 0;
  t0 = ticks();
  for i in 0..n {
    sum += i as integer;
  }
  ms = (ticks() - t0) / 1000;
  println("result: {sum}  time: {ms}ms");
}
