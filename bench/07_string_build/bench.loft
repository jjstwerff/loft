// Benchmark 7: String building (500000 appends)
fn main() {
  n = 500000;
  t0 = ticks();
  s = "";
  for sb_i in 0..n {
    s += "item-{sb_i};";
  }
  ms = (ticks() - t0) / 1000l;
  println("result: {len(s)}  time: {ms}ms");
}
