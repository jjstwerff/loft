use p144_types;

pub fn box_ensure(b: &Box) {
  if len(b.items) == 0 {
    b.items += [Inner { val: 0 }];
  }
}

pub fn box_set(b: &Box, idx: integer, val: integer) {
  box_ensure(b);
  for bs_item in b.items {
    if bs_item.val == 0 {
      bs_item.val = val;
      return;
    }
  }
}
