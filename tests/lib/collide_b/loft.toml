[package]
name    = "collide_b"
version = "0.1.0"
loft    = ">=0.1"

[library]
entry = "src/collide_b.loft"

# @PLN26 phase 1 regression — package B exporting the same `#native
# "collide_shared"` symbol as collide_a (see collide_a/loft.toml).
[native]
crate = "loft-collide-b"
