// Library for T1-2 wildcard and selective import tests.
struct Point {
  x: integer,
  y: integer
}

fn add(a: integer, b: integer) -> integer {
  a + b
}

fn mul(a: integer, b: integer) -> integer {
  a * b
}
