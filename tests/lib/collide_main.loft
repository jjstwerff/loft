// @PLN26 phase 1 regression — a program pulling in TWO native packages that
// export the same `#native` symbol (`collide_shared`).  `--native` must reject
// it with a clear compile_error (the C-ABI flat namespace can't disambiguate);
// see tests/lib/collide_a + collide_b.
use collide_a;
use collide_b;

fn main() {
  print("{ca_compute()} {cb_compute()}")
}
