fn add_i(v: vector<integer>, x: integer) -> vector<integer> { out = v; out += [x]; out }
fn main() {
  a: vector<integer> = [];
  n = 0;
  for k in [10,20,30] { n = n + 1; a = add_i(a, k); }
  println("iterations={n} len={len(a)}");
}
