fn add_i(v: vector<integer>, x: integer) -> vector<integer> { out = v; out += [x]; out }
fn main() {
  a: vector<integer> = [];
  a = add_i(a, 1); a = add_i(a, 2); a = add_i(a, 3); a = add_i(a, 4);
  println("{len(a)}");
}
