==== exit: 1 ====
==== stdout ====
==== stderr ====
error: #c "lc_f64": parameter 1 is a 64-bit float, which travels in an SSE register the caller does not write. Wrap `lc_f64` in an ANSI-C shim that takes it by POINTER instead, and bind that — a loft `vector<float>` crosses as a pointer plus a count, so a 1-element vector carries a scalar
  --> <cases>/44_c_binding_float_needs_shim.loft:7:12
  |
7 | #c "lc_f64" "double(double)"
  |            ^

error: #c "lc_f64": parameter 1 cannot be bound: a float cannot cross directly — it travels in an SSE register the caller does not touch. Pass it by POINTER through an ANSI-C shim: a 1-element `vector<float>` crosses as `(const double*, int64_t)`, and C's writes through a `double*` are visible to loft
  --> <cases>/44_c_binding_float_needs_shim.loft:7:12
  |
7 | #c "lc_f64" "double(double)"
  |            ^

error: aborting due to 2 previous errors
