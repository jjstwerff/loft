[package]
name = "loft-ffi-build"
version = "0.2.1"
edition = "2024"
authors = ["Jurjen Stellingwerff"]
license = "LGPL-3.0-or-later"
description = "Build-script helper for loft native extension cdylibs — generates the loft_register! invocation by scanning a package's `.loft` sources for `#native` annotations (source-scan), or from a loft.toml manifest (legacy)"
homepage = "https://github.com/loft-lang/loft"
repository = "https://github.com/loft-lang/loft"
keywords = ["loft", "ffi", "extension", "build"]
categories = ["development-tools::build-utils"]

[lib]
name = "loft_ffi_build"

[dependencies]
