//! expect: 2:3,4|5,6
fn main() {
  v: vector<vector<integer>> = [[1, 2], [3, 4], [5, 6]];
  s = v[1..3];
  o = "";
  for i in 0..len(s) { if i > 0 { o += "|"; } row = s[i] ?? []; for j in 0..len(row) { if j > 0 { o += ","; } o += "{row[j] ?? 0}"; } }
  println("{len(s)}:{o}");
}
