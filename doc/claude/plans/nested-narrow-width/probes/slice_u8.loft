//! expect: 2:33,44|55,66
fn main() {
  v: vector<vector<u8>> = [[11, 22], [33, 44], [55, 66]];
  s = v[1..3];
  o = "";
  for i in 0..len(s) { if i > 0 { o += "|"; } row = s[i] ?? []; for j in 0..len(row) { if j > 0 { o += ","; } o += "{row[j] ?? 0}"; } }
  println("{len(s)}:{o}");
}
