// elements BORROW the args (min_max shape). No owned __ret_text.
fn both<T: Ordered>(a: T, b: T) -> (T, T) { if a < b { return (a, b); } return (b, a); }
fn main() { for i in 0..3 { r = both("b{i}", "a{i}"); assert(r.0 == "a{i}", "lo"); } }
