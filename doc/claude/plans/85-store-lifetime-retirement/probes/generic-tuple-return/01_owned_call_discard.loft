// element 0 = owned method call; result DISCARDED. Isolates pair-body leak.
struct P { id: integer }
fn to_text(self: P) -> text { return "item-{self.id}"; }
fn pair<T: Printable>(x: T) -> (text, text) { return (x.to_text(), "m"); }
fn main() { for i in 0..3 { pair(P { id: i }); } }
