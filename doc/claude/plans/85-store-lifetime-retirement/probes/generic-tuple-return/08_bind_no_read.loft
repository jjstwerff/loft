// bind, read NOTHING. Does the whole __tuple store free at scope exit?
struct P { id: integer }
fn to_text(self: P) -> text { return "item-{self.id}"; }
fn pair<T: Printable>(x: T) -> (text, text) { return (x.to_text(), "m"); }
fn main() { for i in 0..3 { r = pair(P { id: i }); } }
