fn enc(n: integer) -> vector<u8> { b: vector<u8> = []; b += [n as u8]; return b; }
fn main() { tot=0; for t in 0..2 { for i in 0..2 { ki = enc(i); if i == t { x = ki; tot += len(x); } } } println("C ok tot={tot} (want 2)"); }
