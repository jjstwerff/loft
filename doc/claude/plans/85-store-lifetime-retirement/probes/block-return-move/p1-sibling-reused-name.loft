// EXPECT: no leak, prints 1 / 3. Currently LEAKS 1 (block 2's z unfreed).
struct P { x: integer, y: integer }
fn main() {
  { a = { z = P{x:1,y:2}; z }; println("{a.x}"); }
  { b = { z = P{x:3,y:4}; z }; println("{b.x}"); }
}
