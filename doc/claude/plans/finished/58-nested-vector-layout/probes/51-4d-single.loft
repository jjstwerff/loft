// @PLAN58 — 4-deep single += copy.  depth=4, element=single.
fn main() {
  q: vector<vector<vector<vector<single>>>> = [];
  a: vector<vector<vector<single>>> = [[[5.5f]]];
  q += [a];
  assert(q[0][0][0][0] == 5.5f, "elem");
  println("PASSED 51-4d-single");
}
