// @PLAN58 — nested single-vector returned from a fn.  context=fn-return, element=single.
fn mk() -> vector<vector<single>> { return [[1.5f], [2.5f, 3.5f]]; }
fn main() {
  v = mk();
  assert(v[0][0] == 1.5f && v[1][1] == 3.5f, "ret {v}");
  println("PASSED 45-ctx-single-fnreturn");
}
