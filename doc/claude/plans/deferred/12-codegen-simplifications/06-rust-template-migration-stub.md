# Phase 06 — `#rust"..."` template migration

**Status:** RELOCATED — see
[`deferred/13-rust-template-migration/`](../deferred/13-rust-template-migration/).

The phase 06 stub originally lived inside plan-12 to mark the
boundary "this work belongs to a future plan, not under
plan-12's PR."  On 2026-05-02 the deferred-plan convention was
introduced (sibling to `finished/`).  Plan-13's stub moved into
its own deferred initiative because:

- It's well-described.
- It has no driver (no open P-issue, no waiting feature).
- It's 2-3 weeks of focused work; doesn't fit any current PR
  shape.
- The deferred convention reserves a clean home for exactly
  this kind of "available, not abandoned" work.

This file is left as a breadcrumb so anyone reading plan-12
end-to-end finds the new location.  No content lives here
anymore; everything moved to the linked deferred plan.
