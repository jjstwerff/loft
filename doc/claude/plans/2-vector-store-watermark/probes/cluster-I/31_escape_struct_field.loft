struct Bag { items: vector<integer> }
fn f(n: integer) -> integer {
  b: Bag = Bag { items: [0] };
  if n > 0 { a: vector<integer> = [1, 2, 3]; b = Bag { items: a }; }
  b.items[0] + b.items.len()
}
fn test_31() { assert(f(5) == 4, "vec into escaping struct"); }
