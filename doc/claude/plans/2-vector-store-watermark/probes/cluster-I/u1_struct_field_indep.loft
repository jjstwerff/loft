struct Bag { items: vector<integer> }
fn f() -> integer {
  a: vector<integer> = [1, 2, 3];
  b: Bag = Bag { items: a };
  a[0] = 99;
  b.items[0]
}
fn test_u1() { assert(f() == 1, "struct field is an independent copy of a (not 99)"); }
