// Capture a VECTOR cell in an escaping closure.
fn make() -> fn() -> integer { v: vector<integer> = [1, 2, 3]; fn() -> integer { v[0] + v[2] } }
fn test_06() { f = make(); assert(f() == 4 && f() == 4, "vector capture"); }
