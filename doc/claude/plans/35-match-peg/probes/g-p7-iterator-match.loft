// @PLN35 golden · P7 (L3.6 iterator input) — RED until Phase 7 (the ONLY phase adding opcodes).
// Graduates to tests/scripts/35g-iterator-match.loft. Matching over an iterator<T> can't
// re-index, so a failed alternative must replay pulled items — the memo buffer + the two new ops
// OpMatchAnchor / OpMatchRevert (matching.md P-Anchor/P-Revert), bounded by `max_lookahead`.
//
// TARGET SHAPE (exact source wiring finalized in Phase 7, once a coroutine iterator feeds `match`):
//     match some_iter {
//         [ 1, (x:integer)* ] => "call/{len(x)}",
//         _                   => "?",
//     }
// The cursor is buffered (Lexer::memory model); a side-effecting pull is UB-by-contract.

fn main() {
  print("P7 iterator-match: RED (spec only — needs the L3.6 iterator cursor + 2 ops)");
}
