// @DESC call ..rest fn twice on the same subject (text)
// @EXPECT 3
// @LEAK ok
fn f(v: vector<text>) -> integer { match v { [h, ..rest] => len(rest), _ => -1 } }
fn main() { s = ["a", "b", "c", "d"]; a = f(s); b = f(s); assert(a == 3 && b == 3, "reuse: {a},{b}"); print("PASS"); }
