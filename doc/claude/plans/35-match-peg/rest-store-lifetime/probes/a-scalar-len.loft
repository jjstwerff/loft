// @DESC len(rest) in-arm (scalar)
// @EXPECT 3
// @LEAK ok
fn f(v: vector<integer>) -> integer { match v { [h, ..rest] => len(rest), _ => -1 } }
fn main() { assert(f([10, 20, 30, 40]) == 3, "n={f([10, 20, 30, 40])}"); print("PASS"); }
