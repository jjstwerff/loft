// Copyright (c) 2026 Jurjen Stellingwerff
// SPDX-License-Identifier: LGPL-3.0-or-later
//
// PLAN53 cluster 2 / sub-cluster 2f — REFERENCE (must always PASS).
//
// The SAME vector iteration as 2f-01 but WITHOUT `#remove` — plain read-only
// iteration round-trips fine under V2 (the iterator-step path is sound; the 2b
// fix covered it).  The delta between this PASS and 2f-01's PANIC is the
// `#remove` mutation during iteration — isolating the trigger to the
// `remove()` path's V1-tuned state-block deltas.
//
// Status aligned: PASS ; Flag-OFF: PASS.

fn main() {
  v = [for i in 0..10 { i }];
  s = 0;
  for x in v {
    s += x;
  }
  assert(s == 45, "2f-02: sum {s}, want 45");
  println("2f-02 PASSED");
}
