// @PLN152 phase B — overflow into a u8 slot.
// Rule: formal/types.md spare-code table + (E-Uncomp-NN).
// @EXPECT: a04_ovf_u8 = 0
fn main() {
  x: u8 = 250;
  x += 10;
  println("a04_ovf_u8 = {x}");
}
