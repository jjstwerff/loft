# Biting-issue landing plan — designs to bring the remaining bites home

Concrete step-by-step plans for the open programmer-biting issues
tracked in [CAVEATS.md](CAVEATS.md) and [PROBLEMS.md](PROBLEMS.md).
Each design follows DEVELOPMENT.md's *test with `#[ignore]` → code →
enable → docs* cadence.  When a plan calls for multiple commits, the
commits are listed in landing order so an implementer can follow them
verbatim.

Four issues are in scope.  C3 (WASM parallelism) and C38 (closure
capture) are accepted design trade-offs — no plan here; they're
closed-by-decision and only need documentation if pain resurfaces.

| ID | Severity | Milestone | Scope |
|---|---|---|---|
| [C58 / P135](#c58-p135--canvas-y-direction) | silent wrong pixels | 0.8.5 | S |
| [P137](#p137--loft---html-brick-buster-unreachable) | browser build dead | 0.8.5 | M (bisect) |
| [P54](#p54--jsonbody-newtype) | silent malformed JSON | 0.9.0 | M |
| [C54](#c54--integer-widening-to-i64) | silent null on MIN | 0.9.0 | L (breaking) |

---

## C58 / P135 — Canvas Y direction

**Bite:** non-square sprite atlases render sprites at swapped rows
because three Y-flips (upload row-reverse, UV coord, 2D ortho
projection) don't cancel on non-square layouts.  Invisible on N×1 or
square atlases — the bug only surfaces when a user graduates to a
real sprite sheet.

**Root-causing commits:** `src/wasm_gl.rs:567` (`wgl_upload_canvas`
row reverse), `lib/graphics/src/graphics.loft` (V-coord in
`draw_sprite`, ortho projection in `create_painter_2d`).

**Design:** canonicalise `(0, 0) = screen-top-left`, y grows down.
This matches HTML canvas, PNG file order, and how users already think
about 2D drawing.  The 3D pipeline's internal GL texture coords stay
internal — only the public 2D API commits to top-left origin.

**Steps (one sprint branch `c58-canvas-y`):**

1. **Test first.** Extend `tests/scripts/snap_smoke.sh` with a 2×2
   atlas that draws four distinct 1-pixel sprites into four canvas
   corners.  Mark the assertion `#[ignore = "C58 canvas Y"]`.
2. **Lock the invariant in LOFT.md.** One paragraph in the graphics
   chapter: "The 2D canvas origin is the top-left pixel; `y` grows
   down."  No code change — this is the contract everything else
   preserves.
3. **Remove the upload flip.** Drop the row-reverse in
   `wgl_upload_canvas` (src/wasm_gl.rs:567) and its native mirror in
   `src/canvas_upload.rs` (if present).  Rationale: upload is the
   last flip introduced, and removing it lets the UV coord and ortho
   projection cancel correctly for rectangular atlases too.
4. **Update `draw_sprite`'s V-coord** in `lib/graphics/src/graphics.loft`
   so row 0 of the atlas maps to the top of the sprite rectangle.
   One-line arithmetic flip; document the direction.
5. **Leave the 2D ortho alone.** `-2/H` in `create_painter_2d` is the
   GL-convention compensation for the flipped Y — keep it so the
   shader sees top-left-origin input but writes GL-bottom-left-origin
   output correctly.
6. **Enable the test.** Remove the `#[ignore]`; snap_smoke.sh now
   passes all four corners.
7. **Rebake Brick Buster's atlas.** Its layout was drawn against the
   old flip cascade; swap rows so it looks identical under the new
   canonical order.  Regression test: the existing brick-buster
   visual snap remains pixel-identical.

**Acceptance:** 2×2 atlas places each sprite in its intended corner;
Brick Buster unchanged.

---

## P137 — `loft --html` Brick Buster `unreachable`

**Bite:** the deployed WASM build wedges on the first `loft_start`
call — `RuntimeError: unreachable executed`.  Native build runs
correctly.  Blocks the "share a link, anyone plays" story and the
Moros editor.

**Narrowed down** (already in PROBLEMS.md #137):
- Not an instantiation failure.
- Not a `todo!()` — `grep -c 'todo!'` on `/tmp/loft_html.rs` = 0.
- Not an asyncify artefact — reproduces with `wasm-opt -O1 --strip-debug`.
- Panic is in generated bytecode dispatch, not a host call.

**Design:** bisect via import-stub poisoning.  The wasm trap carries
no source location, but the *sequence of imports called* up to the
trap is observable from the JS side.  If the last import before the
trap always targets the same loft function `F`, then `F`'s emitted
Rust body is the suspect.

**Steps (sprint branch `p137-html-unreachable`):**

1. **Reproducer harness.** `tools/wasm_repro.mjs` — a Node script that
   loads `doc/pkg/loft_html.wasm`, stubs every `loft_gl.*` and
   `loft_io.*` import with a function that appends its own name to a
   global `trace` buffer and calls a provided inner impl (from
   `tests/stubs/loft-gl-stub.mjs` shipped for the smoke path), then
   invokes `loft_start` inside a try/catch that prints the last 50
   trace entries on panic.  Ship as `make wasm-repro`.  No
   `#[ignore]`'d test yet — the harness is the diagnostic.
2. **Identify the last-called-import.** Run the harness, record the
   final trace entry.  Map that import name through
   `src/extensions.rs` (the import table) to the loft `#native`
   function whose emitted Rust calls it.
3. **Read that function's emitted Rust** in the captured
   `/tmp/loft_html.rs`.  Look for type-marshalling mismatches —
   the likeliest pattern from PROBLEMS.md §3 is a `text` param
   emitted as a single `i32` instead of `ptr, len`.
4. **Fix in `src/generation/dispatch.rs::output_native_direct_call`.**
   The marshalling should match the host's extern signature in
   `src/extensions.rs`.  Add a `debug_assert!` that the wasm import
   parameter arity matches the loft signature arity — converts a
   future runtime `unreachable` into a codegen-time error.
5. **Add a smoke assertion to `make game`** that runs
   `tools/wasm_repro.mjs` against the freshly-built wasm with the
   stub imports.  CI fails if `loft_start` panics.  This is the
   regression guard.
6. **Unignore the previously-gated browser smoke test** in
   `tests/scripts/snap_smoke.sh` (if present) or add a new one.

**Acceptance:** `brick-buster.html` runs in Node under the stub JS
without panicking; `make wasm-repro` exits 0.

**Fallback if the bisect points at bytecode dispatch, not a
`#native` function:** add `eprintln!` breadcrumbs to each
`Op*` handler's emitted Rust body (guarded by a compile-time
`LOFT_WASM_TRACE` flag) and re-run the harness to find the last
opcode executed before the trap.  This expands scope from S to M.

---

## P54 — `JsonBody` newtype

**Bite:** `json_items(body)` returns `vector<text>`.  Caller writes
`MyStruct.parse(body)` and gets a silently-zeroed struct on
malformed input — no type check, no runtime diagnostic.  The typeless
API contradicts loft's "static types catch mistakes" promise.

**Design:** introduce a `JsonBody` newtype wrapping `text`.  It's the
*only* type `MyStruct.parse` accepts.  Callers who pass arbitrary
`text` get a compile error pointing at `json_items`.  Add three shape
predicates (`.is_object()`, `.is_array()`, `.is_null()`) so callers
can branch before invoking a parser whose schema doesn't fit.  Full
`JsonValue` enum (Object / Array / String / Number / Boolean / Null)
is deferred to 1.1+ — 80% of the type-safety gain at 20% of the
design surface.

**Steps (sprint branch `p54-json-body`):**

1. **Test first, all `#[ignore]`'d.**
   - `tests/issues.rs::p54_json_body_returned_from_json_items` —
     `json_items` return type is `vector<JsonBody>`.
   - `p54_parse_rejects_plain_text` — parse error when passing
     bare `text` to `MyStruct.parse`.
   - `p54_parse_accepts_json_body` — `JsonBody` → `MyStruct.parse`
     works end-to-end.
   - `p54_shape_predicates` — `.is_object()`, `.is_array()`,
     `.is_null()` return the expected booleans for five crafted
     inputs (object, array, null, number, malformed).
2. **Add the type.** `default/03_text.loft` (or a new
   `default/04_json.loft`) declares:
   ```loft
   pub struct JsonBody { body: text }
   pub fn is_object(self: JsonBody) -> boolean;   #rust"..."
   pub fn is_array(self: JsonBody) -> boolean;    #rust"..."
   pub fn is_null(self: JsonBody) -> boolean;     #rust"..."
   ```
3. **Update `json_items`** signature to `vector<JsonBody>` (currently
   `vector<text>` in the same file).  Implement by wrapping each
   extracted body text.
4. **Gate `MyStruct.parse`** in the parser (`src/parser/control.rs`
   where `.parse()` on a `Type::Name` is resolved).  The argument
   type must be `JsonBody`; any other text-typed argument produces
   a diagnostic pointing at `json_items`.
5. **Unignore the tests** one commit per test, confirming each
   shape.
6. **Document** in LOFT.md and STDLIB.md — one short section in the
   JSON chapter, before the existing `.parse()` examples.  Note the
   1.1+ `JsonValue` plan at the bottom.

**Migration:** this is a breaking change for existing `.parse(text)`
callers.  Two-line fix per call site: `body: JsonBody { body: s }`
at the call, or use `json_items` which now returns the right type.
The common-case call chain (`for body in json_items(raw) { X.parse(body) }`)
keeps working unchanged because `json_items`'s return type updates
in lockstep.

**Acceptance:** `p54_*` tests green; an intentionally-malformed
input produces a compile or parse-time error instead of a silent
zero struct.

---

## C54 — `integer` widening to i64

**Bite:** any arithmetic that lands on `i32::MIN` silently returns
`null` (and debug-aborts).  Integer division by zero also returns
`null`.  In a language pitched as "reads like Python", this is
actively hostile — users hit it when multiplying microsecond
timestamps, accumulating checksums, or building bitmasks.

**Design:** switch `integer` from `i32` to `i64`.  At 8 bytes per
field the overhead is rounding error on today's machines; `i64::MIN`
is `-9.2e18`, so accidental sentinel collisions effectively vanish.
`long` becomes a historical alias (already `i64`).  Future code
writes `integer` and Just Works.

**Breaking-change budget:** schema layouts change (every `integer`
field grows from 4 to 8 bytes).  All `.loftc` bytecode caches become
invalid on first load — add a version bump that auto-rebuilds.  All
native-code artefacts (`.loft/*.rs`, `*.wasm`) rebuild from source
on first run.  Test suite field-offset expectations change in
~30 places.

**Steps (sprint branch `c54-integer-i64`, lands on `main` in 0.9.0):**

1. **Tests first.**
   - `tests/issues.rs::c54_i32_min_round_trip` — `-2_147_483_648 * 1`
     returns `-2_147_483_648`, not null.  `#[ignore]`.
   - `c54_arithmetic_at_boundary` — add/sub/mul/div crossings at
     ±i32::MAX produce the mathematically correct i64 result.
   - `c54_schema_layout_8byte` — a struct `S { x: integer }` has
     field size 8 (assert via `sizeof` or the existing schema test).
   - `c54_loftc_cache_invalidated` — running a program built with
     the old cache triggers an auto-rebuild, not a crash.  (May be
     an integration test against a pre-canned pre-C54 `.loftc`.)
2. **Bump `size(Type::Integer)`** in `src/database/mod.rs` from 4 to 8.
   Walk every caller that assumes 4 bytes: grep for literal `4` near
   `Type::Integer` or `Content::Long(i64::from(i32))` patterns.
3. **Update `get_int`/`set_int` call sites** that read/write integer
   fields to use the 64-bit variants (or introduce a single
   `get_integer`/`set_integer` helper keyed off the type tag).
4. **Update native codegen** (`src/generation/emit.rs`) — `integer`
   emits as `i64` in Rust, not `i32`.  Format strings that used
   `format_int` switch to `format_long`.  Sentinels change from
   `i32::MIN` to `i64::MIN`.
5. **Bump `.loftc` cache version** (`src/state/bytecode_cache.rs`)
   so the old cache is invalidated automatically.  Caches built
   before this commit lose their version match and trigger a
   rebuild on first load.
6. **Deprecate `long`.** Keep it as a type alias for `integer` (now
   i64) so existing code keeps working.  A follow-up 1.1+ commit
   can emit a deprecation warning; don't do that in 0.9.0.
7. **Sweep tests** — update every field-offset expectation, every
   `i32::MIN` sentinel check, every hex literal assumption.  This
   is mechanical but touches ~30 files.
8. **Enable the C54 tests** commit-by-commit as the layout work
   settles.
9. **Document in LOFT.md and CHANGELOG.md** — one short paragraph
   explaining that `integer` is now i64 and pointing at the
   migration note for 0.8.x code.

**Rollout risk:** schemas and `.loftc` caches change.  Users with
persisted loft databases will need a migration pass — add a
`loft --migrate-i64 <dbfile>` tool that walks the schema and
expands each integer column from 4 to 8 bytes.  Design this before
touching `size(Type::Integer)` so it lands in the same sprint.

**Acceptance:** C54 tests green; full suite rebuilds and passes;
Brick Buster runs under both `--interpret` and native; one loft
program that hit the old sentinel collision now returns a
mathematically-correct i64 value.

---

## Out of scope (closed by decision)

- **C3 — WASM `par()` runs sequentially.**  Web Worker pool cost
  exceeds benefit today; revisit when a concrete browser game
  demands CPU parallelism.  Documented in LOFT.md's parallel
  section as an explicit limitation.
- **C38 — Closure capture is copy-at-definition.**  Reference
  capture needs GC or borrow tracking, neither of which fits the
  "no lifetime annotations" ethos.  Test
  `tests/scripts/56-closures.loft::test_capture_timing` pins the
  current semantics.

## Order of work

Recommended landing order, by user pain × implementation risk:

1. **P137** — WASM unreachable.  Highest visibility (blocks deploys);
   bisect harness is cheap and reveals root cause fast.
2. **C58 / P135** — Canvas Y.  Small, well-understood; locks the
   invariant before more games land on the wrong convention.
3. **P54** — JsonBody.  Type-safety win; no breaking wire changes.
4. **C54** — integer widening.  Largest blast radius, schedule last
   in 0.9.0 so earlier bites are fixed on the existing i32 before
   the layout changes.
